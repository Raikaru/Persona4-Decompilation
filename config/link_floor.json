{
  "linked_tu_count": 170,
  "note": "Ratchet floor for translation units linked from compiled source. Raise it when a unit is genuinely added; never lower it to make a build pass. Guards a gap neither SHA1 nor verify.py covers: an ineligible TU falls back to retail bytes, so the image still verifies while its functions stop being proven from source. See check_link_floor() in tools/build.py. Lowered 170 -> 169 on 2026-09-03: code1_0039_cw119.c took three more b119 functions that are not contiguous with its first, so it left the link by the gap rule (see object_layout_is_placeable); the matches themselves are unaffected. 2026-09-03 (wave 36): code1_003a.c left the link the same way when func_003a3de0 moved to code1_003a_cw119.c (which is eligible, so the TU count stayed 169 while 83 linked functions became 1); C-linked functions 1547 -> 1524. Parents lose eligibility as the b119 units take functions out of their middle; the RenderWare-source port is expected to move the block wholesale, after which the cw119 units become contiguous and eligible again. Raised 169 -> 170 on 2026-09-03: src/renderware/plcore/rwgrp.c (one b119 function, contiguous) is link-eligible."
}
