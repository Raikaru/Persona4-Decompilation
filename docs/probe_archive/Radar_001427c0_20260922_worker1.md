# Radar draw 001427c0, 2026-09-22

`func_001427c0` is integrated in `src/promoted/code1_0014.c`. The final
production owner reports **122 MATCH / 4 ASM**. Native MWCC b210 with the
owner's `-O2 -Iinclude` profile emits 1068 executable bytes in the 1072-byte
retail window. All 14 relocations and the four-byte zero suffix are exact.

The recovery resumes after upstream rebase HEAD
`c4388741f9070eaec2b37e3a3af3684886d1a581`. All new measurements are under
`build/continue-first-party-20260922/resource-transform/radar001427c0/post-rebase-c4388741-01/`.
The earlier resource-transform closure and all old probe destinations remain
unchanged.

## Source and behavior

The function draws a seven-vertex triangle fan: the center and six perimeter
entries, with the sixth perimeter entry closing the five-point shape. It
then draws five point sprites, retaining the sprite handle and its RGB
palette for that phase. Sprite opacity is the product of the point's tint
and the low byte of the supplied opacity, divided by 255.

The palette is a real three-byte RGB record. Its typed field accesses retain
the palette base through the unsigned-byte float conversions. A separate
`u8 spriteAlpha` retains the low-byte narrowing before the sprite loop. The
first draw still converts the full supplied word as unsigned, matching the
retail path. All float arithmetic ordering, calls and stores are preserved.

The packet owns seven complete, quadword-aligned PS2 vertices, with a byte
view for the measured store offsets. The public vertex field layout follows
`include/rw/sky2/rwplcore.h:4641-4692`. Only the fields written by retail are
initialized; the first vertex's reciprocal-depth store also remains the
first vertex's store inside the perimeter loop. No synthetic initialization
or store to another vertex was introduced.

Ordinary C now generates the retail 0x250-byte frame, all five quadword GPR
saves and all five floating-point saves. The old archive's putative save
instruction barrier was a consequence of missing palette, sprite and input
lifetimes. The final body uses scoped `opt_loop_invariants on`, inherited
from the measured floor, and no assembly or volatile access.

## Contracts

The definition now places depth between the packed position and integer
arguments. This preserves the EE's separate integer/float argument streams
and reproduces the retail argument-save order. The sole first-party source
caller is the guarded `func_001400f0`; its declaration and call were changed
together. Two fresh, complete native owner compiles with this caller activated
prove identical caller bytes and relocation meanings. All 125 functions
other than the newly recovered radar target and all allocated data are
preserved in that controlled comparison.

The camera getter in `src/promoted/code1_0045.c:1360` returns `s32`. The
owner's declaration now agrees, and pointer uses explicitly preserve its
32-bit address bits with `(u8 *)(u32)`. All existing active functions remain
byte-identical. The sprite provider in `src/promoted/code1_0034.c:477`
accepts two float coordinates, three byte colors and a word opacity. The
00364c50/00364c70 providers in `src/shdMisc.c:340-347` take no arguments and
set/clear the existing rendering flag around the primitive submission.

The radar's typed device bindings use the existing `_abs` address-alias
mechanism for 00887300 and 00887310. Their signatures agree with
`include/rw/plcore/badevice.h:184-207`: an enum and pointer for state setting,
and a primitive enum, PS2 vertex pointer and signed count for drawing, both
returning status. The relocation proofs bind each alias to its actual retail
slot. Broader callback-type candidates were retained as experiments: they
changed two argument-setup words in sibling 0014dd80 and were not integrated.

## Evidence

`final-native/` holds the exact source, object, compile receipt, full resolved
proof and separately replayed `independent-proof.json`. The target references
no owned data. The final whole-owner object preserves all 125 sibling
functions and all previously allocated data, including the 00146a10 vectors.

`candidate-official/` and `production-official/` each retain an exit-zero
official report with 122 MATCH / 4 ASM. Their completion records explicitly
bind the selected native object and source. They reuse that successfully
completed native object and run zero additional compiler jobs.

`guarded-caller-before/`, `guarded-caller-after/` and
`guarded-caller-continuity.json` hold the controlled caller verification.
`validation.json` holds the final independent target replay and scoped checks.
Lint reports zero errors and one inherited pragma warning outside the target.
The scoped whitespace check passes.

| Artifact | SHA256 |
| --- | --- |
| Integrated owner | `0fa76b7fe581bd4f0aade9b7881c3a4e8f07d263f31fb8e90706c37b30282395` |
| Native owner object | `6f9a6fdc6182a8648c61a964a1b91648165daa070b174ba6991f619d5f21fc56` |
| Resolved radar bytes | `98f45f3641fa4b373bd7e5623a6118c5fca0b60f1ce7b0b0e41c7b96acad87fd` |
| Production official report | `475800ad7f318f12264a5a385ae8c4ed7c70ff504220b1e41cd00a5f6b27afe0` |
| Native b210 compiler | `286548490e2e902cfef21dcf39cd5af23766731585d90dea747f8781eadcafd7` |

The final native preparation was session 98818; official preparation and
guarded caller checks were session 32083; integration was session 21776;
final validation was session 78881. All completed successfully. Python's
nonfatal pywin32 bootstrap warning did not affect the returned exit codes.
One earlier script patch was blocked before execution; its separate replacement
was created normally. No vendor implementation, shared header, commit, push,
or whole-repository build was part of this recovery.
