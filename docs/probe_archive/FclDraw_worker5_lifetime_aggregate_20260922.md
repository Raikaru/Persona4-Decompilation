# FclDraw worker-5 lifetime and aggregate evidence — 2026-09-22

**Result: zero promotions.** The live owner remains 50 MATCH / 20 ASM. All work in this lane is scratch or recovery evidence; the game source and shared headers were not edited.

The source baseline is `e3136243e06bc894355c79b778c7b1ed4ae3d02b2363c030a95aed04e8e215d2`, frozen from the dirty workspace at HEAD `5a1bcf7e889d9b1fb1560201d686669739014501`. It is bound to the closed 6484 MATCH / 376 ASM census and the completed `build/first-party-continue-live/fcl/objects/live-29e40/receipt.json`. This run did not repeat a baseline build or claim a fresh official owner MATCH.

At closure, concurrent work had changed HEAD to `c4388741f9070eaec2b37e3a3af3684886d1a581`. Live Fcl source, recursive dependencies, compiler profile, tools, audit receipts and retail identities still match the frozen inputs. Baseline HEAD is an ancestor of closing HEAD: **False**. The first closure preflight (session 49465) rejected the changed HEAD, and the second (77737) rejected non-ancestry, both before writing results. Closure therefore records the branch relationship and authenticates actual source/object/input identities. No native job was repeated.

Fifty new native objects used the configured MWCC b210 `-O2 -Iinclude` profile. Forty-nine targets were fully resolved and nonzero; one successful native compile was rejected for distant pointer entries encoded as out-of-range GP-relative references. Every object preserved all 69 sibling function bytes/relocations and all owner data. Closure re-read and authenticated every source/object/receipt, re-resolved every target, and rechecked the unchanged live source and dependencies.

Lane: `build/continue-first-party-20260922/worker5-fcl-lifetime-v1/`. Its `closure.json`, `authenticated-native-ledger.json`, `ranked-remaining.json`, frozen `inputs/`, per-candidate `receipt.json`, `neighbors.json`, and full `residual.json` are the evidence. Alignment edit counts are diagnostics, not raw word equality. A short candidate with a nonzero missing retail suffix remains invalid.

## New results

| Function | Native objects | Diagnostic result |
|---|---:|---|
| `func_00314ef0` | 8 | 1060/1056 bytes; 23 alignment edits; 243 differing resolved words; NONZERO. |
| `func_00318840` | 7 | 1784/1776 bytes; 195 alignment edits; 403 differing resolved words; NONZERO. |
| `func_0031cce0` | 17 | 2368/2384 bytes; 46 alignment edits; 589 differing resolved words; NONZERO. |
| `func_00320b80` | 7 | 1880/1888 bytes; 50 alignment edits; 363 differing resolved words; NONZERO. |
| `func_0032e570` | 11 | 2916/2800 bytes; 171 alignment edits; 640 differing resolved words; NONZERO. |

The strongest new mechanism is `0031cce0`: using the actual `FclDrawColor` return and whole aggregate copies eliminates the byte-wrapper intermediates. The complete color/snapshot candidate has 46 alignment edits but 589 differing resolved words and misses nonzero retail tail instructions. Another variant has retail-sized code (2384 bytes) and still differs in 503 resolved words. Neither is a match.

Retail `0031D5C0`, the IDA headstart and the Ghidra headstart agree that the one-digit transition destination uses destination Y+6. The guarded C instead reused origin Y+6. This correction exists only in the scratch candidates. Retail also requires branch-local coordinate reloads that typed C currently eliminates, retaining an extra saved float. See `objects/cce0/native-color-snapshots-complete/alignment.txt` and the full retail `asm/nonmatchings/y_fclCombineDraw/func_0031cce0.s`.

`0032e570` has separately confirmed buffer and opaque-call ordering errors: final-label storage is 128 bytes and the template is 64 bytes. The first candidate exposes a real GP relocation failure for `D_00644D18`, `D_00644D1C`, and `D_00644D20`; its `rejection.json` contains complete addresses and displacements. Later scratch pointer-entry declarations produce full-address loads. Those candidates still do not match.

## Ranked remaining functions

This ranking separates observed source/retail findings from untested mechanisms. All 20 frozen bodies were read. New native compilation covered five functions; older completed jobs were inspected rather than rerun.

| Rank | Function and focus | Evidence and next mechanism |
|---:|---|---|
| 1 | `func_0031cce0` — Actual color results and coordinate lifetimes | Retail 0031D5C0 consumes destination Y+6 for the single-digit transition; the guarded C instead reused origin Y+6. Scratch corrects this. Actual FclDrawColor returns and whole color copies eliminate intermediate byte-wrapper copies. Retain source/destination Y only across the observed phases, then reproduce the branch-local coordinate reloads. The compiler currently retains an extra float and eliminates required reloads. Coordinate input declaration/call sites in y_fclCombine.c need coordinated repair before promotion. **New native probes; best alignment result remains nonzero.** |
| 2 | `func_003218a0` — Checked loop values and real task storage | Completed prior paired-view evidence leaves actual pointer storage width and separately normalized loop values unresolved. Model real 32-bit task pointers and checked signed loop values. Preserve opaque lookup/store/constructor order. The previous zero-looking reordered candidate is invalid; do not introduce a 128-bit pointer holder or padding. **Read current body and completed prior evidence; no repeated compile.** |
| 3 | `func_00314ef0` — Row depth constants across draw-call boundaries | The current provider takes an actual FclVec2 and FclDrawColor. Distinct fixed-depth inline wrappers, source-handle placement and real coordinate aggregate returns did not beat the prior 23-edit frontier. Resolve the 151.0f retained depth and 149.0f property lifetime without dummy state or global provider changes. The own-file guarded caller and declaration must remain consistent with coordinate inputs. **Eight new native probes; none exact.** |
| 4 | `func_00320b80` — Cell-operation result and induction lifetimes | Checked row/column snapshots changed register allocation but did not beat the prior frontier. Separate actual initial/transition draw results reached 50 alignment edits with a zero retail suffix, still nonzero. Reconstruct actual task/mode/pointer storage and the two loop-counter snapshots while retaining the real shared position contract and opaque call order. No fake wide pointer storage. **Seven new native probes; none exact.** |
| 5 | `func_00323d00` — Selected resource and alpha transfer chain | Old close scores used a wide provider contract that conflicts with the live narrowed draw-family contract. Prior alpha/declaration permutations do not establish a valid zero. Audit the actual provider/caller parameter widths first, then model resource and alpha lifetimes. Do not promote the old incompatible 29-word frontier. **Read current body and completed prior evidence; no repeated compile.** |
| 6 | `func_0032b9d0` — Panel and selected-row call lifetimes | Prior current-contract probes retain a nonzero frontier after staging panel/row values and genuine shared position calls. Resolve actual mode width, scroll conversion and lookup/write/reload boundaries. Keep the B1 update and subsequent resource lookup in retail order. **Read current body and completed prior evidence; no repeated compile.** |
| 7 | `func_00318840` — Selected position snapshot and retained Y | A genuine void storage copy replaces split scalar position snapshots. Extending selected Y across all icon placements reduces the new residual but remains nonzero. Preserve incoming raw slot versus normalized slot lifetimes and actual s32 depth. The related Combine declaration currently narrows depth and requires coordination before promotion. **Seven new native probes; none exact.** |
| 8 | `func_0031c2b0` — First detail-row color results and Y phases | The guarded body uses the same intermediate byte-color wrappers and two-coordinate input family as cce0. Its existing single-digit destination Y is already distinct. Apply the genuine FclDrawColor return mechanism, then reconstruct coordinate phase lifetimes. Preserve the 126/74 resource and glyph behavior; coordinate the stale Combine declarations and calls. **Read current body and caller/decompiler evidence; not newly compiled.** |
| 9 | `func_00324680` — Aggregate alpha snapshots through resource pairs | The guarded body transfers colors and alpha through the resource chain 297/28B/27D/270 with intervening provider calls. Use actual four-byte aggregate copies only at observed snapshot boundaries, and retain each resource-pair lifetime. Check retail before reordering any lookup, load or store. **Read current body; hypothesis remains untested.** |
| 10 | `func_00315600` — Mutable position snapshots across date draws | The body repeatedly reads mutable position storage across draw and date-provider calls. Replace invalid separate scalar views with genuine coordinate aggregates and use the already verified storage-copy idiom where retail snapshots the position. Preserve repeated date queries and exact arithmetic grouping. **Read current body; hypothesis remains untested.** |
| 11 | `func_003147e0` — Cursor row contracts and bounds lifetimes | The guarded caller uses the 14ef0 family together with actual cursor position and bounds packets. Keep the row-coordinate declaration/call consistent, then isolate cursor and bounds lifetimes. Preserve genuine 128-bit bounds data rather than using it as a register-allocation substitute. **Read current body and prior 14ef0 evidence; no new target compile.** |
| 12 | `func_00317900` — Paired coordinate snapshots and text centering | Incoming coordinate packets feed several selected-card sprites and image-width-dependent centering calls. Use real coordinate aggregates and stage observed centering results across the correct provider calls. Verify operand order before changing fused arithmetic or grouping. **Read current body; hypothesis remains untested.** |
| 13 | `func_0031fa20` — Image height and shared final-handle lifetime | The final handle is used by two items before one close. Local provider declarations need reconciliation with the current shared contracts. Audit and remove stale local provider signatures, then model actual Y and image-height call lifetimes. Preserve mode-specific fade calls and shared-handle closure order. **Read current body; contract work precedes new probing.** |
| 14 | `func_0032e570` — Label buffers, opaque calls and row counters | Retail and both decompilers distinguish a 128-byte final-label buffer and 64-byte template buffer; guarded sizes were swapped. Retail also orders entity loads after opaque calls and requires full-address loads for distant label-pointer entries. Continue actual caption/counter lifetimes after those corrections. Scalar distant pointer declarations emit invalid GP-relative relocations; no masking or low-bit-only acceptance is allowed. **Eleven new native objects: ten resolved nonzero, one strict relocation rejection.** |
| 15 | `func_0031e5b0` — Mode-dependent logical and asset resource lifetimes | The four-way suffix selection distinguishes logical resource ID from asset ID; later flags override only the logical resource. The final image handle is shared by two items. Retain both actual resource roles, use genuine color returns, and preserve switch/default behavior and final handle closure. Any table-bearing candidate requires the complete owned-table resolver. **Read current body; hypothesis remains untested.** |
| 16 | `func_0032c660` — Two-header coordinate aggregate storage | Many eight-byte coordinate writes target separately declared f32 locals, assuming unrelated locals are adjacent. The two header phases then reuse these snapshots across conditional draws. Replace every such pair with a real coordinate aggregate before lifetime probing. Preserve verified bounds packets and the distinct 6/8/99 Y offsets until retail confirms each. Coordinate incoming parameter contracts. **Read all 601 guarded lines; source object-model repair is prerequisite.** |
| 17 | `func_0031ac10` — Provider contracts and branch-local resource roles | A local func_002ba080 declaration conflicts with the float-bearing call family visible in 191c0. Resource selection and the late arcana ID have distinct lifetimes. Audit actual provider arity/types before native probing, then use real color/position aggregates and preserve the late resource role and opaque-call ordering. **Read all guarded lines; provider audit is prerequisite.** |
| 18 | `func_003191c0` — Real pointer storage and conditional color values | The guarded body contains artificial u_long128 holders for ordinary pointer snapshots and conflicting local declarations for func_0034b880. Remove artificial wide pointer state, reconcile actual providers, and reconstruct the raw/normalized slot and selection-color lifetimes. Do not inherit those storage tricks into a candidate. **Read all guarded lines; source and provider repair are prerequisites.** |
| 19 | `func_00321e60` — Controller branch widths and cell snapshots | Several scalar flag/color accesses use wide casts despite byte-oriented indexing or four-byte consumers; each requires provider and retail confirmation. Audit matrix element widths and color reads, then reconstruct input-mode and per-cell position lifetimes. Preserve the repeated opaque cell lookups until proven redundant from actual semantics. **Read all guarded lines; width audit is prerequisite.** |
| 20 | `func_00325450` — Palette aggregates and switch-owned draw layout | The large selector has separate scalar coordinate storage assumptions, six palette values, mutable position reuse and several switches. Use genuine coordinates and FclDrawColor results, then isolate actual per-case coordinate lifetimes. Preserve position reloads after constructors and use full owned-table proof for every switch candidate. **Read all 585 guarded lines; aggregate and table reconstruction precedes probing.** |

## Contracts and preservation

The current `fcl_color.h`, `fcl_draw_types.h`, `fcl_row_draw.h`, `fcl_draw_task.h`, and `fcl_combine_internal.h` are frozen and preserved. The `0031cce0`/`0031c2b0` coordinate declarations and related calls in `y_fclCombine.c` require prime coordination before any eventual promotion. The `00318840` retail depth is an actual s32; its related narrower declaration also requires coordination. No provider or related owner was changed.

Existing exact `0032f4d0`, `0032a960`, `0032b000`, `00329e40`, and the shared position contracts are preserved by whole-source identity plus per-object sibling and data checks. No candidate uses ordinary ASM for the target, invented state, padding, artificial wide pointer storage, or a changed shared provider signature to obtain a match.

The previous close `00323d00` result is not accepted because its provider contract is incompatible with the live family. The previous zero-looking `003218a0` source is not accepted because it changes opaque lookup/store/constructor order. Their detailed receipts remain in the older lanes.

## Completion and delivery

Every native command above completed. The initial e570 parent process exited 1 after its successful native compile because strict relocation resolution failed; all other recorded probe sessions exited 0. Sessions and command receipts are listed in `closure.json`. Blocked exec attempts were not executed and were not counted.

Prime messaging was attempted repeatedly and rejected with: “No sub-agent run is active. The chat that calls agents action=spawn becomes the prime agent of a new run.” No message delivery is claimed. This on-disk handoff remains available independently of agent routing.

No commit, push, rebase, full build, provider edit, live source promotion, or fabricated zero proof was performed.
