# Field render-queue build 00148280: rewrite at 25 edits

`func_00148280` (5024-byte window, 1254 instructions, frame 0x290) stays
INCLUDE_ASM. `RenderQueue_00148280_20260926_body.c` scores 25 fnalign edits
(1254/1254, +225 reloc-only) and 42 reloc-masked differing words. The guarded
draft in code1_0014.c scores 226 edits (1224 instructions, frame 0x2A0).

The first 400 instructions (list heads, callbacks, the l12/l10/l11 walks and
the first sort) were already right. The changes are all in the second half:

- `func_0014a160() == 1 && (*(u16 *)e & 0x3FF) >= 100` selects D_007943F0
  for the sorted list-1 entries. The old draft ignored the return value.
- The attach-point fallback is a struct copy
  (`pos = *(RwV3d *)(mdlGetMatrix(..) + 0x30); pos.y += 175.0f;`).
  Retail loads all three floats, then stores all three, then reloads y.
- The sorted-list loops read the entry through the slot pointer every time.
  Retail reloads `arrA[i]` after each call. In the list-1 loop that pointer
  is an explicit `u8 **pp = &arrA[i]` (149 -> 88; indexing through
  `arrA[i]` keeps the recomputed address in `$s7` and grows the frame by
  0x10). The arrB loops read the same with or without `pp`.
- Every loop gets its own block-scoped flags word, `j` and object cursor.
  The arrB loops declare `j` before `k` in the loop block (88 -> 25 over
  several steps; `n2` as `s32` lets retail recompute `n2 - 1` for each
  loop).
- The list-2 loop has a real bug in retail: the `flags & 0x04000000` arm sets
  the flag and `continue`s without advancing `l2`. The body reproduces it.

Residual (25):

- In the list-1 and arrB `j` loops, retail loads the entry before `sll j,2`
  on the first `unk140[j]` read. We emit the shift first. None of these
  changed it: `((s32 *)(e + 0x140))[j]`, `j * 4 + e + 0x140`, `(e + 0x140)[j]`
  style, `j*4` first.
- The arrB loops swap the slot pointer and `j` (`$s4`/`$s2` and `$s3`/`$s1`).
  In the second arrB loop retail also recomputes `&arrB[k]` after the
  callback allocation instead of keeping it from the loop head.
  Declaration order and scope for `j`/`k`/`pp` (about 40 variants) and flags
  scope (64 variants) do not fix it.
- tools/permute.py ran for 1500 s (7126 compiles) from this body and found
  nothing below 42 words.
