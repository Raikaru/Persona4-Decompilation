# Camp skill application: func_0013f720

The September 20 continuation recovers `0013f720` as exact ordinary C:
**1060 executable bytes in the 1072-byte retail window**, all **30 relocations
fully resolved**, and twelve verified zero alignment bytes. The final body
uses the configured MWCCPS2 b210 `-O2` profile without a new optimization pragma.

## Corrected behavior and contracts

Retail branches to the count test at `0013F808` before entering the multi-target
loop. The saved do/while draft incorrectly executed once for a zero or negative
count. The recovery restores the initial test. IDA and Ghidra independently
agree with the assembly on this control flow.

The two character inputs use the actual signed-halfword provider contract.
The target retains the existing offset-first address helper, ascending shared
case labels, effect order, conditional resource deductions, saved persona
context restoration and result-dependent sound calls. Its caller supplies the
four real arguments and converts the result to a signed byte. The old extra
arguments and 32-bit expression shifted by 56 were decompiler artifacts. That
larger calling draft remains behind its assembly fallback.

The existing `0013ca60` forwarder now uses the same signed-halfword character
parameters. In `0013f620`, an unsigned-halfword skill load is held in an `s32`
local so the truthful `u32` provider declaration preserves the emitted code.
The value remains in the unsigned-halfword range.

The `g_data` wrappers `0010f540`, `0010f6a0`, and `0010f930` explicitly return
the signed result of their real downstream calls. The first also explicitly
accepts and forwards the argument present in retail. Their character inputs,
pointer arguments and downstream signatures agree with the actual datCalc
definitions. All real source callers of these wrappers are in the two updated
owners; generated decompiler copies are not authoritative callers.

## Verification

| Function | Executable / window bytes | Resolved relocations |
| --- | ---: | ---: |
| `0013f720` | 1060 / 1072 | 30 |
| `0013f620` | 244 / 256 | 5 |
| `0013ca60` | 64 / 64 | 1 |
| `0010f540` | 32 / 32 | 1 |
| `0010f6a0` | 204 / 208 | 11 |
| `0010f930` | 328 / 336 | 18 |

Fresh live-owner objects are identical to the independently reviewed complete
proposals. All 42 non-target camp functions and all 137 `g_data` functions
preserve their instruction bytes and canonical relocations; allocated data
remains unchanged. The private `g_data.c` assertion filename is separately
compared with retail at `0x005E4298` before its local symbol is resolved.

The official current-source verifier reports **172 MATCH / 8 ASM across 180
functions**; the only promotion is `0013f720`. All markers are preserved.
Scoped lint has no errors and three pre-existing optimization-provenance
advisories outside the recovered body. Whitespace checks pass.

Evidence is retained in `build/first-party-resume-active/camp/`: `HANDOFF.md`
describes the measured alternatives, `formal/` contains the worker's complete
before/release compiler checks, and `current/proof.json` plus
`current/verify.json` record prime's fresh integrated checks. Provider proofs
use the real `g_data.c` basename; earlier exploratory `owner.c` copies changed
the assertion filename and are not the final acceptance evidence.
