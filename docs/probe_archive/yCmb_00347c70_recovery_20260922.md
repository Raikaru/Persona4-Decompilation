# Card-quad updater: accepted C recovery, 2026-09-22

`src/promoted/y_CmbCardEff.c::func_00347c70` is active C and officially
**MATCH**. The scoped owner report is **32 MATCH / 3 ASM**, with 35
first-party functions total. This note records this owner's result only;
other concurrent first-party changes are outside that count.

All new evidence is under
`build/continue-first-party-20260922/card-effect-worker2-20260922T0652Z/`
(called `lane/` below). No provider, SDK, RenderWare, CRI, shared-header,
compiler configuration, or other owner implementation was edited.

## Behavior and actual contracts

The constructor `func_00348160` allocates 0x1B0 bytes and registers this
callback through `include/sdk_task_registration.h`. Its `SdkTaskUpdate`
contract is `s32 (u8 *task)`, matching the recovered definition and existing
owner declaration. The callback obtains the work pointer at task+0x38.
The constructor initializes flags at work+0x11C, three four-byte colors
at work+0x190, and the scale at work+0x1A0. The position and alpha setters
are the existing `func_003482d0` and `func_003482a0`.

The camera getter is called before testing the enable flag. When bit 1 of
work+0x11C is clear, the updater returns zero without touching the quad,
submitting a packet, or advancing either animation. Retail's branch at
0x00347CA8 jumps to 0x0034810C, past all those operations. The old guarded
draft closed this guard after only the vertex loop; the accepted C uses the
full guard already established by the saved exact candidate.

An enabled update writes four vertices at a 0x40-byte stride. Each receives
the existing depth minus one, reciprocal camera depth, and the four byte
color components converted to floats. The eight coordinate stores form a
square centered on work+0x134/+0x138, with half-width 64 times the scale.
The scalar reads and fused arithmetic retain their original order.

When `func_00285b30() < 0x208`, the updater queues a four-vertex primitive
using `D_00794F00`, then writes `func_00347b30` to packet+8 and the work
pointer to packet+0x10. The existing draw callback has the two-pointer
definition `void func_00347b30(u8 *, u8 *)`.

Position animation uses mode zero, the existing start/end vectors, and
signed-halfword time/duration. It increments time through the existing
clamp helper or applies retail's exact `0xFFFB` flag mask on completion.
That mask is retained literally; it also clears any high 16 flag bits.
Alpha animation uses mode one, start/end alpha bytes, and signed duration
divided by two with C's truncation toward zero. It stores the native
float-to-byte conversion and either advances or resets its timer. Every
path returns zero. State and timer reads remain at their original points
relative to provider calls.

Four private declarations now agree with their actual definitions:

| Function | Existing provider | Correct owner declaration |
| --- | --- | --- |
| `func_002b2aa0` | `src/promoted/code1_002b.c:200` | `f32 (s64, f32, f32, f32, f32)` |
| `func_002b2cb0` | `src/promoted/code1_002b.c:250` | `s32 (s32, s32, s32, s32, s8)` |
| `func_00457120` | `src/promoted/code1_0045.c:1360` | `s32 (void)` |
| `func_00461390` | `src/sdkOt.c:307` | `u8 *(u8 *, s32, s32, s32)` |

The getter's word is explicitly converted to the target's 32-bit pointer.
The primitive provider stores its third argument as a raw word at
packet+0x20, so the vertex address is explicitly converted to that word.
Its implementation was inspected for this contract and left untouched.
The interpolation modes and clamp modes passed throughout this owner fit
their provider domains. The owner retains all preceding matches.

The signed-halfword loop index ranges only from zero through three. Its
wide copy is actually consumed in the vertex address computation; the
unused `off` assignment from the saved candidate is removed. Address
addition uses unsigned 32-bit arithmetic. There is no synthetic memory
state, added padding, dummy store, forced undefined behavior, incompatible
call cast, or ordinary-instruction assembly in the recovered body.

## Source changes that closed the remaining instructions

The saved `index-copy-wide` candidate already emitted 1204 exact bytes but
used the conflicting declarations and the unused offset assignment.
Correct declarations plus offset cleanup left four instruction-order
differences at target offsets 0x254 through 0x260: the integer-converted
vertex address was prepared before the queue and primitive arguments.

Scoped `opt_propagation off` and explicit packet argument locals restore
that order. An explicit camera-result lifetime keeps 1.0 out of a saved
floating-point register across the getter call. An explicit first scale
read restores the first coordinate's load order. The existing
`opt_loop_invariants on` retains the depth-symbol base hoist. Both settings
are enclosed in the function's push/pop scope.

Selected immutable probe receipts:

| Receipt directory | Full resolved differing words | Emitted/window bytes |
| --- | ---: | ---: |
| `no-unused-offset-canonical` | 4 | 1204/1216 |
| `unsigned-address-canonical` | 4 | 1204/1216 |
| `packet-argument-locals` | 4 | 1204/1216 |
| `staged-whole-opt_propagation-off` | 297, from shifted layout | 1216/1216 |
| `staged-camera-result` | 3 | 1204/1216 |
| `staged-camera-scale` | 0 | 1204/1216 |
| `final-named-canonical-v2` | 0 | 1204/1216 |

Pointer arithmetic spellings and inline adapter/staging helpers retained
the same four call-setup differences. Their objects and receipts are
preserved in the lane. The failed `final-named-canonical` scratch candidate
had a renaming-script error that changed pragma `off` to `vertexOffset`.
It was rejected, retained as evidence, and replaced in a new destination
after the script was corrected to leave preprocessor lines untouched.

The old notes and guarded body remain byte-for-byte in
`lane/production-before.c`; their dated floor claims are superseded by this
recovery. Earlier evidence remains in
`build/first-party-resume-20260922/card-effect/` and its `card_effect.py`
driver. `lane/baseline-reuse.json` authenticates the saved baseline source,
dependencies, compiler, flags, and object. That completed baseline was
reused without compiling it again or overwriting any old probe destination.

## Acceptance evidence

The configured native compiler is MWCC PS2 **3.0.1 b210**, with `-O2` and
`-Iinclude`, compiling the whole owner in its normal context. Compiler
SHA-256:
`286548490e2e902cfef21dcf39cd5af23766731585d90dea747f8781eadcafd7`.

`lane/native-owner/compile.json` binds the final source and dependency
hashes to the freshly compiled full object. Its `full-proof.json` resolves
all **14 target relocations** independently by two existing proof routines
and verifies **1204 emitted bytes / 1216 retail bytes**, including **12
zero tail bytes**. The target owns no separate data. All **34 sibling
functions**, their canonical relocations, and all allocated owner data
match the authenticated baseline.

`lane/official/verify.json` is the ordinary `tools/verify.py` main result
for this owner. The wrapper records the exact arguments and copies its
fresh compiler output before the verifier removes its temporary directory;
it does not change comparison, symbol resolution, statuses, or boundaries.
`lane/official/owner.o` is byte-for-byte identical to `native-owner/owner.o`.
The official object also has its own `full-proof.json`.

| Bound artifact | SHA-256 |
| --- | --- |
| Integrated owner source | `6ea52ff1837e235c2464843b49dfc0c4200cd4f438021feaf0c97c4b09003bb5` |
| Native and official complete object | `ecc5992fa219297cf6aaa0a7c4f93012c0ba7380f3ba52ca0fc1badce3a58eb2` |
| Official JSON report | `6b6699176294e7deae581f5bf1399e1b118dbbc3690bf22201977e5b917e6823` |

`lane/official/source-binding.json`, `proposal.json`, and `integration.json`
link the unchanged final source to both proofs. Scoped decomp lint reports
zero errors and one H003 advisory for the measured loop-invariant pragma;
the explanation above and exact receipts justify it. The scoped
`git diff --check` passes.

This is compiler, relocation, whole-owner preservation, and source/retail
semantic review evidence. No gameplay, GPU, allocator-failure, sanitizer,
or hardware execution test is claimed. The source retains the engine's
existing valid task/work/camera and animation-input assumptions.

## Remaining owner work and sessions

`func_0033e810`, `func_00345700`, and `func_00348c40` remain ASM. Inspection
of `0033e810` finds an inherited block-scope four-argument declaration of
`003dc740` conflicting with the owner's three-argument contract. Inspection
of `00348c40` finds an inherited explicit `Cmb48Work.pad190` and an older
frame/return/register residual. Those need renewed contract and storage
review before another candidate is accepted; neither was promoted or
given a new matching claim in this recovery.

Completed terminal receipts for the decisive work:
`17384` (exact canonical candidate), `76440` (named exact candidate),
`95245` (final source/native proof), `52594` (integration), `2021` (official
verification and object identity), and `10921` (lint and scoped diff).
The rejected first naming pass is completed session `15337`.
No commit, push, or full build was performed.
