# Fcl result-state machine: accepted C recovery, 2026-09-25

`src/Event/Fcl/y_fclCombine.c::func_003097e0` is active C and **MATCH**
(object 6264B inside the 6272B retail window, 0 differing words; the tail is
alignment padding). Owner file: 32 MATCH / 9 ASM of 41. No other function
changed status.

## Relocations
193 in total. R_MIPS_26 to 36 callees (func_002e48a0, func_00106390,
func_00105f50, func_00303a20, func_002bab80, func_002badc0, sprintf,
func_003489c0, func_00348c30, ...). HI16/LO16 go to D_00641B50 and two
anonymous rodata literals. GPREL16 goes to iGpffffb3d4, iGpffffb440,
iGpffffb44c and iGpffffa8a4 (gp -0x575C = 0x00763994, now in
config/symbols_recovered.txt).

## The last blocker: the $t1/$t2 swap in the 6-id scans
This started from `Fcl_003097e0_body_20260925.c`: size-exact, 23 words, all
from one register swap in states 0xCC/0xCE. Retail keeps the outer counter in
`$t1` and the search index in `$t2`.
- While the search is the `fclCombineHasEntry` static inline, its index is
  always coloured after the caller's counter. This holds whatever the
  declaration order, loop form or scope of the counter.
- Putting the whole outer loop in a second inline does not help either. b210
  does not inline a call inside an inlined body, and with
  `#pragma inline_depth` the swap persists.
- **Fix:** write those two scans out in the caller. Declare the index `n`
  before the outer counter `m` (`s16 n; s16 m; u8 *w; s32 r;`; `w` and `r`
  may go anywhere). Keep the inline's result shape: `r = 1; goto found;`
  inside the loop, `r = 0;` after it, then `found: if (r == 1)`. With `m`
  declared before `n` it is 23 words again.
- The new locals moved the `func_00247770` result in the state-0xCD `else`
  arm from `$s1` to `$s4` (3 words). Passing the call straight into
  `func_00311930(func_00247770(...), func_002e48a0(...), 0)`, with no `v`
  temp in that arm, closes it. Changing the order or type of `v`, `r` and the
  scan locals, or using a function-scope index, did not.

The single-id tests still use the inline helper. The contract fixes from the
draft stay in: `func_00105f50(u16)` and `s32 func_00303a20` returning the
`func_00303610` result.
