# func_00387e70 (code1_0038.c): persona list panel, matched 2026-09-26

Window 2880 B (2876 B of code plus padding), frame 0x100, nine saved GPRs
plus f20-f23. Relocations: `D_0064EEB0` (assert file), `D_005E5810` and
`D_005E5850` (font resource names), the gp literals 178.5f (-0x7C24,
written as a literal; the pool links correctly) and `fGpffff83b0`
(-pi/6, the sibling idiom), and `func_0034f460`/`func_0034f4a0`,
`func_00364c90`, `func_00275020`, `func_00109280`/`func_00109220`,
`datPersonaGetLevel` and `func_0010b5b0`.

The guarded draft had 626 fnalign edits. A fresh rewrite from the retail
listing started at 93 edits, and declaration and type probes took it to 0.

- **Handles are `s32 fonts[2]`.** Retail's `addiu $v0, $sp, 0xF8; bnez`
  is an assert on the array's address before the two `func_0046a770`
  loads.
- **The ease is `0.0f + 2.0f * t - t * t`.** This gives the
  `adda.s zero, 2t; msub.s t, t` shape. The later ones are
  `2.0f * t - t * t` (plain `mula`). Positions use `0.0f + K + W * e`, as
  func_00387750 does. The int-to-float operands (526, 269, 313, 471, 493)
  are passed as ints: b210 does not fold those conversions.
- **The title x is its own variable.** In each switch case it is
  `x = 154.0f; pos.x = x;`, and the first call uses `x`. Retail sets
  `$f12` and stores `pos.x` in every case.
- **Variable reuse.** One `y` holds `178.5f * fade` and then, in the loop,
  `pos.y * slide`. The 160*ease product in the tail is a common
  subexpression, not a variable.
- **Types.** `id` is s32; its second use re-masks to u16. `icon` is s32
  with `(u16)((u16)icon - 1) + 0x21`. `value` is u16, clamped with
  `if (value > 99)`: `>= 100` puts the compare in `$v0` instead of `$at`.
  The entry is `(s32 *)((u8 *)&state[idx] + 0x4C)`.
- **Declaration order.** f32 `t x y fade slide` (52 edits down from 72-83
  in other orders). u8 `barA alpha r barB g b`: barA and alpha share
  `$s4`. With `r` first it is 49; with the given order it is 0.

No pragmas are needed. All 91 other functions in code1_0038 keep their
status (90 -> 91 MATCH, 5 -> 4 ASM). Lint count is unchanged.
`tools/build.py`: both SHA-1 OK, 594 linked.
