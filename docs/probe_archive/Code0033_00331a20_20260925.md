# Level-gated event pick 00331a20

`func_00331a20` in `src/promoted/code1_0033.c` is now C and verifies as
MATCH: 347/347 instructions, 0 differing words, 1388 bytes in the 1392-byte
window. The other 38 functions in the file keep MATCH.

## Starting point

The guarded body sat at 24 differing words for several passes, all of them
the `$s2`/`$s3` exchange between the `work` pointer and the sign-extended
level. Declaration order, statement order, the pragma pair sweep and the
`s16`/`s32` spellings of the `level` copy had all been measured flat or worse.

## Levers

- **Natural control flow first.** The goto-dispatched body was rewritten as a
  `switch` with the case bodies inline and the scan conditions as one `&&`
  chain per loop. That ties at 24, so it costs nothing and is the shape the
  match was found in.
- **No `level` copy; compare `level_value` directly, under
  `#pragma opt_loop_invariants on`** (scoped with push/pop). The loop
  invariant pass hoists the `(s32)level_value` extension out of the first
  scan. The hoisted temporary gets `$s3`, and `work` goes back to `$s2`.
  Measured on the same body:

  | spelling                                   | words |
  |--------------------------------------------|-------|
  | separate `s32 level = level_value`         | 24    |
  | direct compare, no pragma                  | 45    |
  | direct compare, `opt_loop_invariants on`   | 0     |

  The earlier pair sweep had tried `opt_loop_invariants on` only with the
  separate `level` copy, where it ties at 24.

## Notes

- `lint` reports the scoped pragma as H003 (advisory). The measured comment
  sits directly above it.
- The trailing `return -1` after the second scan reuses the `-1` left in
  `$v0` by the loop test. The source needs no special spelling for this.
