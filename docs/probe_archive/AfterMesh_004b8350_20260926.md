# After-image mesh build 004b8350: rewrite at 135 edits

`func_004b8350` (2720-byte window, 680 instructions, frame 0x100) stays
INCLUDE_ASM. `AfterMesh_004b8350_20260926_body.c` is a fresh body without the
guard's `opt_propagation off`. It scores 135 fnalign edits (680/680). The
guarded draft scores 350.

Measured steps (fnalign edits):

- rewrite (switch per phase, u16 `w` for the strip vertex counter, which
  gives retail's `daddiu 2`; `sphere.radius = 1000000000` gives retail's
  runtime `cvt.s.w`): 217;
- `next`/`mid` locals in the mode-0 strip loop; `base`/`next`/`a`/`b` in the
  mode-1/2 loops (retail keeps `base + 1` / `base + 2` in sq spills), with
  block-scoped colour-clear loops (retail uses temporaries there) and a
  `last = n * 3 + 6` local: 173;
- counters `i`/`j` declared after the pointers, opt_loop_invariants on
  (retail hoists the 1.0f/0.5f words out of the UV loops): 150;
- the first flags test reads the config pointer as `*(s32 *)arg0`. Retail
  does not CSE that load with the per-case `count` reloads, but it does CSE
  the later flags test with the UV-phase reads: 136;
- while-loop increment order in the UV loops (`q += 6; k++; h += step`): 135.

What is left:

- the mode-1/2 strip loops have `w`/`k`/`base`/`next` in `$s0`/`$s1`/`$s6`/`$fp`,
  where retail has `$s1`/`$fp`/`$s0`/`$s6`. The index, colour and UV loops also
  swap their counter and pointer registers.
- the case-1 and case-2 UV blocks come out differently in retail (`$f0`/`$f1`
  against `$f4`/`$f0`, and different pointer/counter temps). So the two
  copy-pasted blocks in the original source were not identical.
- climbing the order of the function-scope declarations (single-line moves,
  about 60 compiles) found nothing better than 136. Case-scoped `base`/`next`
  shared with the tail calls scores worse (225).
