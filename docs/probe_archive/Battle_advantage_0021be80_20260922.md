# Battle advantage panel: func_0021be80

The recovered ordinary C closes the final guard in `src/promoted/code1_0021.c`.
With the immutable portrait and transition recoveries, official native b210
verification is **108 MATCH / 0 ASM**. All 108 functions pass full unmasked
retail proof. The target supplies 5560 instruction bytes in its 5568-byte window;
the remaining 8 bytes are zero tail alignment. Every one of its
208 relocations is resolved. All 107 siblings preserve
their instruction bytes and canonical relocations relative to
`ready-transition-v2`; all 105 existing C functions also remain unchanged from
the production 105 MATCH / 3 ASM baseline.

The final selector is a complete operation: it samples once to choose the
alternating-pattern phase, samples again for segment selection and returns the
actual unsigned-halfword result. The caller publishes that result with one
store to work+4. Recovering this operation's local lifetime closes the final
AND operand ordering at 0021d3d4. It uses the actual stateful `u32(void)` random
provider and preserves both calls, their order, unsigned floating conversion
and signed positive-pattern shift. It introduces no fake prototype, artificial
side effect, synthetic state, CPU assembly or optimization pragma.

The rest of the function restores both full background paths, reflected tiles,
all six inlined segment operations, both complete four-vertex strip builders,
diagnostic bounds checks, real color channels, frame-four sound calls and
frame-dependent selection. The tile object consists of six floats. All eight
records at 00628fd0 and both complete Vec2f anchors at 006290b0 are authenticated.
Horizontal reflection uses 1.05f around 319; vertical reflection uses 228.5f.
Both emitted literal sections are separately bound to their exact retail
addresses: **007615bc = 6666863f** and **007615c0 = 00806443**. The 4-byte pools
have 4-byte alignment, contain no relocations and match their full retail bytes.
The two original jump tables, 24 and 40 bytes, preserve all 16 data relocations and
also have complete retail proofs. All 72 allocated data bytes across all four
sections are covered; nothing is excluded or masked.

The real 0x2f0 stack frame contains two arrays of four 64-byte shared sky vertices,
six complete 16-byte anchor snapshots and the 4-byte color union. The arrays
occupy 0x80..0x17f and 0x1a0..0x29f; anchors occupy 0x180, 0x190 and 0x2a0..0x2d0;
the color is at 0x2ec. Every consumed position, UV, reciprocal depth and color
field is initialized through the actual vertex provider. Other shared vertex
fields are real RenderWare layout members. No ad-hoc padding array is added.

The private 0021b500 provider has only same-owner consumers. Its real formal
order is destination, six float values, then packed color. Its source now has
that contract and private linkage; its own emitted body remains identical.
Both original quad helpers and every other sibling body are preserved.

The target uses `sdk_snd_internal.h`'s actual
`s32 func_0045af60(s16,s16,s16,s16)` interface, confirmed against `src/sdkSnd.c`.
The inherited local declaration that shadowed this header has been removed.
Its small constant arguments retain their exact call setup and the result is
unused. The platform render-state provider uses the complete real
`enum RpSkyRenderState`, a `void *` payload and an `s32`/RwBool return.
The enum and contract agree with `include/rw/sky2/rwcore.h:5683-5747`;
the full retail window confirms pointer payloads for states 4/5 and 0/1 returns.
ALPHA_1 explicitly accepts packed constants, so the four calls pass their actual
0x48/0x44 values through that pointer payload type. No provider implementation
or header is changed. This package supersedes the v1 ABI review and patches.

The callback uses the actual dispatcher contract
`void(u8 *drawPayload,s32 workAddress,void *callback)`. Factory 002011c0 allocates
and zeros 0xa20 bytes; 00200fd0 initializes the 12-byte panel work at +0x9e4, places
the callback at +0x9f8 and its work address at +0xa00. Update 00200cf0 queues the
node at +0x9f0. Dispatcher 004623a0 supplies node+0x1c, its stored work address
and callback identity. Only the work address is consumed. Start 0021d440 resets
the frame and sets active; frames 0..47 render and advance, frame 48 clears
active, and 0021d470 queries completion. The address-only declaration in code20
still says void(void); it is never used for a direct call and its registration
erases the pointer type. This package changes no code20 source or header.

`abi-data-review.json` records the complete frame, real work lifecycle,
ten external data spans, 18 retail provider/caller windows, all direct
call sites, all literal bindings and every owned-data relocation. Runtime
pointer and callback slots are documented with their initial ELF contents;
the review does not treat those contents as immutable runtime values.

The reviewed final source was compiled once after the actual sound and render
contracts were restored. Its whole object is identical to discovery case 23. The official
verifier reused that authenticated object without a duplicate compile.
No production files, configuration, headers or tools were changed and no
global build ran. Prime owns final linker placement and integration.

Receipts: `build/finish-first-party-20260922/resume-07-battle/ready-advantage-v2/`.
Native source/object: `build/finish-first-party-20260922/resume-07-battle/native/advantage-authentic-sound-and-sky-state-contracts-25/`.
`change-after-transition.patch` applies this recovery and archive after the
immutable 107 MATCH / 1 ASM source. `change.patch` applies all three pending
recoveries and archives to the 105 MATCH / 3 ASM production source. Both are
applied in independent scratch copies and reproduce the exact final hash.

Source SHA256: `667d261ff11266371d7f6b48e8d4fdec4c66db4256b6f73185938033ed7f0494`.
Object SHA256: `8ac1f8e0f6004b8cf3a22e3e6b4c8ae5986fb0878d5fe697fdc5e7643bf9a6b5`.
Resolved target SHA256: `8c34cf715a936485f0e0c0ccf97df00d914c1cc1d0932e22d13233668bc1b9fe`.
