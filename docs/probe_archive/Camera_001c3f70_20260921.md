# Target and actor camera poses: 001c3f70

The configured MWCCPS2 b210 `-O2 -Iinclude` compiler emits **4,504 exact
executable bytes** for `func_001c3f70`, with eight zero alignment bytes in
the 4,512-byte retail window. All **74 relocations** resolve exactly. The
official actual-owner verification completed in session **26879**, exit 0,
with **61 MATCH / 2 ASM** in `src/promoted/code1_001c.c`.

All 62 sibling functions preserve their bytes and canonical relocations;
allocated data is unchanged. The clean proposal and actual-owner objects
agree for every function and allocated data section. The clean proposal
compile completed in session **40784**, exit 0. `git diff --check` passed.

## Native geometry and lifetime reconstruction

The complete 1,169-line retail assembly and both full IDA/Ghidra bodies were
read before editing. Eight `RwV3d` objects, three independent horizontal
vectors, four grouped bearing/projection-input vectors and two complete
camera poses replace the padded raw frame. They reproduce the retail
0x1a0-byte frame without explicit padding or alignment annotations.

The old guard confused actor, target and resource radii and omitted cached
actor/target top heights. It also mixed the pre-projection and post-projection
height decisions, omitted a half-distance focus adjustment, and left the
alternate minimum-side calculation as an empty conditional. The accepted
body restores the retail radius factors, all distinct height branches,
the 125/135/275/550-unit floors, both actor/target focus calculations, and
the actual normalized saved-direction coordinates used for lateral offsets.

The selected unit changes to the target only on the side-camera path and
is passed consistently to the final camera-state provider. The current
action pointer is reloaded at the final provider call, as in retail.
Both complete poses, quaternion snapshots, 25-unit height floors, duration
selection and optional notification are preserved.

Separating the resource radius, branch-local camera distances and final
duration reproduces the preserved floating-register lifetimes. The existing
ordinary `func_001c_mul_add` helper preserves the final projection product's
operand order. No new helper, optimization pragma, padding, volatile access,
artificial store, or assembly was introduced.

## Contracts and evidence

Sphere-center calls use `(BtlUnit *, RwV3d *)`; the camera predicate receives
its actual `s32` address argument. The projection provider receives four
complete two-float objects and returns a floating line distance. Look-at,
quaternion-transform and transition calls use complete typed vectors/poses.
Only this target and its block-local declarations changed. Shared providers,
headers, configuration and publication files were not edited. Valid retail
camera/action/unit/resource pointers remain prerequisites; the proof does
not claim whole-game execution coverage.

Evidence root:
`build/first-party-finish-20260920/camera-current-work/3f70-release/`.
The integrated proof, official report/receipt, actual compile receipt,
object, source snapshots, applied patch, resolved/retail bytes and every
sibling/data check are retained there. Completed compiles and official
verification must not be rerun merely to recover their output.

Source SHA-256:
`8b7ca8d4393509ced5acfac0b0bb26b799b56e1fe0bd65d186595630e47c5361`.

Actual-owner object SHA-256:
`b75646b136b70e87dcec75318243790f50a1db83c81a123b737860d17b6ca721`.

Resolved target SHA-256:
`48baa54697f2ce9b9d7cba88d9e6ebc4393716b9d468a57ceceabf418bc1b3bc`.

Compiler SHA-256:
`286548490e2e902cfef21dcf39cd5af23766731585d90dea747f8781eadcafd7`.

The earlier 001c1040, 001c17a0, 001c21d0 and 001c2ee0 proofs are retained.
The five-word 001c5b80 frontier is unchanged; the next active target is
001c9820.
