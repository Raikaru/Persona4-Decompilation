# Battle ring-segment renderer, 2026-09-22

`src/promoted/code1_0020.c::func_0020f730` is active C and officially MATCH:
820 emitted bytes in the 832-byte retail window, 26 fully resolved code
relocations, twelve zero alignment bytes, and three complete owned lookup
tables. The owner has 139 MATCH / 7 ASM at this closure.

The caller passes a packed four-byte color and visits segment indices 1 through
11. The recovered signature agrees with that caller. Reading its representation
through unsigned bytes preserves the RGB/alpha components and the original
word spill. Width and height queries use their existing floating return types;
the renderer's channel parameters remain unsigned bytes.

The initialized corners, interleaved offsets, and sprite table reproduce the
retail values at `00626c40`, `00626c50`, and `00626c70`. The offset table is one
24-byte union object with complete six-float X and five-float Y array views.
The Y array starts after the first float. Byte strides 0, 8, and 16 remain
within each actual array, including the four-byte float read. The one-element
array of this complete union preserves the native aggregate initialization.
An earlier exact-looking candidate traversed beyond a single float subobject;
it was rejected and is not the integrated implementation.

Distinct width, height, adjustment and coordinate lifetimes retain the original
floating-point expression order. The global scale is reloaded at the original
call boundaries. The signed panel threshold is read after the first draw,
which preserves changes a drawing callback can make to the panel. No provider
is declared pure, no render call is moved, and no source assembly, artificial
padding or unused state is introduced.

The configured native compiler is MWCC PS2 b210 with `-O2 -Iinclude`. Final
source SHA-256 is
`a4a45072b06c4c9cb49a8dcd353f8af9579b45ae11dfac1bbde803f4eb4b47d4`;
the complete native object is
`4a7834d70d043ad4bf87d8a7591fd47a7b438688965896038a1ba920bf823465`.

`build/continue-first-party-20260922/panel20f730/integrated/completed.json`
binds the actual source, compiler, headers, fallback inputs, object, and
official report. Its final closure reused the already completed native
compilation explicitly. Separate unmasked proofs protect the existing caller
`0020fa70` and menu callback `00207320`; all other 143 functions retain bytes
and canonical relocations. Original allocated sections remain present, and
the only additions are the three completely proved lookup tables. An initial
closure incorrectly removed the old menu table while comparing data; the
corrected comparison preserves every original section and permits only proved
additional sections. No code or relocation comparison was weakened.
