# Field event step: func_001727f0 recovered, 2026-09-26

`src/promoted/k_fldEvent.c::func_001727f0` is active C and **MATCH**
(932-byte object in the 944-byte window; the tail is retail alignment).
The file went from 23 to 24 MATCH.

Relocations: `func_00155280` (every table access), `func_001452b0`,
`func_0047a0e0`, `func_00479940`, `func_002bd410`/`func_002bd3c0`,
`func_0018e030`, `func_0029db50`, `func_00182310`, `func_001560a0`,
`func_00452490`, and the 1.0f `lui` pair.

## Starting point

The guarded m2c draft (P4_FIELD macros, `goto loop_11`) measured 185-197
masked words with the wrong frame (0x70 against retail 0x50).

## Levers, in order (masked words)

1. A clean rewrite with case 2 listed as its own `break` (retail tests the
   cases 4, 3, 2, 1, 0): 185.
2. The event record is a 0x2C-byte struct read as
   `&(*(FldEvtEntry **)(func_00155280() + 0x18DC))[h->idx & 0x3FF]`; subscripting
   loads the table base before the index multiply, which the byte-offset form
   does not: 141.
3. `#pragma opt_common_subs off` (scoped push/pop). Retail reloads `h + 0x2C`
   after every `func_00155280()` call; b210 with CSE on keeps the scaled index in
   a saved register across the calls: 84 with the loop reload below.
4. The search loop re-reads `h + 0x1C` at the top of the body
   (`while ((node = h->1C) != NULL) { node = h->1C; ... }`), matching retail's
   two separate `lw $s2, 0x1C($s0)`: 66. Case 3 falls into case 4's `return 0`.
5. The loop's compare uses the byte-offset form (index first, then base), the
   only site that does in retail: 58.
6. Every call argument written inline. Named temporaries for the
   `func_00155280()` results rotate all four saved registers (h went to `$s3`).
   `func_0029db50`'s third argument is read as `u32` (retail loads it before the
   next call, which a same-type argument does not do): 9.
7. Callee contracts from their definitions: `func_0047a0e0(u8 *, s32, f32)` and
   `func_001560a0(u8 *, u16, u16, u16)`. The latter was `s16` in
   `code1_0015.c`, but its body only stores the three values with `sh`, and this
   (its only) caller passes `u16` fields with no conversion: retail keeps the two
   record pointers live and loads the fields with `lhu` right before the call.
   With `s16` parameters the `u16` fields are converted early. code1_0015.c stays
   68/68 MATCH: 5.
8. `unk1A` is `u16`: it is passed to `func_00479940`'s `s16 animation`, and that
   conversion is what makes retail load it (as `lh`) before `h->1C`: 0.

Declaration order had no effect on the register assignment in any state
(9 positions measured for `h` twice).
