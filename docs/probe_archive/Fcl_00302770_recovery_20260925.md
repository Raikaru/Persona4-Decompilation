# Fcl result-screen state step: accepted C recovery, 2026-09-25

`src/Event/Fcl/y_fclCombine.c::func_00302770` is active C and **MATCH**
(object 3732B inside the 3744B retail window, 0 differing words; tail is
alignment padding). Owner: 31 MATCH / 10 ASM of 41, no other status change.
The previous guarded draft needed `#pragma peephole off` and still sat at
728 words (object 3828B); the accepted body uses no pragmas.

## Relocations
jtbl_007492E0 (HI16/LO16, cases 0x6E..0x74), GPREL16 iGpffffa8a0 (x2, the
sprintf format), HI16/LO16 D_008C024E, and R_MIPS_26 calls including
sprintf (func_00442088), func_002b81f0, func_0032c660, func_0033e540,
func_00325450, func_0032e570, func_002b69f0, func_002b6970.
`config/symbols_recovered.txt` gains `iGpffffa8a0 = 0x00763990`, produced by
`tools/recover_symbols.py` from this match (gp 0x007690F0 - 0x5760); without
it the owner reports an unresolved symbol in `explain_ineligible.py`.

## Key shaping levers
- `func_002b69f0` now declared file-wide with its real contract
  `(s16, FclVec2f, FclVec2f, u32, u32, s16)` (as in y_fclCombineDraw.c and
  the promoted definition). Passing `*(FclVec2f *)(q + 0x38)` yields the
  retail ldr/ldl pair; func_0030f650's stack vectors pass as
  `*(FclVec2f *)&w` and stay byte-identical.
- Tracked-position blocks: `fv = *(FclVec2f *)(q + 0x38)` (lwc1/swc1 to the
  stack), and `*(FclVec2f *)func_002b81f0(...) = *(FclVec2f *)&sp` (lwc1 x2,
  swc1 x2).
- Retail arm order: both bb680 tests (`!= 0` busy arm first).
- `*(s8 *)(p + 0xD) = func_002bab80(...)` then pass `*(s8 *)(p + 0xD)`
  (store forwarding, see the 0030b060 note).
- `(f32)413` and `(f32)321` are runtime cvt.s.w conversions in retail.
- Format string: `extern s32 iGpffffa8a0; (const char *)&iGpffffa8a0` as in
  the matched func_0020e420.
- Stack layout by declaration order: `fv0`, the 38 s64 homes (0x250..0x128),
  `fv7..fv1` (0x120..0xF0), then `char buf[128]` at 0x70.
