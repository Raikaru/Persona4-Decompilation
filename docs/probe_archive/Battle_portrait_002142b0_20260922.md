# Battle portrait panel: func_002142b0

Recovered the ordinary C implementation in `src/promoted/code1_0021.c`.
The final whole-owner native b210 result is **106 MATCH / 2 ASM**, compared
with the authenticated **105 MATCH / 3 ASM** owner at HEAD
`693a76f`.

The function emits 6,492 bytes matching the full retail code after all
relocations are resolved. Its 6,496-byte retail window has a four-byte zero
alignment tail. All 108 owner functions pass full unmasked proof. The other
107 function bodies and canonical relocations are identical to the baseline,
including the changed C call expression in `func_002167f0`. The two existing
owner jump tables retain their bytes and all relocations; no data is added.

The closing change reconstructs one slide phase containing its duration,
quadratic coefficient, current layer curve, and mutable frame. The duration
is initialized to 10 and the coefficient to 2 on both direction branches.
The frame progresses by -1.25 or +1.25 through the three layers. The ratio
temporaries belong to their evaluation blocks. This matches the retail
lifetimes of f8, f7, f6, and f5 respectively, closing the previous 18-word
frame/duration register swap. All values are initialized before use and
perform their actual animation computation. The scoped loop-invariant
option is restored with pragma push/pop.

The stack frame is 0x180 bytes. Its real objects are four `Vec2f[3]` arrays
at 0x100, 0x120, 0x140, and 0x160, plus the five color slots and status at
0xE0. The maximum HP and SP values occupy compiler-generated scalar spill
slots at 0xD0 and 0xC0. The shared `Vec2f` is two floats. No artificial frame
fields, register variables, volatile accesses, or CPU assembly are used.

The function and its only owner caller consistently pass work and actor in
a0/a1, X/Y in f12/f13, and row in a2. The C prototype places X/Y before row,
as do the adjacent panel providers. The HP and SP readers at 00231ed0 and
00231ee0 are the grouped `datCalcGetHp` and `datCalcGetSp` implementations:
both return u16 and take an integer unit address. The maximum-stat providers
at 00231f80 and 00232290 return u16 and take `DatUnit *`. The new function
uses those actual contracts, an opaque DatUnit declaration, and explicit
low-halfword consumption. Correcting these declarations preserves the zero
match and every sibling. The draw and scale declarations agree with their
implementations in `src/promoted/code1_0020.c`.

Retail float loads and index strides establish two tables accessed as
3 by 24 f32 samples at 00626cc0 and 00626de0, two 24-sample opacity curves
at 00626f00 and 00626f60, and two three-float coefficient vectors at
00626fc0 and 00626fd0. The complete consumed spans were read from the
validated retail ELF and hashed. Initializers 00213b10/00213b30 zero their
respective effect timers; this function increments them and disables the
effect at 24. Table addressing, signed timer loads, byte opacity conversions,
status dispatch, draw-call order, and guarded HP/SP digits match the full
retail instruction stream.

Primary references reviewed: the retail assembly for 002142b0; current IDA
and Ghidra owner references; `include/shd_misc_internal.h`; the four stat
provider definitions; the current draw providers; color provider 00213e20;
and caller 002167f0. Their hashes and the complete ABI/data audit are saved
with the receipts below.

Receipt directory:
`build/finish-first-party-20260922/resume-07-battle/ready-portrait-v1/`.
`completed.json` authenticates the native compiler, flags, source inputs,
object, official result and sibling preservation. `all-function-proofs.json`
contains the 108 unmasked proofs. `func_002142b0-proof.json` records all
90 target relocation resolutions. `abi-data-review.json`
records the tables, provider contracts, real frame and exact caller.
The official verifier reused the authenticated final native object; it did
not launch a second compiler. All earlier incomplete-looking jobs were
collected as completed or completed-rejected before new probes.

Final source SHA256: `42a2182711093d64162f40c80bc997e0220e7fbc02746c46b8b68f7c39103874`.

Final object SHA256: `5d818737d60c8e3b80693b3673d9d414e368db7177827cb6cfb8cc74a76b0cdd`.

Resolved target SHA256: `a6d03d2fa251c88d1036e0d059c46f0f37ba6a18acba3b8ae5ef7cfaa5ee0d62`.

Baseline source SHA256: `d7e3dae6801f4c808e06f3a89ba52321672059995b3ccba7a277d422eb0a914e`.

This proposal removes only the portrait's obsolete floor notes and ASM
guard, installs the recovered C body, and updates its declaration/call.
The remaining owner ASM functions are 00212270 and 0021be80.

The successor documentation package records two current Git identity reads in
`build/finish-first-party-20260922/resume-07-battle/ready-portrait-v2/head-observation.json`.
The source, native object and sealed v1 verification receipts are unchanged.
