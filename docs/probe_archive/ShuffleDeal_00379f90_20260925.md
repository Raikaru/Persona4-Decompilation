# Shuffle deal state machine 00379f90

`func_00379f90` in `src/promoted/code1_0037.c` is now C and verifies as MATCH.
No other function in the file changed status. Its twins `0037ad10` and
`0037bc80` (94-96% the same opcodes) are still guarded.

Contract changes:
- `func_00375e50` (btlShuffleDraw.c) now takes `f32 *arg4`, which it already
  used as a pointer. Its only declaration (code1_0037.c) already said so.
  btlShuffleDraw.c keeps all its statuses.
- The file-scope `D_008C024E` in code1_0037.c is `u16 []`. Retail reads it
  with lui/%lo; a scalar `u16` goes through $gp. The guarded twins now index
  it too.

## Levers, from the guarded draft (230 edits) to exact

- `D_008C024E[0]`, the `++*(u16 *)` counter tests, the duration update as
  one expression, and v0/v1/v2 as separate locals instead of struct members.
  With opt_loop_invariants on this reached 117 edits.
- **`sbase2 = arg0 + 0x1F1D0` recomputed.** It is not a copy of `sbase`.
  Retail rebuilds it after the join at case 0.
- **`s32 mod = (u16)(...)`**, with `(u16)mod` inside the two deal loops.
  The last loop passes `mod` to `func_00378c80` unconverted. With a `u16 mod`
  that call needs an `andi`, which LI hoists, and every saved register
  rotates. 178 -> 76.
- The first-third point as `x = base.x + dx / 3.0f; first.x = 150.0f + x;`
  with named `dx/dy/dz` differences. This keeps retail's lazy 150.0f load.
- `angle = 0.0f` at the top of case 4. The fan angle is
  `angle + (360.0f / n) * (n - sel)`, which gives adda/madd.
- **Separate counters:** `i` for the case 3 loop, `k` for the case 4 loop,
  and `j` for the case 10/11 loops and the trailing update loop. The case 4
  offset has its own local. Retail has the first two loops in $s4 and the
  last three in $s2.
- `(u16)m` and `(u16)(m + 0x14)` at the `func_00375e50` call, where
  `m = (u16)mod * i` is not masked.
