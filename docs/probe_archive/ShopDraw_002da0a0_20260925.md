# Shop swing panel 002da0a0

`func_002da0a0` now matches: 4,956 native bytes in the 4,960-byte retail
window (the last word is zero alignment). The previous floor was 50 words
(`ShopDraw_002da0a0_body_20260925.c`), every one the same saved-register
swap in the five `D_0063FA48`-table groups: retail keeps the table pointer
in `$s1` and the `func_0046d200` handle in `$s2`.

## What closed it

The six `func_0046d200` handles were one `void *r` reused across the
groups. Giving each group its own handle local (`r0`..`r5`, declared after
`i`) gives retail's colouring: the first group's handle takes `$s1`, the
later ones `$s2`, with the table pointer in `$s1`. Splitting only the first
group's handle stays at 50; splitting `fa` or `base` per group as well is
inert (still 0). This is the same lever as the block-scoped handles in
`func_002cb6c0`.

## Unit changes

- `func_002d83c0`'s body moves into `static inline void
  shopSwingCursor(void *arg0)`; `func_002d83c0` calls it (still MATCH) and
  so does this function, where retail inlines the block. Declaring
  `func_002d83c0` itself `inline` drops its out-of-line copy.
- Already on the branch when it closed: `D_0063FA40/44/46` as arrays
  (lui/%lo rather than gp-relative), `func_002e09b0(void *, f32, s16)`,
  `ShopSpinParam` for the swing tables, and `fGpffff852c` for retail's
  `-0x7AD4($gp)` load (the draft's `fGpffff7ad4` is the wrong encoding).
- The guarded `func_002be530` body's local declaration of this function
  now takes `void *`.

## Relocations

All code relocations resolve (calls, `D_0063F5A0`..`D_0063F8C0` bases,
`D_0063FA40/44/46/48` HI16/LO16 pairs, `fGpffff852c` GPREL16); verify.py
reports no WRONG SYMBOL line for the unit.
