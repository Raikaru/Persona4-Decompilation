# Group camera initializer: 001c1040

The configured MWCCPS2 b210 `-O2 -Iinclude` compiler emits **1,884 exact
executable bytes** for `func_001c1040`. Its 1,888-byte retail window ends with
four zero alignment bytes. All **48 relocations** are resolved to their retail
targets. The official actual-owner run completed in session **27299**, exit 0,
with **58 MATCH / 5 ASM** in `src/promoted/code1_001c.c`.

All 62 sibling functions retain identical bytes and canonical relocations,
including the previously promoted 001c17a0. All allocated data is unchanged.
The prepared proposal and actual-owner objects agree for every function and
allocated data section. `git diff --check` passed after integration.

## Recovered objects and lowering

Eight ordinary `RwV3d` objects, a `P4CameraVec2`, a `RwMatrix`, and two complete
`P4CameraFrame` poses replace the previous type-punned unions and raw arrays.
Their natural allocation reproduces the retail 0x150-byte frame. The lateral
offset product is split into two ordinary statements, reusing the mechanism
that closed 001c17a0. No new pragma, explicit padding, alignment annotation,
volatile access, assembly, or artificial store was introduced.

The first native candidate reproduced the arithmetic and stack layout, but
default-forward copies used three floating loads and stores. The closing
source is the direct aggregate assignment `direction = D_0060A0F0`, with that
existing constant declared as `RwV3d`, matching `src/Battle/btlCamera.c`.
Casting and dereferencing its address did not yield the same lowering.
An aligned declaration also matched, but the accepted source needs no
alignment annotation. Other references in this owner now take the constant's
address explicitly; their emitted bytes and relocations are unchanged.

## Contract and semantic review

The complete 493-line retail assembly, both full IDA/Ghidra exports, current
callers and native camera providers were read before editing. The accepted
routine preserves both action-selection branches, resource reset and camera
state writes, 1,000/1,200 distance floors, coincident-position fallback,
branch-specific height selection, both ground-point computations, the
floating-point operation order, random rotation, full pose writes, camera
clamps, the 25-unit height floors, and the ten-unit transition duration.

The sphere-center call uses the native `(BtlUnit *, RwV3d *)` interface.
The group query retains its six-argument unsigned-filter/vector/height-output
contract. The target explicitly declares `func_001bc240(s32)` and converts
the camera address to its provider's 32-bit argument; `func_001bc1b0` takes a
byte pointer. The two-float normalization helper and tangent both return
`f32`. Matrix, quaternion and pose pointers cover complete objects.

The owner-local declaration of `func_001bc3a0` now returns `u32`, matching its
native clamp provider. Its dormant duplicate declaration was corrected too.
No call in this owner consumes that return value, and all sibling code was
verified unchanged. No shared provider, header, configuration or publication
worktree was edited. The target still requires valid camera/action/unit data
and a nonempty eligible group, as the retail routine does; this proof does
not claim whole-game execution coverage.

## Saved evidence and identities

Evidence root:
`build/first-party-finish-20260920/camera-current-work/1040-release/`.
`integrated-proof.json` records the official target, resolved references,
all sibling/data checks, and source/dependency hashes. `official.json`,
`official-receipt.json`, `live/compile.json`, saved objects and resolved/retail
bytes retain the actual-owner result. `proposal-proof.json`,
`change.apply_patch`, `before.c` and `retired-floor.c` preserve the accepted
change and superseded floor. Do not rerun completed commands to retrieve
their output.

Source SHA-256:
`21abea3699845ad86109de09e6d6336553f6b4fc18e9e9d6780a392869fc9e95`.

Actual-owner object SHA-256:
`d518ed063bd0ecda4e3387e533923e592ad50faf2089108910e3d0d79a9ace77`.

Resolved target SHA-256:
`fef4e848bd6619448c87135c34413456f7b7349ef3509727c2b7c08b07f95a67`.

Compiler SHA-256:
`286548490e2e902cfef21dcf39cd5af23766731585d90dea747f8781eadcafd7`.

The preceding 17a0 proof and five-word 5b80 frontier were preserved without
recompilation. The next independent camera target is 001c21d0.
