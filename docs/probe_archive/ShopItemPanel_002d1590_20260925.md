# Shop item stat panel: func_002d1590 recovered, 2026-09-25

`src/Event/Fcl/y_fclShopDraw.c::func_002d1590` is active C and **MATCH**
(2643 instructions, frame 0x2B0, `$s0`-`$s4` and `$ra` only). The file went from
31 to 32 MATCH.

## Shape

It returns 0 if any of six panel objects (`func_002e05a0` on C60/C80/C70/C84/
C74/C78) is still busy, after hiding the stat widgets. Otherwise it draws the
held item's name, stock/price colouring, element dashes or the
`func_002bc7f0` element strip, and the per-tab stat block. There are three
work[7] tabs, each with up/down stat-delta arrows (`func_001068b0/68e0/6940/
6970` against the equipped item from `func_00106cd0`). After that come the
moon badge, the header bars, the scroll thumb (with the
`264.0f + work[0xC] * cursor` FMA), the equipped item's name/icon/strip and
its stats, and the stat boxes, then it returns 1.

## How it closed (fnalign edits)

The archived guarded draft re-extracted at 3134 edits. It was fully rewritten
from the retail listing: 166.

- Slot map: the retail listing was reduced to events (stack address per
  `func_002b2970`/`func_002b2a60` call). Every retail stack address got its
  own named local, declared high to low, so the frame and all offsets land
  exactly.
- `c2AC = c2A8` as a whole-struct FclDrawColor copy. mwcc emits it as
  `lwc1/swc1`, and retail has four `lbu` then four `sb`; the pointer form
  `*(FclDrawColor *)&c2AC = *(FclDrawColor *)&c2A8` gives retail's copy.
- Stat deltas: retail calls the stat function a third time and keeps
  `d = f(item) - uB` in `$v0` for both the `< 0` and `> 0` tests. uA/uB are
  s32, because the u16 return is masked once at the call: 166 dropped to 74,
  and with the copy fix to 66.
- One s16 counter per loop (five loops). A single shared `i` pinned every
  loop to one register: 66 dropped to 31.
- Callee widths: `func_001069d0(s16)` (as datCalc/code1_0021 declare it) and
  a local `func_00106b80(s16)`. In the second tab switch retail re-extends
  the s16 `func_00106cd0` result before the stat call, but not in the first
  switch, so only the second-switch calls are written `f((s32)func_00106cd0(..))`:
  31 dropped to 13.
- The DBC/E88 handle arrays are addressed as `*(void **)((u8 *)work + i * 4 + 0xDBC)`.
  Retail adds work first and CSEs the sum across both arrays in the final
  loop; overlay-array indexing adds the index first: 13 dropped to 2.
- `t = work->fE60; func_002e0940(t, fGpffff7ad0, fGpffff7ad0, ..)` loads the
  handle before the shared gp float, as retail does: 2 dropped to 0.
