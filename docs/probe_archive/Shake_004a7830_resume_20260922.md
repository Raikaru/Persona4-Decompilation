# Camera shake continuation, 2026-09-22

`func_004a7830` remains guarded and NONMATCHING. This continuation did not
produce an exact C candidate and did not change `src/promoted/code1_004a.c`.
The best result remains seven fully resolved differing words, with 532
executable bytes in the 544-byte retail window. The remaining 12 retail bytes
are zero alignment padding.

## Inputs and retained evidence

The worktree was `build/first-party-publish-latest` at `5a1bcf7`, with its
existing dirty work preserved. The unmodified owner byte SHA-256 is
`484d4ab31d6b07635513d45365d22247b8937cd23a7330c557f0bdbef6a0258b`.
All artifacts below are relative to this worktree, under
`build/first-party-resume-20260922/shake/`.

`before.c` preserves the owner. `inputs.json` records its header hashes and the
configured native MWCC b210 executable hash and `-O2 -Iinclude` flags. Every
probe uses `probe_variants._compile_in_context` with the real owner path;
the source redirected to scratch is a complete owner translation unit.
Each destination retains `body.c`, `code1_004a.c`, `owner.o` when compilation
succeeded, `compile.log`, and `compile.json`. Successful comparisons also
retain `result.json`, `alignment.txt`, `resolved.bin`, and `retail.bin`.
Existing completed probe destinations were not rerun.

The current guarded-body baseline is `baseline-current/`. Its candidate
source hash is
`45427c7d4e790794ebba1f9442b791e9ddaa341bda1f2c196ddfb8b4d1d227af`,
and its object hash is
`4d7b692544aa67d6af080637583b98a7a791a7be2e6fec752fdb0b9a6a27ab36`.
Those reproduce the saved census hashes, now with an independent full
relocation and owned-data comparison.

## Exact residual and contracts

All sixteen code relocations were resolved to their actual destinations.
The anonymous eight-byte `{0.6f, 0.6f}` object was located at `0x00764200`
and its entire contents matched retail. Its SHA-256 is
`be51db4c5a64da9a56bebc23b5305d400523548c2506f1d3e019ff144778b061`.
Existing owner data and all 91 sibling functions' bytes and canonical
relocations were unchanged in each fully compared candidate.

| Offset | Retail | Current C |
| --- | --- | --- |
| `+0xB0` | `mtc1 zero,f2` | `mtc1 zero,f3` |
| `+0xC0` | `lwc1 f1,0(s0)` | `lwc1 f2,0(s0)` |
| `+0xC4` | `abs.s f3,f1` | `abs.s f1,f2` |
| `+0xD4` | `c.lt.s f3,f0` | `c.lt.s f1,f0` |
| `+0xE0` | `c.le.s f1,f2` | `c.le.s f2,f3` |
| `+0xEC` | `add.s f0,f1,f21` | `add.s f0,f2,f21` |
| `+0xFC` | `sub.s f0,f1,f21` | `sub.s f0,f2,f21` |

The source preserves the unordered behavior of the magnitude test and the
ordered `current <= 0.0f` sign test. No inverse comparison that changes NaN
behavior was accepted. The step and random-kick MULA/MADD/ADDA expressions,
RNG calls, clamps, frame, integer-register allocation, and translation call
already agree outside this register cycle.

Contract review covered the caller `tnEffect.c:func_00481980`, the real
`effMisc.c:effMiscRandFloat` and `effMiscRand` implementations, camera accessor
`code1_0045.c:func_00457120`, the translation wrapper's declaration and
implementation, and the matched scene analogue `mt_scene.c:func_0026d440`.
The existing canonical RNG prototype is correct. Its address was resolved
from the provider's `FUN_004BD0B0` marker because the configured symbol map
does not contain that canonical name. No provider or shared header needed an
edit; no vendor implementation was changed.

## New probes

`probe-summary.json` inventories 73 target sources: the current baseline and
72 new variants. There were 72 successful compilations, 66 full resolved
comparisons including the baseline, six rejected scalar-type variants whose
changed layout prevented owned-data placement proof, and one syntax failure.
There were zero accepted new matches.

The new families tested inline directed updates with return values or output
pointers, sample/magnitude capture expressions, aggregate sample returns and
outputs, current-value pointer aliases and accessors, full-axis and partial
step inline boundaries, structured and explicit branch endings, separate
clamp endings, helper-local optimization controls, and natural scalar-type
spellings. Sources and results, rather than a masked score, establish each
outcome.

Output-pointer and whole-axis forms generally retain exactly the same seven
register differences. Returning a directed-update result merges stores and
shrinks the body to 528 bytes. Separate clamp endings expand it to 576 or
620 bytes. The center-parameter variants retain the original seven differences
and add four differences in the random-kick expression. The positive-magnitude
branch arrangement changes block order without fixing the register cycle.
Helper-local controls did not improve their corresponding sources; no compiler
pragma was added to the owner.

The six double-precision variants are rejected screening results, not full
proofs: they produce 560--588 bytes and 118--129 relocation-masked differing
words, with additional relocations. `directed-do-break/compile.log` preserves
the C89 declaration-after-statement error. None of these failed probes was
integrated or counted as a match.

## Preserved owner and wind proof

`official-owner/official.json` and `official-owner/official.log` are the
official verifier run scoped to this owner. It exited zero and reported
**90 MATCH, 2 ASM**, all first-party. Those two ASM results are the guarded
fallbacks for `func_004a5fc0` and `func_004a7830`; they are not C-match claims.
`official-owner/owner.o` and its compiler receipt are retained.

`official-owner/owner-proof.json` confirms all 92 production functions and
all existing allocated data are unchanged from the initial production object.
Every one of the 90 existing MATCH functions also passed the full resolved
code and owned-data prover. Their individual reports and resolved bytes are
under `official-owner/resolved/`, with no unresolved proof exceptions.

The required wind donor proof is
`official-owner/resolved/func_004a1d70.json`: **824 executable bytes in an
832-byte window, four fully resolved relocations, exact**, with eight zero
tail bytes. The owner and its measured headers still match the initial hashes.

## Other first-party target in this owner

The only other guarded function is `func_004a5fc0`. Its saved census lead is
3064/3024 bytes and 714 differing words, not an accepted match. Its existing
source, retail assembly, IDA, and Ghidra were reviewed. It needs substantive
reconstruction before allocation work:

* At `0x004A6008`--`0x004A603C`, retail enters the body when `count < total`
  or `total <= 0`; the guarded source's `count >= total` test is reversed.
* The guarded inline color code stores to literal `0x110(sp)`, then C reads
  local `sp110` without a declared output connecting the two. A matching
  stack layout must not substitute for a valid output contract.
* Retail stores the same scaled Y coordinate at stack offsets `0x92` and
  `0x96` (`0x004A63CC`, `0x004A6464`). The latter source assignment instead
  uses `blkC0[8] * sx`, the X coordinate.
* Retail's final quad corner at `0x004A6804`--`0x004A6818` uses the retained
  `(a + e0)` products. The guarded source reuses the `(a - e0)` X product
  and sets its final Z coordinate to `t2 + t1`; those are different values.
* The D0/CC dimension conversions and reciprocal denominators also need an
  unsigned-conversion audit against retail, regardless of old notes that
  favored signed casts for a smaller instruction count.

This review did not modify that guarded body or add assembly. These concrete
source-contract failures make it unsuitable for a quick near-match promotion.

Both attempts to report intermediate progress through the worker channel
returned `AGENTS_BUSY: no agent family belongs to this conversation`. Local
work and validation completed; the owner is unchanged and no compiler is
still running. The seven-register-word shake residual remains the blocker.
