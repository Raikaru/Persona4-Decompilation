# First-party recovery checkpoint: 6,531 MATCH / 329 ASM

The installed first-party census contains 6,860 functions across 292 native
owners. This batch adds ten recovered C functions to the authenticated
6,521 MATCH / 339 ASM working-tree checkpoint. It does not count an assembly
fallback as recovered C, change code-origin classifications, or remove functions.

| Owner | Newly recovered functions | Installed owner result |
| --- | --- | --- |
| `src/cmmRankUp.c` | `00253850`, `00254a70` | 24 MATCH / 2 ASM |
| `src/promoted/code1_0015.c` | `001561a0`, `00156800`, `00157310`, `001582f0`, `0015b3e0` | 67 MATCH / 1 ASM |
| `src/promoted/code1_0021.c` | `00212270`, `002142b0`, `0021be80` | 108 MATCH / 0 ASM |

The rank-up entrance and exit recover the actual sprite argument streams,
byte opacity, signed-halfword centers, complete coordinate/color/rectangle
objects, animation phases, and scoped constant lifetimes. The related sprite
providers and their callers have consistent declarations across thirteen source
owners. The other twelve owners preserve every existing function's instruction
bytes and canonical relocations. The private interpolation routine and its
shared native floating literal remain unchanged.

The field recovery uses the task's actual 32-byte loader work, all nine loader
states, the signed coordinate and rotation domains, the real room-list count,
and complete map-template storage. Its 36-byte dispatch table has all nine
entries fully relocated and compared with retail. The tile caller and
environment selector use the same proven parameter contracts; their existing
code and data remain exact.

The field flood retains a documented original omission: one reachable
north/south room branch does not assign the next recursion depth before using
it. This is present in retail and is not described as portable, fully initialized
C behavior. The reconstruction preserves the native behavior without inventing
an initialization or excluding the branch. The detailed audit is retained at
`build/finish-first-party-20260922/resume-07-field/audit/Flood_0015B3E0_original_behavior.md`.

The battle owner is complete. Its encounter transition, portrait panel, and
advantage panel use the actual callback signatures and complete shared geometry
objects. The final advantage selector owns both ordered random samples and
returns the selected halfword. That operation's lifetime closes the last AND
operand difference. The owner includes two exact dispatch tables and two exact
four-byte native literal pools; all 72 allocated data bytes are accounted for.
The dedicated battle archive notes record the frame, provider, data, and
initialization audits.

## Installed-source verification

The current-source official verifier was run for all seventeen changed owners.
Completed native objects were reused only after their source, compiler binary,
flags, recursive project includes, and assembly inputs were authenticated. Every
new function also passed an independent complete relocation and owned-data
proof. All other functions preserve their original instruction bytes and
canonical relocations. Existing data is preserved, and every added data section
is covered by a target proof.

The patch writer added an EOF newline to `k_fldEnvironment.c`, whose reviewed
input lacked one. That actual installed source was compiled separately; every
function, relocation, and data section agrees with the reviewed native object.
No other native compilation was repeated during installed verification.

The durable installed record is
`build/finish-first-party-20260922/resume-09/retained-ten/installed-closed.json`.
It names all 292 owner receipts, the full source-input map, ten target proofs,
official reports, and the final census. Per-owner current-source reports and
proofs are in that directory's `installed/` subdirectory. The batch's focused
integrity lint and `git diff --check` passed.

This is the native source checkpoint. Full-image linkage is a separate gate;
the GNU linker integration records its image, original source definitions,
foreign gaps, and final ELF verification independently. Remaining work is not
represented as completed by this record.
