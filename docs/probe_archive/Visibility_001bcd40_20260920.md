# Battle visibility provider recovery, September 20, 2026

`func_001bcd40` in `src/promoted/code1_001b.c` is integrated ordinary C.
The completed provider delivery records official `MATCH`: 1,472 resolved
bytes in the 1,472-byte retail window, all 22 relocations resolved, zero
differing words, and zero decoded alignment edits. Its official owner report
contains 114 `MATCH` and nine `ASM` rows. All 122 neighboring functions and
the inherited four-byte `.lit4` section were preserved.

This note preserves the completed delivery at
`build/first-party-resume-persist/side-camera/visibility-provider/integration/`.
It does not represent another compiler run. The delivery includes the actual
official compiler object, command, output and exit code, independent unmasked
proof, input hashes, semantic audit, patch and manifest. Its source SHA-256
is `826fdc8889bccd5506800cccc769568b26b51516821ca88220c0ff58a6668785`;
object SHA-256 is
`9e6dc6545593d37d32e5c910104ff94dbbb42ac310e49c7731280173893241d9`.
The manifest SHA-256 is
`cdcf79fe714a2cb4ffe52d863c5dd03e1312ca37bb4263766438146f30f817d9`.

The provider retains the coherent seven-owner interface:

```c
void func_001bcd40(u8 *action, u8 *first, f32 *second,
                   f32 radius, u16 mode);
```

It traverses eligible action/unit pairs, applies the ordered mode decisions,
then tests the XZ camera segment where required. Both endpoints have complete
two-float storage, the sphere center has three floats, and the projected
point has two. The real getter result from `func_001b1560` is consumed through
the existing integer-return contract. Unit flag writes use the canonical
`btlUnitClearFlags` and `btlUnitSetFlags` providers. The target-list accessor
uses the actual halfword ordinal and real action target array; independent
loop indices preserve the two searches' conversions.

Scoped `opt_loop_invariants on` produces the retail invariant flag spills
from the actual predicates. The source introduces no padding, artificial
wide spill objects, dummy effects, assembly or volatile forcing. The sole
lint advisory is the documented optimization setting; structural integrity
checks passed. The target adds no owned table or literal.

The detailed semantic audit retains the exact ordered floating predicates
and short-circuit behavior, including unordered inputs. It also records the
source-domain precondition for `1u << group`: the inspected unit-constructor
call paths provide group IDs 0, 1 or 2. Arbitrary corrupted byte values are
not claimed as valid C shift counts. The explicitly masked diagnostic added
instructions and was retained as a rejected experiment.

Successful historical sessions were patch 72858, official verification
96851, proof/lint/diff check 69542 and sealing 77170. The final process scan
reported no active provider jobs. `closure.json` records an unavailable old
terminal replay separately; no completed compiler job was repeated.

This provider recovery and the already integrated `001c04e0` seven-owner
recovery remain separate from the subsequent `001ccdb0` NOP-camera work.
The latter starts from camera owner SHA-256
`b874effa51f8aaf28aaf37a20ec19ac79d8e6d6ebf3bc1c21a0061f0034fcab4`.
Full-image gates and Git publication remain prime's responsibility.
