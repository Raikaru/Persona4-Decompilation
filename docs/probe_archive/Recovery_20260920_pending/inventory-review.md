# Saved recovery inventory review - 2026-09-20

Worker-1 audited immutable recovery commit `17f42cac4480348bc4b08c681820dc9e1ab27201` against fetched upstream `9ed8ca6b1442af9a305f75491a44950816fa78df`. The seven recovery commits were already above that upstream revision when inspected. This report describes those fixed revisions, not any subsequent changes by prime.

The accepted exact recoveries are accounted for, but the saved work is broader than `saved-inventory.json`: two additional zero-word candidates, creator-return corrections, a small caller correction, and several material guarded proposals remain outside those seven commits. The two zero candidates are unfinished recovery work, not established additional ABI-correct C promotions.

Path abbreviations: `M` = `/persona4-decompilation/build/match-next-latest`; `A` = `M/build/all-first-party`; `C` = `/persona4-decompilation/build/close-sweep-latest`; `R` = `/persona4-decompilation/build/register-pair-latest`. Unabbreviated repository paths below refer to the fixed recovery revision.

## Already upstream or retained locally

| Saved result | Disposition and concrete evidence |
| --- | --- |
| Exact `001eca10`, `001c79f0`, `0025dd30` | Already active C in `9ed8ca6`, respectively in `src/btlTarget/btlTarget.c`, `src/promoted/code1_001c.c`, and `src/shdWindow.c`. Saved proofs are `R/build/pair-001eca10/recheck/integrated-proof.json` and `R/build/close-wave2/{001c79f0,0025dd30}/integrated-proof.json`; the coordinated `00366670` parameter order belongs to the window recovery. Do not replace newer upstream owners with old snapshots. |
| Exact `0015d310`, `00242990`, `00152170`, `003233d0` | Already upstream through `745ea72`, `02898dd`, and `0ec5899`. Saved reports in `C/docs/probe_archive/{Sweep_5ec1761_00242990_exact.md,SWEEP_9f866b7_00152170_worker4.md,Sweep_9f866b7_worker5_Fcl_exact.md}` and `C/build/sweep-5ec1761/0015d310/semantic-final-summary.json` account for them. The Fcl selection provider's signed-byte third argument and its other guarded caller's argument-load improvement were coupled with `003233d0`. |
| Exact `0032f4d0`; Fcl position/constructor contracts | The function is already upstream through `7791d67`. Local `a0ea1fd` retains the broader shared `FclVec2`/`FclPackedPosition` contracts, constructor destination casts, and corrected descriptor snapshots in guarded `002e90d0`. Saved proof: `M/build/match-next/fcl-constructor/current-proof.json`; archived explanation: `docs/probe_archive/Match_next_0ec5899_Fcl_position.md`. The earlier six-edit archive is superseded, not another missing match. Existing exact `00315310`, `0032f060`, `0032fa30`, and `0032fb60` are preservation checks, not new recoveries. |
| Exact datCalc `00232d80`, `00233bc0` | Retained in `7575421`: shared skill-search helper/public `00232730` preservation, and native status multiplier with the audited selector range and third argument. Saved proof: `A/datcalc/verified/two/current.proof.json`, independently repeated under `A/verified/datcalc-two/`. Saved emitted/window sizes are 1304/1312 and 1280/1280, with 24 and 23 resolved relocations. |
| Exact Fcl `00303610` | Retained in `97f2b74`. `A/verified/fcl-rule/proof.json` accounts for 1036/1040 executable/window bytes, eight code relocations, and the owned 44-byte switch table. |
| Exact Fcl `0032a960`, `0032b000` | Retained in `17f42ca`, together with `002b6c30`'s resource/position contract and actual provider/caller updates. Saved proposal/evidence: `A/fcl/draw-contract/`, `A/fcl/provider-resource/`, and the corresponding per-target experiments. Earlier isolated zero candidates are superseded by this coordinated integration. |
| Other material retained work | `40821ef`: native Windows owner lock plus process tests. `5d576ee`: field direct-provider types, explicit three-argument `003e9cb0` frame forwarding, camera's four float symbol aliases, rank-up const position tables and canonical primitive argument declarations/casts. `f3887a3`: guarded `002411a0` improvement and complete pending-ABI `00238940` archive. |

Upstream's newer `00238940`/`0023a6b0` control-flow improvements remain in the recovery source. The saved earlier `0023a6b0` baseline is not an improvement to restore. The card pair `00215c10`/`002161d0` parameter-order improvement is already in upstream `0ec5899`. `003627b0` was already C before the match-next review; its saved MATCH is not a new recovery.

## Additional work absent from the seven commits

| Priority / item | Saved evidence and present gap |
| --- | --- |
| Small caller correction: `001f14f0` -> `00242990` | `C/src/promoted/code1_001f.c` has an uncommitted local `extern s32 func_00242990(u8 *, s32);` and explicit `(u8 *)` conversion of the loaded pointer at its call. `17f42ca:src/promoted/code1_001f.c` still has only the raw integer-valued call and no declaration. The active exact provider already uses `(u8 *, s32)`. This bounded saved correction is independent of a new exact match and should be retained after prime's review. The caller remains guarded. |
| `001a4800` zero-word action candidate | `A/001a4800/counter-index-typed-state.{c,tu.txt,o}` restores the ushort increment, uses the existing offset helper, and views the state fields through a typed snapshot. Saved measurement: 107 -> 0 masked words, 1148/1152 bytes. The fixed recovery still contains the earlier guarded body. No complete relocation/sibling/data proof was found in this target's saved directory. In addition, it consumes the return of `001f3870`, whose active provider is still declared `void`; caller declarations use conflicting pointer types. Preserve this as unfinished candidate work and reconcile that provider/callers before claiming a new accepted C match. |
| `002e8410` zero-word initializer candidate | `A/fcl/experiments/func_002e8410/3fea0ff69ebc/scoped_initial_loops.{c,tu.txt,o}` uses independent counters for the initialization loops. Saved measurement: 84 -> 0 words. Its `scoped_initial_loops-proof.json` explicitly records exact 3256/3264 bytes, eight zero tail bytes and all 148 relocations resolved. The fixed recovery still contains the old guarded loop body. This target proof does not establish complete current-owner isolation or compatible creator/SDK contracts: it consumes handles from `0034ad70`, `0034b740`, and `002b5c90`, whose active providers remain void and have additional parameter-type discrepancies. This is a real saved instruction recovery with unfinished source-contract integration. |
| Creator return corrections | `A/001f3870/return-allocated-packet.c` and `return-packet-allocator-contract.c` return the allocated packet; both saved scores remain zero at 112/112 bytes. `A/{002b5c90,0034ad70,0034b740}/captured-return.c` capture/return the task handle at saved zero scores of 264/272, 220/224 and 204/208 bytes. All four active providers still return void in `17f42ca`. These are material missing contracts, not four new C bodies: each baseline was already C and zero-word. For the last three, switching to the existing SDK provider's actual raw-address declaration costs ten words in each saved experiment; a caller-only return patch does not finish their API recovery. |
| `0012d630` partial UI improvement | `A/0012d630/label-conversion-lifetime.c` retains the label float lifetime and corrected direct-provider declarations; saved result 9 words versus baseline 13, at 1348/1360 bytes. `selected-row-direct.c` also records nine. The fixed recovery lacks `labelY` and those declaration corrections. `coupled-color-before-depth.c` reaches five, but its associated multi-owner proposal changes two already-exact providers and retains aggregate/scalar contract debt. Retain the source/evidence as pending work; do not apply the five-word result as an isolated caller match. |
| Formation `001d2e20` typed proposal | `C/build/sweep-9f866b7/001d2e20/phase-mode-raw-node-unit-paired-struct.c` and companion measurements restore the inspected provider widths and real four-float rotation/three-float position payload. Saved baseline 89 -> 87 words, 440/448 bytes. Its helper/type work is absent from the fixed source, which independently has upstream's local declaration-order improvement recorded as 87. Thus the old experiment is not evidence of a further current score reduction, but its provider/payload cleanup remains unintegrated. |
| Weighted selector `0024be40` typed row | `R/build/close-wave2/0024be40/row3-typed-six-weight-row.c` removes the pointer-valued 0/1 selector using a six-byte weight row and integer selector. Saved score ties eight words at 672/672 bytes. Only a follow-up comment was added to `WSCR2_0024be40_body.c`; the actual candidate remains scratch. Its inherited RNG return discrepancy was explicitly not reconciled; canonical-wide experiments regress. This is a structural improvement to preserve, not an exact recovery. |

## Deliberate deferrals and invalid acceptance shortcuts

* `00238940`: `A/datcalc/owner-eaa4708c2bba/00238940/both_clear_switches.c` is already preserved as `docs/probe_archive/DC_worker7_00238940_pending_abi.c` by `f3887a3`. Saved result is ten words at 3728/3728 bytes. Its full TU widens `0023d8e0`'s second parameter while external callers remain narrow, and adds a 40-byte local jump table whose complete relocation proof stops at `@1733`, HI16 offset `0x418`. Do not transplant the body alone or call it exact. Saved worker7 reports/hash checks cover all 141 datCalc experiments; none of the later candidates reaches zero beyond the two retained exact functions.
* Rank-up `002561f0`/`00256460`: the saved zero candidate `C/build/sweep-5ec1761/002561f0/loop-colors-main0-split0.c` reaches 612/624 bytes, but uses the incompatible pointer-form primitive declaration and unproved color-buffer spare storage. `rankup-review/review.md` also identifies incompatible array storage views. Canonical-provider variants retain seven words. The seven-integer/five-float target contract and improved color-loop proposals remain valuable scratch work; `5d576ee` retains the accepted table/provider corrections only. The first target's old guarded signature and both old reconstruction bodies are not thereby repaired or promoted.
* SDK registration: `A/sdk-registration/checks.json` records 74 equal active-owner objects. This is not proof that every guarded caller or callback type is valid. `sdkTask.c` dispatches through its `KwlnTask *` callback model while the proposal transports `u8 *` callbacks, including casts from differently returning callbacks. `A/00451fc0/`'s zero-word typed-provider variants and the three creator-return probes belong to this unfinished coordinated migration.
* UI order/width: `A/ui-contract-order/manifest.json` and `A/ui-color-width/manifest.json` preserve eleven-owner proposals. Color-before-depth changes `00364320` and `0034f9d0` by two words each in the saved direct measurements. Width follow-up still changes `0034f9d0`; the script explicitly leaves aggregate/scalar declarations unresolved. The target's five-word score is not an accepted improvement across the affected providers.
* Card pair: the saved `00215c10` compound-product variant's lower 13-word score changes retail's fused multiply-add into separate operations; the report rejects it. The faithful parameter-order work is already upstream.
* The `KOC_00243a30_body.c` / `KoC_00243a30_body.c` NTFS case collision and malformed partial archive are documented exclusions in `Recovered_worker_contracts_8217f8c.md`. Preserve original blobs/worktree evidence; do not count the damaged archive as a recovered function.

The remaining recorded close/register experiments provide no additional exact or lower-score accepted candidate: `001b11c0` (diagnostic register renaming is not C), `00331a20`, `00365f00`, `001f4e50`, `00268230`, `00485870`, `001130c0`, `0048a980`, and match-next's `00375f00`/`0034ddf0`. Their scoped/type alternatives and negative measurements remain in the older worktrees and archive reports. Other Fcl per-target measurement files contain only positive-score baselines; they are not omitted completed work. Saved `00235520` and `0023e6f0` datCalc measurements are compiler errors, not recoveries.

## Fresh audit checks and limitations

This audit parsed the saved 408-row census and every `A/**/measurements.json`, inspected the older reports/measurement summaries and immutable source/commit diffs, and read `saved-inventory.json`. The census has zero zero-word rows, but the separate experiments do contain the additional candidates above; the census alone cannot establish recovery completeness. Zero baselines for already-C creator/SDK/UI providers were kept separate from new candidate results. This worker ran no compiler, linter, test suite, retail scan, rebase, commit, or push.

Fresh SHA-256 checks reproduce each recorded body/TU/object hash for the two omitted zero candidates:

| Candidate | Body / complete TU / object SHA-256 |
| --- | --- |
| `001a4800/counter-index-typed-state` | `10a0ed526f5cbe042a89c33a660350b274d6ee2ec2110f25c9ca436d9b044f53` / `40ed99d8e8823a8427313cf900e2a33794a658cb5c8860ad83bca099dfd37833` / `66334405967a34abde0efb4a1e8ad4f4afdd414e40a78367be2c4975545d9554` |
| `002e8410/scoped_initial_loops` | `ca33af0d5453bfcd8c7f99b0119bc07bf5ed0aea165209ef294bb5ec2a43ba38` / `77e9ddfc8db1214a50bedad7eee6a90916a7a2d85c92d60a6533a786fac262db` / `588e929a812e7ec67298f1a65f910b3052ff4425cfbd1c5b5ddd9b0e3ac16f04` |

All instruction counts, scores, relocation results and owner identities attributed to saved proofs above are historical measurements, not fresh source-compilation claims. The sole file written by this worker is this inventory report. Current-source verification and any recovery action remain prime's responsibility.
