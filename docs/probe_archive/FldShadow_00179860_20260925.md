# Skinned shadow triangle 00179860

`func_00179860` in `src/Kosaka/k_spipe.c` is now C and verifies as MATCH:
1796 code bytes in a 1808-byte window (12 zero bytes), 449/449 instructions,
16 resolved relocations. The other five functions in the file still MATCH.

## How

The guarded draft measured 243 words. It is the skinned twin of the matched
`func_001791d0` directly above it, so the body was rewritten in that
function's shape: the same `RwV3d` src/dst arrays, the staged `t = ...;
tmp.x = t;` output copies, the negated culling test and the
`1.0f - z * z` colour blocks. The per-vertex `func_003e42e0(&skinned[i], ...,
1, func_003e9700(frame))` loop and the `normal` copy come first, and
`context->work` is re-read at each use, as retail does.

That first compile was 68 fnalign edits, all stack offsets. The last lever
was the frame layout. MWCC places later-declared locals lower in the frame, so
retail's layout (skinned 0x60, dst 0x90, src 0xC0, tmp2/tmp1/tmp0 0xF0-0x110,
normal 0x120) needs the declaration order `normal, src, dst, skinned`.
