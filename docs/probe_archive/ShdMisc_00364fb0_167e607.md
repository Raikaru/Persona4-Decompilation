# 00364fb0: rotated transition quad recovered in ordinary C

Date: 2026-09-20. Owner: `src/shdMisc.c`. Branch: `work/first-party-0360227`
on the upstream167e607 recovery state. This work adds one first-party C match.

The installed function emits 2,072 bytes in its 2,080-byte retail window.
Resolving all eleven relocations produces an exact byte comparison. The final
eight retail bytes are zero alignment. All nine other owner functions retain
their bytes and canonical relocations. The owner has no allocated data section,
and this function owns no table or anonymous data relocation.

## Reconstruction

The previous guarded body eliminated the repeated origin load and addition for
the fourth vertex. A small inline store helper accepts the actual `const f32 *`
origin and writes the actual output coordinate. This preserves the load and
arithmetic sequence without an artificial memory access, incompatible type,
volatile qualifier, assembly, or compiler-setting change.

The resulting two-word residual was the operand order of the final Y addition
in each branch. Accumulating the rotated Y coordinate in a real `far_y` local,
then adding the width offset, closes both words. The preexisting zero terms
remain in the rotation expressions because they are part of the retail
floating-point operation sequence.

The helper is placed before the function marker, and the ordinary C function
replaces its `NON_MATCHING` guard. The original assembly and earlier probe
archives remain intact. No other function was promoted or relabeled.

## Provider and caller contracts

The shared declaration in `include/shd_misc_internal.h` is:

```c
void func_00364fb0(Vec2f position, f32 depth, s32 color,
                   f32 angle, s32 arg2, s32 arg3);
```

The retail prologue saves the two-float aggregate from `$a0`. Depth and angle
arrive in `$f12` and `$f13`; the color and two mode arguments use `$a1` through
`$a3`. All sixteen calls in the three current C caller bodies were inspected
against their retail argument setup. All callers ignore the result.

The current camera provider in `src/promoted/code1_0045.c`, `func_00457120`,
returns `s32`. The owner now declares that actual return type and converts its
address value explicitly before pointer arithmetic. This change preserves all
sibling code and relocations. The sine and cosine assembly providers
`func_0044b7b0` and `func_0044b610` take one float in `$f12` and return one in
`$f0`.

The render callback is verified against the current provider, not just an SDK
declaration: `func_0040d110` assigns `func_0040c0f0` to `D_00887310[0]` in
`src/rw/rwcore_grouped.c`. That provider has the same
`s32 (s32, void *, s32)` type used by this owner. No SDK migration was needed.
The mode word `iGpffffabe8` is the 64-bit object at `0x00763cd8`; its four
target accesses remain `ld`/`sd` at GP displacement `-0x5418`.

Caller cleanup uses real `Vec2f` storage and the shared declaration:

| Caller | Calls | Change | Guarded comparison, before to after |
| --- | ---: | --- | --- |
| `00212270` in `code1_0021.c` | 4 | Restores retail X/Y component order and GP angle loads `fGpffff8374`/`fGpffff8378`; removes packed-integer type punning. | 1,051 to 930 aligned edits; 3,864 to 3,464 emitted bytes. |
| `00385970` in `code1_0038.c` | 10 | Reuses one position aggregate corresponding to the retail `spA8`/`spAC` pair. | 288 to 198 aligned edits; 4,700 to 4,580 emitted bytes. |
| `0020ff00` in reserved `code1_0020.c` | 2 | Converts `posA`/`posB` to `Vec2f` and uses the canonical argument order. Prepared for prime integration. | 72 to 68 aligned edits; 3,456 emitted bytes in both versions. |

These caller bodies remain guarded, nonmatching reconstructions. Their active
owner functions, all other functions in each targeted guarded compilation, and
allocated data were compared before and after without regressions. The first
two caller changes were installed by this worker. The reserved `code1_0020.c`
change is supplied as a separate reviewed patch; this worker did not edit that
live owner. Its integration is tracked by the prime, and the installed-owner
proof records whether it was present at the time of that proof.

The first `00385970` candidate used a new aggregate at each call and regressed
the guarded alignment to 308 edits. It was not installed. A single shared
aggregate, as evidenced by retail, produced the retained 198-edit result.

## Exact proof and artifacts

All scratch evidence is below
`build/first-party-resume-167e607/fresh/worker7/`; older artifacts are unchanged.

| Evidence | Path beneath that scratch root |
| --- | --- |
| Final source, real shared header, compiled object, resolved bytes and full proof | `shd364fb0/installed-owner/` |
| Canonical-owner proposal, original owner and independent proof | `shd364fb0/canonical-owner/` |
| Original active and guarded owner objects | `shd364fb0/active-before/`, `shd364fb0/guarded-baseline/` |
| Caller proposals, patches, objects, call-site disassembly and comparisons | `callers/0021/`, `callers/0038-shared/`, `callers/0020/` |
| Scoped verification of the three installed owners | `final-verifier.json` |
| Installation patch for the worker's four files | `install.patch` |

The configured compiler is Metrowerks b210 at
`D:/mwcps2-3.0.1b210-060308/mwccps2.exe`, with the owner's configured flags
`-O2 -Iinclude` and its existing source pragmas. No shared build or verifier
tool was edited.

The resolved target and its 2,072 retail bytes both have SHA-256:

```text
178516a20fa039c78ffb85a0475f930a97a4660679c5be593ce7a961046786df
```

The eleven resolved relocations are the HI/LO pair for `D_008872F8`, calls to
the camera, sine and cosine providers, four GP accesses to `iGpffffabe8`, and
the HI/LO pair for `D_00887310`. The proof records each final instruction and
resolved address. There is no masked relocation or unproven owned table left
in the exact comparison.

The final scoped verifier command was:

```text
python -E -s tools/verify.py src/shdMisc.c src/promoted/code1_0021.c src/promoted/code1_0038.c --json build/first-party-resume-167e607/fresh/worker7/final-verifier.json
```

It exited zero: 213 functions scanned, 186 `MATCH`, 27 `ASM`; of these,
208 were first-party, with 184 `MATCH` and 24 `ASM`. `git diff --check` also
passed. This is scoped validation, not a full build.

An early exploratory alignment lacked the optional user-site Capstone import
when run under `python -s`. Its alignment counts in the initial `shd_probe.py`
results are invalid and must not be used. The byte comparisons were valid.
The final proof recomputes the guarded baseline with the actual disassembler:
14 instruction edits plus eight relocation-only differences. Later caller
metrics and the final exact proof use the corrected setup; the exact proof
compares resolved bytes independently of disassembly.

The first installation check failed on line-ending context with
`core.autocrlf=true`. No source changed on that failed check. The same reviewed
patch passed `git apply --check --ignore-space-change` and was applied once.
Compiler logs retain the environment's nonfatal `pywin32_bootstrap` and
`MWCIncludes` warnings. Compilation and verification completed successfully.
