# Nibble-packed stat roll: accepted C recovery, 2026-09-25

`src/Event/Fcl/y_fclCombine.c::func_00308f40` is active C and **MATCH**
(object 1712B == retail window 1712B, 0 differing words). Owner report after
the change: 27 MATCH / 14 ASM of 41; no other function changed status.

## Relocations
R_MIPS_26 to func_001102d0, func_0010b6f0, func_00109470, func_0010ace0,
datGetFlag (func_00106330), func_002b2cb0, func_001102e0, func_003095f0,
func_0010cad0, func_00110270, func_00106390, func_001102c0; HI16/LO16 pairs
to D_00749350 (one per table read).

## Key shaping levers
- The earlier guarded draft (s64 nibbles, recompute, `opt_propagation off`,
  switch on n2) plateaued at ~100 fnalign edits. Rewriting from the retail
  asm instead of the m2c draft converged in a handful of probes.
- Nibbles are `s8` locals; retail re-sign-extends each at every use, which is
  exactly what b210 does for s8 locals. Extraction order is n1, n2, n0, n3.
- `packed = n0 | (n1 << 4) | (n2 << 8) | (n3 << 12)` into a `u16`, no masks.
- The switch is on n0 (the old draft's n2 was a semantic error).
- `total` is `s16`, loop counters `s32`, count compared as `(u16)func_0010b6f0()`.
- Contract fixes: `func_003095f0` now returns `u16` (it returns 0xB3/0xBA;
  object for it unchanged) and `func_001102c0` takes `u16`; both removed a
  spurious andi / sign-extension at the call sites.
- Byte sum read must be `((u8 *)&func_001102e0()[0x13])[j]` (addu base+j,
  lbu 0x26). `(p + j)[0x26]`, `p[j + 0x26]` and `*(p + j + 0x26)` all fold to
  addiu j,0x26 first.
- Declaring `packed` before `j` fixes the last s1/s5 rotation (8 -> 0).
