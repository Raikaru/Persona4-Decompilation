# func_001823d0 (code1_0018.c): field area ambient cue, matched 2026-09-26

Window 1904 B (1892 B of code plus padding), frame 0xC0. Relocations:
`iGpffff9db0` (GPREL16, the field map id pair), `D_005F08B0` (per-map area
tables), `D_005F1D00`/`D_005F1D08` (the compiler's pooled `{0, 1, 0}`
initialiser; the C now owns it), `D_007F1760` (cue counters), `D_007EFA00`.

The guarded round-1 draft sat at 342 words / 210 fnalign edits. It was
rewritten from the retail listing, and these levers took it to MATCH:

- **The normal is an initialised local struct.** Retail's `ld` from
  0x5F1D00 plus `lwc1` from 0x5F1D08 is the copy of a 12-byte
  `{0.0f, 1.0f, 0.0f}` initialiser inside the loop. An `f32[3]` initialiser
  copies it as three `lwc1` instead (360 words). A `struct { f32 x, y, z; }`
  gives `ld` + `lwc1` and the early `addiu $a2, $sp, 0xA0` (47 words).
  Declaring `tri[3]` before `normal` puts normal at 0xA0 and tri at 0xB0.
- **The five trigger arms are one inlined helper.** The slot id, the
  `(s16)` arguments (the `dsll32`/`dsra32` pairs come from the real
  `func_0045af60(s16, s16, s16, s16)` prototype) and the four-step counter
  are all in `static inline func_001823d0_play`. Retail's `addu $v0, $v1, $v0`
  wants `base + *slot`.
- **The dispatch is `switch (mode)`** with `case 1`, `case 8` and `default`.
  b210 tests 8 before 1, as retail does.
- **Declaration order sets the saved registers.** With
  `area, pos, node, stat, status, time` the registers came out as retail's
  s5/s4/s3/s2/s6 (43 words down to 3). Other orders: 43 and 19.
- **The area lookup indexes the table twice.** Retail loads
  `iGpffff9db0[0]` and then `[1]` before `sll`. With `entry = D[index]`,
  b210 sinks the single-use index load below the offset load (3 words).
  `if (D_005F08B0[index] == NULL) ... D_005F08B0[index][offset]` makes
  `index` multi-use. CSE merges the two table reads, and the loads keep
  source order (0 words). These were inert: a `state` pointer, swapped
  `index`/`offset` statements, u32 types, an inline lookup helper, and a
  `{major, minor}` struct view. A struct copy was worse (410).

Signature: `void func_001823d0(s32 arg0, u16 mode, u16 id)`. This agrees with
the only caller's extern in code1_0016.c. The file-scope `func_00479c30`
declaration was corrected from s64 to its defined s16 return. All 62
code1_0018 functions keep their status. `K_FldEvent_ArePosWithinDist` is
declared with the definition's `u32 (const struct RwV3d *, const struct
RwV3d *, f32)` through the incomplete struct tag.
