# pscRes::func_0036d3e0 exact recovery — 2026-09-22

Integrated first-party C in `src/Main/Social/pscRes.c`. The official verifier
reports `MATCH` for the target and all 30 functions in this owner. No guarded
targets remain in the owner. The production source is byte-identical to the
verified final source fixture.

## Source change

The prior reviewed palette view had 11 unmasked differing words: palette colors
and flags occupied the opposite saved registers, and the entry copy order
differed. Keeping `paletteView.colors` as a retained base and advancing a
separate `const u32 *colors` inside the upload branch reduced this to the two
entry words at offsets `0x20` and `0x24`.

Accepting the read-only image as `const void *input`, assigning it to a genuine
`const u8 *source` cursor, and declaring that cursor before the palette view
closed the remaining two words. Flags are initialized before the input cursor;
the header and upload cursor have separate lifetimes. No extra move, compiler
pragma, assembly, volatile value, dummy state or uninitialized read is involved.
The existing scoped decoded-depth phase is retained.

The final source names the observed header fields in `PscImageHeader`:
palette count at `0x10`, width at `0x12`, height at `0x14`, pixel format at `0x16`,
and a `0x40`-byte header. `PscPaletteView` holds the palette base and flags.
Its colors member is assigned on the depth-4/8 branches and read only after
those branches set the presence flag. The format decoding, diagnostics,
palette skips, 16/256-color uploads, pixel loops and lock/unlock sequence agree
with retail. Existing callers retain the 32-bit pointer argument and raster
handle return ABI.

Only this owner's private raster declarations were corrected to the existing
provider contracts: `RwRaster *` create/unlock returns, byte-pointer lock
returns, and the byte-sized raster-lock level. Ignored unlock returns remain
ignored. RenderWare implementations and shared headers were not edited.
The obsolete mismatch commentary, `NON_MATCHING` guard, assembly fallback and
unused `include_asm.h` include were removed.

## Proof and retained artifacts

All new work is retained under
`build/first-party-next-20260922/social-next/`. Historical probes were not
rerun or overwritten. This lane contains 27 successful native results and one
failed compile; remaining queued hypotheses stopped after exact closure.

The accepted directory is `final-source-reviewed/`:

- `pscRes.c`, `body.c`, `owner.o`, `compile.json`, `compile.log`: immutable
  final native compiler input, output and receipt.
- `result.json`, `unmasked-proof.json`, `resolved.bin`: complete target-byte,
  sibling, relocation, owned-data and suffix proof.
- `independent-comparison.json`, `independent-resolved.bin`: separate
  relocation implementation using symbol addresses, without deriving targets
  from retail bytes.
- `official.json`, `official.log`, `official-completed.json`: official
  `tools/verify.py` main verdict for all 30 owner functions. The scoped wrapper
  reused the immutable native owner-context object after checking source,
  object, compiler and flag identities; comparison logic was unchanged.

The native profile is MWCCPS2 b210 with `-O2 -Iinclude`. The target emits
1144 bytes within the 1152-byte retail window. All 11 relocations resolve
correctly, every unmasked instruction word agrees, and the remaining eight
retail bytes are zero. There are no target-owned data relocations or allocated
data sections. All 29 sibling functions retain identical bytes and canonical
relocations; the owner's allocated data is unchanged.

`behavior-api-review.json`, `integration.patch` and `integration.json` at the
lane root document the source/API review and exact installation. The owner
still matched its frozen pre-change source before installation, and LF line
endings were preserved.

Validation commands completed:

```text
python -B -S -u build/first-party-next-20260922/social-next/probe.py final-source
  0 unmasked words; 1144/1152 bytes; 29 siblings/data preserved
python -B -S -u build/first-party-next-20260922/social-next/prove.py
  independent exact proof; official MATCH; 30 owner functions MATCH
python -B -S -u build/first-party-next-20260922/social-next/integrate.py
  installed byte-identical proven source; remaining owner targets 0
python -B -S tools/decomp_lint.py src/Main/Social/pscRes.c --errors-only
  1 first-party file; 0 findings, 0 errors, 0 warnings
git diff --check -- src/Main/Social/pscRes.c
  exit 0
```

Do not rerun completed probe/proof/integration actions: their destinations and
receipts are immutable, and the probe baseline intentionally names the old
owner source. Reuse the saved final object or create a new receipt for any
future changed source.

## Identities and handoff

| Artifact | SHA-256 |
| --- | --- |
| Original owner source | `ec9da450c5755f434ed21d84a6123a2a9d127250d1bbcae802e5819d3ac93e55` |
| Integrated owner / final fixture | `9e40a39bea69879a374109924fcd5335c230834594ba5c120f93d8c5a9de5fc1` |
| Final native owner object | `138c01521223bb6e0fa37a3d4ad647476616c017ac44d748041f0d6e0e8b43f4` |
| Compiler | `286548490e2e902cfef21dcf39cd5af23766731585d90dea747f8781eadcafd7` |
| Resolved target bytes | `75c6fdba0a8ee57e121d2b597983b285286c3ac1555657052605a9659736a2f6` |

Released owner: `src/Main/Social/pscRes.c`. No remaining assigned targets.
No commits, pushes, whole-repository builds/tests or publishing were performed.
Inherited changes in other owners were preserved.

The rejected `immutable-palette-typed-header-input` compile reports three
illegal implicit conversions from `unsigned char *` to
`const struct PscImageHeader *` at the owner's byte-pointer callers. Its
failure receipt is retained; the accepted `const void *` contract needs no
caller edits. One initialization probe command was temporarily blocked with
“couldn't determine the safety status of the request”; the unchanged retry
completed, without replaying completed compiler work.

Prime messages failed with the exact connector error
`AGENTS_BUSY: no agent family belongs to this conversation`. Local edits and
verification remained available; the durable note and final handoff carry
the result despite that messaging failure.
