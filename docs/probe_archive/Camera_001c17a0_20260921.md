# Animation and orbit camera poses: 001c17a0

`func_001c17a0` in `src/promoted/code1_001c.c` now builds its two camera
poses in ordinary C. The configured b210 `-O2` compiler emits **2,000 of
2,000 retail bytes exactly**, after resolving all **60 relocations**.
There is no remaining suffix, differing word, or alignment edit.
The completed official owner report is **57 MATCH / 6 ASM**.

All 62 other functions retain their bytes and canonical relocations, and
every allocated data section is unchanged. The private proposal and the
integrated owner also compare equal for all functions and allocated data.

## Recovered objects and behavior

The old guarded frame overlay is replaced with complete `RwV3d` vectors,
an `RwMatrix`, two-float horizontal directions, and two `P4CameraFrame`
poses. One transition object groups the two horizontal directions and the
two poses that use them. Each pose contains three position floats followed
by its complete quaternion.

The function preserves the action-flag early return and the animation and
orbit branches. The animation branch selects the animation record, updates
the unit state, transforms both sets of animation coordinates, and produces
both camera orientations and positions. The orbit branch retains the sphere
centers, scaled height and radius, horizontal side selection, both orbit
angles, the lateral focus adjustments, and both final poses. The final
transition, duration calculation, and optional notification retain their
retail order.

Named vector snapshots preserve the center before later focus adjustments.
The two lateral-offset calculations retain their distinct values and
evaluation order. Splitting each final multiplication by `0.21875f` into a
second ordinary statement closes the last operand-order differences. The
accepted function adds no padding, assembly, volatile access, helper,
redundant source store, or optimization pragma.

## Call contracts

The provider in `btlCamera_grouped.c` returns `u8 *` from `func_001d0730`.
The promoted owner's declaration now agrees. The new function and the
existing `func_001c1f70` consumer explicitly convert that result to the
`u16 *` animation-record view. The existing consumer retains every
instruction and relocation. No shared provider or header was changed.

The sphere-center provider accepts `(BtlUnit *, RwV3d *)`. The two-float
normalizer `func_003e41e0` takes two pointers, reads and writes two floats,
and returns a float. `func_0044b868` takes and returns a float. Matrix,
vector, quaternion, pose-transition, and duration calls retain their
established contracts. The dispatcher passes `(u8 *, s32, s32)` and does
not consume a return value.

The review covered the complete 516-line retail assembly, both saved
Ghidra and IDA bodies, the current dispatcher, the camera providers, and
the grouped animation lookup. The objects passed to callees cover their
complete read and write ranges.

## Saved evidence

All paths below are relative to
`build/first-party-finish-20260920/camera-current-work/`.

The accepted candidate is `17a0/offset-staged/`. The release directory
`17a0-release/` contains `proposal-proof.json`, `integrated-proof.json`,
`official.json`, `official-receipt.json`, and `live/compile.json`, together
with the complete sources, objects, resolved and retail bytes. It also
contains `change.apply_patch`, `before.c`, and `retired-floor.c` for the
replaced guarded implementation and its historical measurements.

The official current-owner command completed in session **35189**, exit 0.
`integrated-proof.json` was completed on September 21, 2026 at 04:17:40 UTC.
The integrated source and these completed receipts were checked again by
hash during continuation; the successful compile was not repeated. The
source patch is already applied and must not be replayed to recover output.

The proof establishes the configured owner build and its complete target,
sibling, relocation, and allocated-data comparisons. It is not a claim of
runtime emulation. Full-image integration and publication are recorded by
the prime separately.

## SHA-256 identities

Integrated `src/promoted/code1_001c.c`:
`75826ec67603b93b2fbe78c28382aa78667b0727254b14385d935fd7f05b0f88`

Official `17a0-release/live/owner.o`:
`01231042f1457778e1c4601198ff7441872a889466534c1d51f11c699563170b`

Resolved target and retail target:
`9073eda31109d0370a17fdbfde26195d3cd2bdbe3cb0b82263a4a511f90f2035`

Compiler (`mwcps2-3.0.1b210-060308`, `-O2 -Iinclude`):
`286548490e2e902cfef21dcf39cd5af23766731585d90dea747f8781eadcafd7`

`include/include_asm.h`:
`8c2cde497ecdcafdb07af8d62c1e0eb1a30eeb14d1c4567128e5f2714e5cd963`

`include/type.h`:
`d98ec5482a2782c2a9ca41f93d590ab2007b9817936d5208746d6e8794906db3`
