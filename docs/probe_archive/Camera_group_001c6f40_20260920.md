# Group camera projection: func_001c6f40

The recovered C function matches 1180 executable bytes in its 1184-byte retail
window. All 26 code relocations are resolved, the four trailing bytes are zero
alignment, and there is no added owned data. The resolved body SHA-256 is
`f6fd96f8761d84c24584b5371ae3d8690d0d9412471fc5764d872cc53d2c0a77`.

Ordinary local vectors, two complete poses and a quaternion replace the
decompiler's padded stack overlay. Their lifetimes reproduce the retail
0x120-byte frame, including the twelve-byte mixed integer/float copy for
`direction = unitPosition`. Earlier diagnostics that copied sixteen bytes
were rejected; the accepted vectors contain exactly three floats.

A real two-field projection record evaluates the half-angle before the
scaled extent. The inline projection then reproduces the division and
distance-addition order. Its distance-selection helper preserves the original
ordered comparison and unordered case. No compiler barrier, ordinary assembly,
forced padding or fabricated state is used.

The two output parameters now have their actual `P4CameraFrame *` types. The
active caller `001c73e0` supplies two 28-byte frames, and `001cf020` supplies
one. Both output copies are complete frame assignments. The canonical
unit-center and vector-normalization declarations agree with the actual
providers. The alternative position, group sphere, quaternion, tangent and
pose-copy operations were checked in their active C or retail definitions.
As in retail, the selected battle group must provide the bounds consumed by
the projection; this is not a guarantee for arbitrary empty group fixtures.

The P3 FES draft `func_002add10` at donor commit `04d95e2` supplies structural
evidence. That draft is guarded and is not claimed as a P3 match. Acceptance
uses the complete P4 assembly, both decompiler hypotheses and actual callers.

Fresh whole-owner proof preserves all 45 non-target functions in
`src/Battle/btlCamera.c`, all 63 functions in `src/promoted/code1_001c.c`, and
all allocated data. It also independently resolves `001c73e0` at 280/288 bytes,
`001cf020` at 76/80 bytes and the existing `001cde50` recovery at 1336/1344
bytes. Complete referenced basis vectors and scalar constants are checked.
The official proposed-owner verifier reports 95 MATCH / 14 ASM and scoped
structural lint reports no findings.

The older guarded caller `001ccdb0` now binds its result as a complete frame
but remains assembly-backed. Enabling that draft still produces the same five
unrelated, pre-existing argument-type errors before and after the change;
the logs are preserved and no match is claimed for it.

Evidence is under `build/first-party-resume-now/camera-other/release/`:
`proof.json`, `official.json`, `sources.json`, `handoff.json`, both immutable
whole-owner proposals and their compiler objects. The patch was prepared
after the separate `001be050` caller correction and preserves it.

```text
python tools/verify.py src/Battle/btlCamera.c src/promoted/code1_001c.c --show-mismatches --json build/group-camera-verify.json
```
