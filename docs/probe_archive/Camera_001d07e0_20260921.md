# Camera sequence 001d07e0 recovery

`func_001d07e0` advances a battle camera sequence through its keyframes. It
validates the active units, transforms position and focus offsets, clamps
the focus height for the sequence flags, builds both camera poses, and
updates the interpolation and sequence timers.

The C proposal replaces the old guarded reconstruction with native vectors,
quaternions, a RenderWare matrix, and two complete camera poses. Aggregate
assignments implement the vector and quaternion copies. The mode branches
use ordinary switches. The existing byte-index helper preserves the duration
address expression, and the next signed keyframe index is incremented as a
signed short.

The final arithmetic difference in the saved reconstruction was the lifetime
of the unit-radius product. Expressing radius times unit scale at each
displacement component lets the configured compiler share the product in
the retail destination. The Y/Z products pass through a small multiplication
helper before their coordinate additions; the X expression retains the
retail accumulator addition and multiply-add. These are ordinary floating
point expressions with no assembly, register barriers, fabricated side
effects, or explicit repeated stores.

The matching body was found in the completed prior-worktree candidate
`build/first-party-finish-20260920/camera-current-work/1d07e0-campaign/native-inline-radius-products`.
The publication continuation authenticated that candidate and its inputs,
resolved it again against publication symbols, and compiled a cleaned
proposal. The cleanup adopts the vendored `RtQuat` layout (`imag`, `real`),
matches `func_001bd780` to its provider's `void *` / `const void *` parameters,
removes the unused scale local and obsolete probe comments, and places the
types and helper before the function marker. The resulting object is
identical to the saved matching object.

The actual caller, `func_001bdeb0` in `src/promoted/code1_001b.c`, ignores the
return value. Its declaration is corrected from `void` to the recovered
`s32` return type. The only other source occurrence outside generated
decompilation output is the definition in `src/Battle/btlCamera_grouped.c`.
The caller correction reuses authenticated whole-owner before/after objects;
their complete bytes, every function and relocation, and allocated data
are identical.

## Evidence

The publication proof root is
`build/continuation-20260921/camera-worker1/grouped07e0/`.

The configured owner compiler is MWCC PS2 3.0.1 b210 with `-O2 -Iinclude`,
compiler SHA256
`286548490e2e902cfef21dcf39cd5af23766731585d90dea747f8781eadcafd7`.
The reconstructed function matches all **2,288 of 2,288 bytes**, with all
**40 relocations resolved** and no omitted suffix. All ten other grouped
functions retain their bytes and canonical relocations. The grouped owner
has no allocated data to alter.

| Artifact | SHA256 |
| --- | --- |
| Grouped owner before | `5d768585c6845f8e9afcb6da9b8ceb750ac3430fca3334b78d94d55e82c54747` |
| Grouped proposal source | `582248752bbccbf9cc4238e7b9a6c8a82719ca4bd73c6c398305506a6dded05d` |
| Grouped proposal object | `f397c1f1831613407c8766bd4e65d1c91cedfa6737b0389c36c115e0b984a3a2` |
| Resolved 001d07e0 bytes | `f6788391a82937ac5220ff6ac897ad763a83d769339e4ec4bb4e889135c2cf2d` |
| Caller owner before | `b4c05b808ae6238b443556757c4aa38f5484b9794a325371d08fbb7cc29ff02a` |
| Caller proposal source | `87913dadbc5351d0f401a593b6000c2c5c1648c9de2304275bea6188c84d04cd` |
| Caller object before and after | `9e6dc6545593d37d32e5c910104ff94dbbb42ac310e49c7731280173893241d9` |

`canonical-proposal/proposal-proof.json` binds the grouped source, compiler,
dependency hashes and resolved target; `preservation-proof.json` records its
ten siblings. `caller/proposal-proof.json` records the caller contract and
whole-owner comparison. `handoff-manifest.json` authenticates the UTF-8
patches, source snapshots, objects and proof files. Terminal 76001 performed
the saved-object publication audit, and terminal 84894 compiled and proved
the canonical proposal. Neither run changed a live source owner.

The proposal is ready for the prime's publication integration and official
verification. Replacing this sole grouped fallback makes the owner eleven
C functions with no remaining assembly fallback. `func_001c5b80` remains a
separate guarded recovery; its residual copy evidence is preserved in the
same continuation lane and is not represented as an exact match.

## Integrated-source seal

The current grouped owner now equals the canonical proposal, and the actual
caller has the recovered `s32` declaration. A fresh scoped official verifier
run in terminal 82470 reported eleven MATCH results. Terminal 36036 then
authenticated the complete current source, compiler/profile and dependencies
against the saved exact object, resolved all forty target relocations again,
and checked the ten siblings and absence of allocated data. The proof is
`build/continuation-20260921/camera-worker1/grouped07e0/current-integration-seal/seal.json`.
This seals the existing integration; it is not an additional recovery count.
Later camera residual measurements and remaining source/API contracts are
recorded in the lane's `worker1-followup-handoff.md` and JSON companion.
