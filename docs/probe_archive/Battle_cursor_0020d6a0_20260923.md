# Battle cursor rendering 0020d6a0

The final cursor callback animates movement and opacity, draws four rotating
segments, then draws the directional markers and central sprite. Its four
segment calls use the even indices `i * 2` of the eight-slot ring. The retained
guarded C incorrectly used `i`; retail explicitly doubles the loop index.

Configured MWCCPS2 b210 with `-O2 -Iinclude` emits **2,404 executable bytes in
the 2,416-byte retail window**. All **32 relocations** resolve exactly, followed
by twelve zero alignment bytes. The reviewed object preserves the other three
cursor functions and all allocated data, completing the four-function owner.

## Source identities that close the match

The scoped lifetime pass retains the incoming task on the stack while holding
the repeatedly used color components in saved registers. Without that pass,
the compiler retains task and spills a color byte, changing the loop setup and
its call arguments. No artificial local, assembly register binding or added
side effect is used.

The four direction components are copied as one complete value before the
directional-marker loop. This preserves retail's four loads followed by four
stores, instead of interleaving each load with its destination store. The
loop then has two actual local values: `ringScale`, read from cursor state,
and `ringRadius`, derived as `21.0f * ringScale`. Keeping those producer-first
declarations separate from the earlier interpolation values closes the final
six floating-register differences.

The original reset paths leave `alpha` unwritten, as do the already matched
three sibling cursors. The source keeps and documents this retail omission;
no defensive default is inserted.

## Shared contracts

The task-work getter is declared with its actual `u32 (void *)` signature;
all four cursor calls explicitly convert the task word and returned work
address. Their native instructions remain unchanged.

The tile-offset input of `func_002016e0` is a signed word. Its provider stores
the low halfword and separately converts the complete input to float. The
provider definition in `code1_0020.c`, declaration in `code1_0021.c`, and cursor
declaration now agree. Every function and allocated-data section in those two
owners remains unchanged after the correction. Their existing constant call
sites require no additional narrowing operations.

The resolved executable SHA-256 is
`82ca0402ead80925b043ee3b6f7d8844183ff0264ae0c9b5a955175187090fec`.
The initial semantic-index audit is retained in
`build/finish-first-party-20260922/continue-25-panel/`.
Discovery, rejected alternatives, complete source-family review, native
compiler receipts and installation/publication evidence are in
`build/finish-first-party-20260922/continue-26-cursor/`.
