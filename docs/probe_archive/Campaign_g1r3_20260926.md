# Campaign group 1, round 3 (2026-09-26)

Matched this round (each has its own note): func_001ba0e0
(ColorBlend_001ba0e0), func_001b4060 (SkillChain_001b4060) and func_002e6280
(YList_002e6280).

Contract fixes landed: func_00145510 is `s32 (u16, s32)` and func_0047d170 is
`void (void *model)`. See BattleSetup_001b1d70_20260926_body.c.

Levers that worked this round, and should be tried on similar floors:
- Pooled gp float literals written as literals, with the CSE and propagation
  pragmas removed. func_001ba0e0 went from 174 words to 8.
- Rebuilding from the retail listing instead of patching Ghidra/m2c drafts.
  Packet chains link to the *previous* packet's +0x58.
- Inline helpers that return 1/0 for membership scans. A real function
  defined earlier in the file is NOT inlined automatically.
- Removing a separate `u16 id = i` local. The compiler's own `(u16)i` temp
  then takes the register retail uses.
- Declaring locals in retail's `$s` order, highest first (func_001b4060).
  func_002e6280 and func_002e5ae0 did not respond to declaration order.

## Unmatched targets touched

- func_001b1d70 — 1 word (was 4). The one residual is func_00477c40's
  prototype. This unit needs u16 parameters, but seven matched callers in
  other units need 32-bit ones. Also measured: unprototyped declaration,
  `u32`/`s32 id`.
- func_002e5ae0 — 77 words, exact size (was 302). Body:
  YList_002e5ae0_20260926r3_body.c. The residual is a saved-register
  permutation.
- func_002e5000 — a fresh rewrite with the yListEntry helper under
  loop-invariant motion reaches 97 words (608B). Retail reloads
  `D_00882F70[0]->0x38` in the body; ours caches `&D0->0x38` from the loop
  condition. These did not help: struct-indexed condition (140), propagation
  off (91), CSE off (172), no LICM (152), `func_002e4870(0)` condition (90,
  but 628B).
- func_0037ca60 — still 25. New spellings of the bare-column branch all
  scored 25-75 and did not change the order (y before x, block-scoped
  column, `+=`, `col -= k`, a col-minus-k temp, operand swaps, `-k` / `k - col`,
  declaration order). The permuter found nothing.
- func_0034ddf0 — still 5. Literals for iGpffff8094 (pi/2) and iGpffff8220
  (212.5) tie at 5. With the pragmas removed it scores 123-161.
- func_002239a0 — still 58. A variable shared between e/k/a,
  propagation off and declaration order (with the parameter copy last) all
  scored 58-61. The permuter made 1720 compiles without improving it.
- func_001b05d0 — still 36. Integer base, declaration orders and a
  block-scoped entry all tie. Without CSE-off it scores 87.
