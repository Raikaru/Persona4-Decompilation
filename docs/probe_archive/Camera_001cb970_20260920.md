# Animation-center side camera: 001cb970

`func_001cb970` now emits 1,540 exact executable bytes in its 1,552-byte retail
window. All 25 code relocations resolve exactly, and the remaining 12 bytes
are zero alignment. The current two-owner verifier reports 100 MATCH / 9 ASM:
54 MATCH / 9 ASM in `code1_001c.c`, and 46 MATCH / 0 ASM in `btlCamera.c`.

All 62 other functions in the target owner retain their complete bytes and
canonical relocations, including 001c5500 and 001ca590. All 46 caller-owner
functions and all allocated data are unchanged. The sole caller declaration
and call in 001cbfe0 now use the actual `void (u8 *, f32, s32)` interface.
No other production owner or symbol configuration is changed.

## Source and contract

The old padded stack overlay is replaced by six real three-float vectors,
two complete 28-byte poses, and six two-float XZ arrays. The two centre
queries use the existing typed 001959d0 provider, which reads animation
bounds and writes all three centre components. The projection provider writes
both output floats; look-at writes the complete quaternion; both submitted
poses have complete position and quaternion storage.

The selected centre's Y value is captured in each branch and its Z value
inside each lateral-position branch. Those values remain live across the
calls where retail preserves them. The actual GP references fGpffff8128
and fGpffff8138 replace the draft's guessed numeric constants. The first
flag is initialized only on the requested update and honors battle flag
0x200000. Both cached flags remain untouched on subsequent updates. The
100-unit centre floor, 500-unit projection floor, 12.5-unit final-height
floor, optional visibility call and supplied duration are preserved.

The source requires the initialized camera/action/unit graph and valid
animation bounds that the original routine dereferences. No defaults,
padding, volatile access, assembly, or new optimization pragmas are added.

## Comparison lowering and portability

The two side flags retain the original guarded draft's `dot >=0.0f`
relations. Under the configured b210 `-O2` compiler they produce the exact
retail `c.lt.s`, constant-one load, `bc1f`, and zero-result sequence.
A read-only signature census found the same pattern in matched 00177120
and 00375970, whose current C also uses `>=`.

This does not assert portable IEEE equivalence between `>=` and a negated
`<` for NaNs. An explicit negated-comparison helper emits 1,540 bytes with
four comparison/constant-load ordering differences. Other explicit unordered
forms retain extra instructions. Their completed objects are saved. The
accepted result is the actual b210 output, whose full unmasked instruction
stream preserves the retail behavior; no claim that NaNs are impossible is
used to waive an instruction difference. `comparison-audit.json` records
the source/model disagreement for NaN inputs rather than hiding it.

The final quaternion-angle condition retains the decoded negated `<=`
spelling. No portable equivalence to a strict greater-than test is asserted.

## Validation and evidence

`build/first-party-next/camera-resume-b970/release/integrated-proof.json`
holds the actual owner objects, source hashes, official report, all 25
resolved relocations, sibling comparisons and allocated-data equality.
`change.apply_patch` is the exact two-owner patch; `retired-floor.c`
retains the superseded source and historical measurements.

A native GCC run of the accepted body with bounded finite provider doubles
passes 3,072 caller-contract cases. These cover initialized and retained
flags, the special battle flag, all cached flag values tested, both sides,
height floors, complete output poses, quaternion selection, visibility modes,
and ordered spline/duration calls. The compile log has no warnings. The test
does not emulate EE arithmetic or RenderWare numerical algorithms and does
not claim portable NaN behavior.

The existing whole-owner placement limitations are unchanged. Whole-image
verification, aggregate progress and Git publication remain prime-owned.

Final `code1_001c.c` SHA-256:
`52a7ee5aeb95f6bea89422218f7d945011d1792363e661da6fde6db9bebde235`.

Final `btlCamera.c` SHA-256:
`4ace68387db5a4fe4481a7b350a4d5fcfe7e29473f10e159f38e268f69ac9fc8`.
