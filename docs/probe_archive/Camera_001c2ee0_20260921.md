# Side and group camera poses: 001c2ee0

The configured MWCCPS2 b210 `-O2 -Iinclude` compiler emits **4,040 exact
executable bytes** for `func_001c2ee0`. Its 4,048-byte retail window ends with
eight zero alignment bytes. All **87 relocations** resolve exactly. The
official actual-owner run completed in session **3012**, exit 0, with
**60 MATCH / 3 ASM** in `src/promoted/code1_001c.c`.

All 62 sibling functions retain identical bytes and canonical relocations,
including 001c1040, 001c17a0 and 001c21d0. Every allocated data section is
unchanged. The cleaned proposal and actual-owner objects agree for all
functions and allocated data.

## Recovered source and semantics

The complete 1,053-line retail assembly, full IDA and Ghidra bodies, current
dispatcher and native providers were read before editing. Nine complete
`RwV3d` objects, two pose frames, one matrix, a horizontal vector and a pair
of grouped bearing vectors replace the padded raw frame. Their natural
allocation reproduces retail's 0x1e0-byte frame. Block-local displacement
vectors express the three products before the three pose-coordinate adds.

The old guard used two uninitialized scalars where retail calls direction
normalization and tangent. Both missing calls were restored. The first
three tangent calls and a later distance call reuse the cached converted
half-FOV angle, while the other lateral-offset calls recompute it as retail
does. The separate group-branch tangent takes the raw half-FOV, without the
angle-conversion multiplier. This distinction is retained deliberately.

The first side pose adjusts the working focus, while the second adjusts
the saved focus. The old guard updated the wrong Z coordinate; both the
saved-focus update and its pose output are corrected. The group branch
writes a complete XYZ pose before replacing its height, preserves the
300-unit radius floor, and retains every mode-specific height, timing and
notification assignment. The direct-timing and ordinary-timing provider
calls are kept distinct.

Phase-specific scalar lifetimes reproduce the preserved floating registers.
Natural maximum expressions, staged lateral-width products, and the existing
ordinary multiplication helper retain retail's operand and store ordering.
The accepted source adds no new optimization pragma, padding, alignment
annotation, volatile access, assembly, or artificial store. Historical
cold-floor comments are not current measurements; their original body and
annotations remain in the saved source snapshots.

## Contracts and scope

The sphere-center calls use `(BtlUnit *, RwV3d *)`. The group query retains
its six-argument filter/vector/height-output interface. The camera predicate
`func_001bc240` takes its actual `s32` camera address via explicit conversion.
Normalization, tangent, matrix and quaternion calls use the current native
contracts. Clamp and transition consumers receive complete camera poses.
The native `func_001bbf40(u8 *, f32)` provider stores the direct duration and
its reciprocal; it is not interchangeable with the normal duration call.

Only this target and its block-local declarations changed in this promotion.
The preceding owner-local ABI corrections remain unchanged. No shared
provider, header, configuration or publication worktree was edited. Valid
camera/action/unit/resource pointers are required, as in retail. This is
actual compiled-byte equality, not a claim of whole-game execution coverage.

## Evidence and identities

Evidence root:
`build/first-party-finish-20260920/camera-current-work/2ee0-release/`.
`integrated-proof.json` records the official target, references, sibling/data
checks and source/dependency hashes. `official.json`, `official-receipt.json`,
`live/compile.json`, the actual object and log, resolved/retail bytes,
`proposal-proof.json`, `change.apply_patch`, `before.c` and `retired-floor.c`
are retained. The clean proposal was compiled in session **36308**, exit 0.
Do not rerun completed compiles or proof commands to retrieve their output.

Source SHA-256:
`3517eaa00c7ab0424e8a1f86eb6d854db92a3adf832f2ef576438df0337c7af0`.

Actual-owner object SHA-256:
`98609a837b4dabca12da2c274b57f9f32edfea6fb06f1741c134eb08ed3e9f44`.

Resolved target SHA-256:
`0e4f7f73d14f3a600cb5b5a960a3dcc0fd00a6e72be84b8272b34903b509d377`.

Compiler SHA-256:
`286548490e2e902cfef21dcf39cd5af23766731585d90dea747f8781eadcafd7`.

The existing 001c5b80 five-word frontier is preserved. The next independent
camera target is 001c3f70.
