# Combination-menu position transition: 003297f0

Recovered in `src/Event/Fcl/y_fclCombineDraw.c` on 2026-09-20 with the
configured MWCCPS2 b210 compiler at `-O2`. The official owner verification
reports **42 MATCH / 28 ASM**, up from 41 MATCH / 29 ASM. Only `003297f0` was
promoted in this change.

## Source and ABI

The constructor outputs are ordinary `FclVec2` values. Direct component reads
retain the retail local homes; descriptor-position arguments still use the
existing aggregate interface. A shared pointer to `D_006440B8`, preserved by
scoped `opt_propagation off`, matches the bounds lifetime across constructors.
The two layout choices use an `if` / `else if` chain, and both selectors are
converted to their signed-byte values.

The consistent source signature is:

```c
void func_003297f0(f32 x, f32 displacement, u8 *task, s64 layout, s32 transition);
```

The EE has separate integer and float argument counters. The float-first
ordering preserves `f12/f13` and `a0/a1/a2`, while reproducing the early saved
float move. The definition, owner declaration, and both actual C callers were
changed together. `0032b770` remains byte-identical; a separately enabled
`0032b9d0` retains all 1732 bytes and canonical relocations. Neither caller
loses or changes an argument value.

The external retail call at `002FD054`, inside `002fbea0` state `0x8D`, was
checked against the executable and both decompiler exports. Its mapping is:

| Register | Value |
| --- | --- |
| `f12` | `417.0f`, converted from integer `0x1A1` |
| `f13` | `220.0f`, bits `0x435C0000` |
| `a0` | The original task, retained in `s4` |
| `a1` | `0` |
| `a2` | `1` |

The current `y_fclCombine.c` contains **zero C declarations or calls** for
`003297f0`: its incomplete guarded `002fbea0` draft represents state `0x8D`
only by `break;`. Consequently there is no incompatible external C site to
update. This conclusion comes from the source audit, not from the assembly
fallback preserving the machine ABI. The omitted state-machine case was not
restored as part of this recovery. Generated reference drafts are not
authoritative callers.

## Verification

| Function | Exact emitted bytes / retail window | Applied relocations |
| --- | ---: | ---: |
| `003297f0` | 1604 / 1616 | 50 |
| Protected `0032a960` | 1684 / 1696 | 53 |
| Protected `0032b000` | 1904 / 1904 | 54 |
| Protected `0032f4d0` | 1368 / 1376 | 22 |
| Protected `003233d0` | 2352 / 2352 | 59 |

The proof applies every relocation before comparing instructions. All
remaining window suffixes are zero; the new target's suffix is twelve bytes.
It owns no new table. All 69 other active functions preserve their bytes and
canonical relocation destinations. All three allocated data sections preserve
contents, alignment, and relocations. Scoped lint reports zero findings, and
`git diff --check` passed for the owner. These are owner-level proofs, not a
complete-image build claim.

```text
python -B -S build/first-party-finish-20260920/fcl-worker5/deliver.py current
python -B -S tools/verify.py src/Event/Fcl/y_fclCombineDraw.c --json build/first-party-finish-20260920/fcl-worker5/current/official-verify.json
python -B -S tools/decomp_lint.py src/Event/Fcl/y_fclCombineDraw.c
git diff --check -- src/Event/Fcl/y_fclCombineDraw.c
python -B -S build/first-party-finish-20260920/fcl-worker5/external_closure.py snapshot
```

The completed current compilation is immutable; inspect its retained proof
instead of rerunning `deliver.py current`, which rejects an existing output.

## Identities and retained evidence

```text
Draw source before:
5f249fe233e6cd32ca186f1ca580ffe85a5be2e97aa65ab30650f1458446bd4d
Draw source after:
a730f8e5fe12be8b3a8ca5e5173727e1ecab6ecfa288da56977fa01ca814b18e
Actual final owner object:
bb077f0025184a0df472db70bf26c077b6fe95a559e74c9976771e4f20a11115
Audited external combine source, unchanged:
5fc86bb4923509d3cc2f183c02dfa9acf88c7a467d9ea3c034f8d0ffe9fc3bf8
```

The archive root is `build/first-party-finish-20260920/fcl-worker5/`.
`current/proof.json` records actual header identities, all five fully resolved
targets, sibling comparisons, and allocated data. `current/official-verify.json`
is the official result. `caller-closure/proof-utf8.json` preserves the enabled
guarded caller witness. `external-closure/evidence.json` records the absent C
sites, retail state-table entry, exact argument-setup words, and source hashes.
`inputs/manifest.json` and the per-probe receipts retain frozen input and
compiler identities. `delivery-utf8/integration.patch` is the already-applied
reviewed patch; the older `delivery/` staging was rejected before integration
because a default-encoding read changed unrelated non-ASCII comments.
