/* object 1376B baseline / window 1376B / normalized_diff 130 (prior preserved-body measurement); differing offsets: six addu operand-order rows, loop-CSE dsll32/dsra32 region around +0xA00..+0xE00, and stack ldr/ldl read around +0x050 (historical note); classification: addu-order + loop-CSE + stack-read unaligned-load floors; ruled out ix-local, every u32/u8/s64 cast and pointer-local spelling, scaled-index address forms, loop-shape variants, and corrected shape/body (allocation then matched); no new source probing because durable prior residual remains in the hundreds. */
