# Side and approach camera poses: 001c21d0

The configured MWCCPS2 b210 `-O2 -Iinclude` compiler emits **3,336 exact
executable bytes** for `func_001c21d0`, followed by eight zero alignment bytes
in its 3,344-byte retail window. All **59 relocations** resolve to the retail
targets. The official actual-owner verification completed in session
**59522**, exit 0: **59 MATCH / 4 ASM** in `src/promoted/code1_001c.c`.

All 62 sibling functions retain their bytes and canonical relocations,
including 001c1040 and 001c17a0. Allocated data is identical. The clean proposal
and actual-owner objects agree for every function and allocated data section.

## Geometry and source reconstruction

The full 871-line retail assembly, complete IDA and Ghidra bodies, the current
dispatcher and native providers were read before editing. Nine `RwV3d`
objects, three independent horizontal vectors, four grouped projection-input
vectors, two complete `P4CameraFrame` poses and a `RwMatrix` replace the raw
padded frame. Their natural allocation reproduces the retail 0x200-byte frame.
The grouped projection inputs preserve the side-vector stores and their
relationship to the camera-horizontal and line-projection inputs.

The old guard omitted retail's **50-unit minimum framing radius**. This was
restored. The scale begins at 1.0 before the initial provider calls, as in
retail. The alternate target radius and extent are evaluated before the
125-unit snapshot-height clamp. The target-height product is written at its
two genuine uses, allowing the compiler to share the same loaded product in
the retail order rather than imposing a differently ordered cached scalar.

Native aggregate assignments retain all vector and quaternion snapshots.
The branch-local side-dot lifetime separates the two camera paths and fixes
their preserved floating-register allocation. Explicit staged lateral-offset
multiplications and the ordinary existing multiplication helper retain source
operand order. The tilt is selected before a single matrix-rotation call;
the accepted positive/negative test emits retail's exact three-word branch
layout. No new pragma, explicit padding, alignment annotation, volatile access,
artificial store, or assembly was added.

The body retains actor/resource/target height adjustments, forced and random
side selection, resource resets and camera state writes, both projection
paths, the saved direction, the 550-unit distance floor, both 25-unit pose
height floors, distinct-versus-equal pose selection, duration and optional
notification. Historical cold-floor comments preceding the accepted body are
not current measurements; the old body and annotations are preserved in the
release's `before.c` and `retired-floor.c`.

## ABI and scope

The native sphere-center contract is `(BtlUnit *, RwV3d *)`.
`func_001bc240` receives its actual `s32` camera address through an explicit
conversion. `func_001ec3d0(u8 *, u8 *, u8 *, u8 *)` receives four complete
two-float objects and returns the line distance; the native provider reads
and writes exactly those coordinates. Matrix, quaternion, normalization,
tangent and pose contracts agree with their current providers. The
dispatcher still calls `(u8 *, s32, s32)` and consumes no return value.

This promotion changes only this target and its block-local declarations.
No shared provider, header, configuration or publication worktree was edited.
The preceding 001c1040 owner-local constant and clamp-return corrections are
retained unchanged. Valid camera/action/unit/resource pointers are required,
as in retail. The proof is actual compiled byte equality, not whole-game
execution coverage.

## Evidence and identities

Evidence root:
`build/first-party-finish-20260920/camera-current-work/21d0-release/`.
`integrated-proof.json` contains the official target, resolved relocations,
sibling/data checks and source/dependency hashes. The actual-owner command,
object and log are in `live/compile.json`, `live/owner.o` and
`live/compile.log`. `official.json`, `official-receipt.json`, proposal,
patch, source snapshots and resolved/retail bytes are retained alongside.
Do not rerun successful compiles or verification commands for their output.

Source SHA-256:
`70d6d52683c03360152ed32fe06e3116e1246591e0f5a90b9bb93e1b4970d56e`.

Actual-owner object SHA-256:
`0efac103082d8ff18a37f006e0b02a924b906feeef474ce36b069a2cacac632d`.

Resolved target SHA-256:
`92138035a928817f64bf1f328fe34635231360c14e04724743b2f03ea16202dc`.

Compiler SHA-256:
`286548490e2e902cfef21dcf39cd5af23766731585d90dea747f8781eadcafd7`.

The saved 001c5b80 five-word frontier remains unchanged. The next active
independent target is 001c2ee0.
