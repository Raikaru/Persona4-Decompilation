/* object 3024B baseline / window 3024B / normalized_diff 668 (durable prior measurement); differing offsets: retail keeps packed f32 pair memory-resident (high word hoisted only) while MWCC caches both words in saved float regs, shrinking frame and rotating all call argument setup; repeated s64 arg2/arg3 normalization at 6a70/69f0; classification: float-pair register-hoist + s64-parameter-normalization floor; ruled out four pointer/local draft shapes and inline casts; no new source probing. */
