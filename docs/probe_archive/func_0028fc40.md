# func_0028fc40: current parameter-home audit

Measured on 2026-09-20 at `0360227d0a4194cb84500a5cf63ac554b38a79c2`,
branch `work/first-party-0360227`, using the configured MWCC b210 profile.
Owner: `src/Event/evtPolygonMovie.c`. The production function remains
`INCLUDE_ASM`; no candidate from this pass was integrated.

## Current proof

The guarded C emits **2,088 bytes**, comprising 522 instructions, in the
**2,096-byte** retail window. The omitted eight bytes are zero alignment.
Earlier comments that give 2,096 emitted bytes conflate the C prefix with the
retail window.

All 16 code relocations resolve to the retail words. The complete unmasked
comparison differs at only `+0x20`, `+0x24`, `+0x28`, `+0x2c`, and `+0x30`.
There are five positional word differences and four word-alignment edits:

| Sequence | Parameter home moves |
| --- | --- |
| Retail | `s1 <- a0`, `s0 <- a1`, `s5 <- a2`, `s4 <- a3`, `s3 <- t0` |
| Current C | `s5 <- a2`, `s4 <- a3`, `s3 <- t0`, `s1 <- a0`, `s0 <- a1` |

Every other emitted instruction, including every relocated instruction, is
equal after resolution. All seven sibling functions have identical bytes and
canonical relocation records against the complete production owner. Neither
object contains an allocated data section, and the target references no owned
local data or jump table.

The official current-owner verifier reports **7 MATCH / 1 ASM**. That ASM is
this target; its fallback's zero diff is not evidence for a C match.

## Caller and provider contract

The sole real call is in `func_00285dd0`, in
`src/promoted/code1_0028.c`, not `evtMain.c`. Retail prepares:

| Register | Source and meaning |
| --- | --- |
| `a0` | Event state plus `0x678`, the message context |
| `a1` | The allocated `0x114`-byte movie work object, stored at state `+4` |
| `a2` | Resource reader context `+0x34`, first data blob |
| `a3` | Resource reader context `+0x38`, second data blob |
| `t0` | Resource reader context `+0x3c`, third data blob |

The downstream `func_00290880` in `src/Event/evtMessage.c` consumes the first
pointer as its message context and the selected first-blob message data as its
second argument. The movie function returns the work pointer on its normal
paths and null for a missing first blob. Its caller discards that return.
There are no float positions or hidden result parameters that justify
permuting the five integer-register arguments.

Two existing declaration defects were reported to the prime without changing
their owners. The caller declares the movie function `void` and represents
four pointer arguments as `s32`; the definition and retail support a pointer
return and five pointers. Also, `func_00294040` is defined `void` in
`evtPMFileReader.c`, despite its caller consuming the pointer returned through
`v0` from `func_00452560`. The reader fills offsets `0x34`, `0x38`, and `0x3c`
with the loaded blob addresses. These type inconsistencies do not account for
the target's current home-move order: changing a separate translation unit's
declaration does not change this owner's compilation.

## Bounded source experiments

The pass measured the baseline and 31 additional whole-owner candidates.
The installed `opt_propagation off` / `opt_loop_invariants on` profile stayed
fixed. No generic pragma pairs were repeated.

| Family | New candidates | Result in positional words |
| --- | ---: | --- |
| `register` on meaningful local/formal cohorts; immutable pointer locals | 13 | All 5 |
| Block scopes for record rows, counters, or both | 3 | Row temporaries 5; separate counters 163 |
| Real inline helpers for first, second, third, or all record traversals | 4 | Third pass 5; the other three 163 |
| Void/typed pointer boundaries and integer address representations | 10 | Six at 5; integer work-address forms 476 |
| Descriptive parameter/local names | 1 | 5 |

Separate first/second pass counters rotate the saved-register coloring of the
context, work pointer, and counter. They preserve the 2,088-byte size but
regress to 163 differing words. Keeping row temporaries within their actual
loop scope changes neither the coloring nor the home order. The helper for
the third traversal also ties the baseline. Integer work-address arithmetic
adds instructions and emits 2,100 bytes; it is not a retained candidate.

Including the baseline, the distribution is 23 candidates at 5 words, five at
163, and four at 476. All 23 close candidates were fully relocated and checked
for the same five offsets, the zero suffix, sibling equality, and allocated
data equality. No new match or lower floor was found. The historical claims
that the order is universally unreachable from source, or necessarily chosen
by a particular compiler phase, are not proved by these experiments.

## Reproduction and artifacts

The private scratch lane is `build/first-party-0360227/movie/`:

- `run.py` extracts the current guarded body, preserves whole-owner context,
  deduplicates source-identical probes, and retains each source/object/log.
- `0028fc40/measurements.json` records all 32 measurements and source/object
  hashes. `owner-before.c` and `owner-before.o` preserve the actual baseline.
- `prove.py` and `proof.json` record full relocation resolution, raw-word
  alignment, all sibling comparisons, and the zero tail for the 23 close
  candidates. Capstone was unavailable; the alignment operates on fully
  resolved 32-bit words and does not depend on disassembler text.
- `official-owner.json` is the official scoped verifier report.

Run from `build/recovered-upstream`:

```text
python -B -S build/first-party-0360227/movie/run.py
python -B -S build/first-party-0360227/movie/prove.py
python -B -S tools/verify.py src/Event/evtPolygonMovie.c --json build/first-party-0360227/movie/official-owner.json
```

The harness checks the immutable baseline hash before reusing measurements.
This pass did not modify any production owner or caller, run a full build,
commit, or push.
