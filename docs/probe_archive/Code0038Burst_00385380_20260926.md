# Two-phase burst draw: func_00385380 recovered, 2026-09-26

`src/promoted/code1_0038.c::func_00385380` is active C and **MATCH**
(1512-byte object in the 1520-byte window; the tail words are alignment).
code1_0038 went from 87 to 88 first-party MATCH.

Relocations: gp floats `fGpffff8308`, `fGpffff82cc`, `fGpffff80bc`,
`fGpffff83c8`, `func_00373cb0` (x5), `RpSkyRenderStateSet` (x4),
`func_00364c90`, `sinf`/`cosf`, `func_0034f4a0`.

## Starting point

`Code0038_00385380_20260925_body.c` (round 1): 378/378 instructions, 13
edits, all FP colouring - retail gives the first interpolation (`start`)
`$f21`, the gp-constant/blend group `$f20` and the growth ratio `$f22`.

## The lever

Retail reuses the same FP register for pairs of values that never overlap,
and the source reused the variables the same way:

- one variable for the growth ratio in both phases *and* the phase-2 delta
  (`ratio = blend - extent`, `$f22` throughout);
- one variable for the start value and, after its last use, the half width
  (`extent`, `$f21`);
- one `blend` for both phases (`$f20`).

Merging start/half-width alone: 12. Adding the ratio/delta merge: 12. With
all three merged, the declaration order decides the colours: `ratio, extent,
blend` is 0; the other five orders are 11-17.

Kept from round 1: the gp multiply written first (`fGpffff8308 * ratio`),
`u8 alpha` declared before the resource local, `++*counter` in the final
test. The local `Vec2f_5380` typedef and `func_00364c90` block extern were
dropped for the `Vec2f` prototype `shd_misc_internal.h` already provides.
