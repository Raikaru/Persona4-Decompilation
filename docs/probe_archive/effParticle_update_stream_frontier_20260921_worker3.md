# Particle update and stream-record frontier

The immutable evidence is under
`build/first-party-focus-20260921/particle-update/`. `HANDOFF.md` explains the
provider and data-layout findings; `measurements.json`, `SEALED.json`, and
`INVENTORY.json` retain the native results and inputs. No new production
function was activated. The earlier 780-byte particle constructor and 572-byte
geometry constructor remain unchanged.

For `func_00487c30`, native record/header views and true snapshot-provider
declarations retain a 42-word difference in 892/896 bytes. The historical
27-word control used incomplete provider contracts. Per-case constant counts,
record cursors, indexed traversal, subtype helpers, and grouped iteration
state did not close the corrected candidate. All sixteen siblings and both
accepted tables remained unchanged; the existing hardware blocks were retained.

For `func_00487fb0`, the five pointer/stride outputs are actual eight-byte
`RpPTankLockStruct` objects. The old separate-local form passed a four-byte
pointer object to a provider that writes eight bytes. Reconstructing these
records restores the retail 0x1D0 frame. The selected candidate also restores
the first-material dereference, cached versus reloaded color-scale lifetimes,
and the two emitter guards. It emits 3476/3520 bytes with 756 differing words
and 358 aligned edits; it is not a match.

The existing transformed color-rescale assembly hardcodes a `lui` where retail
`00488980` uses `mfc1 $2, $f23; nop` before the VU transfer. This is an explicit
constraint on further ordinary-C-only changes with that block frozen.
`hardware-constraint.json` records the source and retail evidence. No assembly
instruction was added or rewritten, and no vendor/provider source was edited.

The update path also needs a coordinated game-provider contract correction:
`00484a90` currently omits the floating scale that the retail wrapper forwards
to its callback. No global callback declaration repair or provider promotion is
claimed by this archive.
