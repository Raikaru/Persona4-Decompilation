# func_0032b9d0 recovered (2026-09-25)

`func_0032b9d0` (y_fclCombineDraw.c, 1776-byte window, 441 instructions plus
three zero tail words) now verifies MATCH from C. The previous floor was 289
differing words (FclDrawB, 2026-09-17).

## Contracts it needed

- **The mode byte is `s8` in every callee.** Retail passes `arg3` unconverted
  to `func_002b77d0` and `func_00329e40` (already `s8`) and also to
  `func_0031e5b0`, `func_0031ac10`, `func_00324f80`, `func_003297f0` and
  `func_0032b000`. With an `s32` caller every `s8` call gets a
  `dsll32/dsra32`. With an `s8` caller every `s32` call gets one shared
  normalization. Those five prototypes (and the matched definitions of
  `func_00324f80`, `func_003297f0`, `func_0032b000` and caller
  `func_0032b770`) now take the mode as `s8`. The matched bodies already
  narrowed it with `(s8)` casts, so their bytes do not change.
- **`func_0031ac10` is `(u8 *, FclVec2, s8, s8, s32, u16, s16, s8, s8, u8)`**:
  the row index is converted to `s8` at the call and the list word is passed
  as a full word. `u16`/`s16` for that fifth parameter moves the `lhu`. The
  callee spills its sixth argument with `sh`. `func_0032b770` passes its
  packet through the `FclVec2` view and still matches.

## Shape levers

| Step | Differing words (fastprobe) |
| --- | ---: |
| Real signature, `FclByte4` colours, `*(FclVec2 *)&spF8` float-pair copy (a plain struct copy emits `ld/sd`) | 64 |
| `*(FclDrawColor *)&c128 = c124` for the grouped `lbu`x4/`sb`x4 copy while `c128` still receives `func_002b2a60` directly | 64 |
| `t2 = *(u8 **)(arg0 + 0x38)` reloaded after `func_0031ac10` | 42 |
| Block-scope `extern FclVec2 func_002b2970(f32, f32)` inside the `func_0031ac10` call (coordinator's struct-return lever): the row offset `mult` is evaluated before the constructor | 40 |
| Returned point assigned to the named slot `sp110` in the argument, which keeps retail's frame order (an unnamed temporary goes to 0xC0) | 0 |

Integer literals `535` and `359` passed to `f32` parameters reproduce retail's
runtime `cvt.s.w`.

Ruled out: `opt_propagation off` (~1800), an `off` variable reassigned after
the `if` (CSE removes the recomputation), an inline label helper (42),
`s16 off` (742).
