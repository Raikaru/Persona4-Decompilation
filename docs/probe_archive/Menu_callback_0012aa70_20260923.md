# Menu callback and memory-card task contract

`func_0012aa70` in `src/promoted/code1_0012.c` is installed as native C.
The configured MWCCPS2 b210 `-O2 -Iinclude` output is exactly 3,056 bytes
in the 3,056-byte retail window. All 105 code relocations and the complete
17-entry switch table at `0x00746770` are independently resolved and exact.

The callback retains its 17-state dispatch, deliberate fallthroughs, controller
checks, animation updates, memory-card task handle, and queued draw callback.
Quaternion and translation transfers now use complete vector, matrix and union
objects. The quaternion-to-matrix helper is fully inlined; its diagonal sum
is retained as the actual intermediate value used by the retail calculation.
Reset-phase models and the draw-task allocation have separate source lifetimes.

The memory-card registration provider `func_002aa300` now returns the task
handle its caller consumes. Its parent argument is a pointer, and declarations
in every first-party caller owner agree. The native provider and unrelated
functions retain their instruction bytes and canonical relocation targets.
The existing `func_00124350` takes the work pointer passed at the retail call
site; the argument is unused by the provider, whose native body is unchanged.

The state-8 identity-matrix macro reads and ORs its existing flag word. This
branch does not initialize that word in retail; the reconstruction preserves
that behavior rather than inserting an initialization. The separate quaternion
paths set their own matrix flags as before.

The persistent copy destination at `0x00762D80` is a complete `0x5D0`-byte
external object. Its small-data section annotation preserves the observed
GP-relative address materialization at `0x0012AB24` while accurately describing
the buffer extent. Every native function and allocated section remains
unchanged by this correction. The curated symbol entry is derived from the
retail GP displacement, and the generated symbol update retains all prior
definitions.

The reviewed sources, native compiler receipts, target/table proofs, sibling
comparisons and installed-source official reports are retained in
`build/finish-first-party-20260922/continue-18-menu-integration/`.
`prepared-final.json` records the reviewed five-owner batch; `completed.json`
authenticates all 292 first-party owner sources, native objects and reports at
**6,546 MATCH / 314 ASM**, including `func_00386c00` from the accompanying
shuffle recovery. Failed and superseded candidate records are preserved.

Focused source-integrity lint and `git diff --check` passed. The batch's full
build and source-link publication evidence is recorded separately in that
integration directory; no assembly fallback is counted as recovered C.
