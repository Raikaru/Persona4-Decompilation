# func_00314ef0 recovered (2026-09-25)

`func_00314ef0` (y_fclCombineDraw.c, 1056-byte window, 263 instructions plus
one zero tail word; 32 `R_MIPS_26` call relocations, no data relocations) now
verifies MATCH from C. Prior floors: 307 differing words (live guarded body),
23 alignment edits / 243 words (worker-5 lifetime lane).

## Levers, in measured order

| Step | Differing words |
| --- | ---: |
| Guarded body (s64 params, byte-wrapper colors, `fclPacketPosition`) | 307 |
| Real signature `(u8 *, s8, FclVec2, s16, s16, s8)`, `FclByte4 = func_002b2a60(...)`, `FclPackedPosition` locals passed as `.position` | 87 |
| `o2 = order + 2` staged right after the first `func_002b7750` call | 73 |
| Each `func_002b81f0(*slot)` used directly as the store base (149.0f is then materialized in `$f21` before its lookup instead of a `lui`/`sw`) | 19 |
| Slot pointer `(u8 **)(t + 0x258) + slot` (retail `addu $v0,$v0,$fp`) | 18 |
| `func_002b77d0` prototype with `f32 depth` after `color` | 0 |

## The 77d0 parameter order

The last 18 words were the depth constant being loaded after the order cast at
every `func_002b77d0` call. `$f12` is independent of the GPR slots, so the
position of `depth` in the prototype does not change the ABI, only the order
MWCC evaluates the arguments in. The callee's own retail prologue
(`asm/nonmatchings/y_draw/func_002b77d0.s`) moves `$f12` to `$f20` between the
`$a3` color spill and the `$t0` order move, which is the declaration-order
evidence. `include/fcl_row_draw.h`, the guarded `y_draw.c` definition and every
call in y_fclCombineDraw.c were reordered together. The matched caller
`func_00320970` is unchanged, and y_draw.c (55/3) and y_fclCombine.c (26/15)
keep their status.

Ruled out on the way: `opt_propagation off` (165-212, it CSEs 151.0f into
`$f21`), `s16`/`s64`/`u16` staging of `o2` (120-194), inline row wrappers (199),
double or integer depth literals and explicit `(s16)` casts (no change),
`s32 order` in the provider (56).
