# Native angle-limited camera transition: 001c9820

`func_001c9820` is live ordinary C in `src/promoted/code1_001c.c`.
The actual-owner MWCC b210 verification reports **62 MATCH / 1 ASM**.
The target emits 3,364 bytes identical to retail after complete relocation
resolution; the remaining 12 bytes of its 3,376-byte window are zero.
All 62 sibling functions retain identical bytes and canonical relocations,
and all allocated owner data is unchanged.

The implementation uses existing `RwV3d`, `P4CameraVec2`, quaternion holders,
and two complete `P4CameraFrame` poses. It retains the actual center-height
and center-depth lifetimes and the angle conversion product. Its float-second
signature follows the retail argument saves in `a0`, `f12`, `a1`, and `a2`.
The pose copy, projection, and quaternion-angle declarations agree with their
providers in `code1_001b.c` and `code1_001e.c`.

The exact `9820-campaign/native-scalar-cache-order` result and the prepared
`9820-release` proposal were recovered from saved work. Their source and object
hashes were checked before integration. Neither completed candidate job was
rerun. The earlier 1040, 21d0, 2ee0, and 3f70 official receipts were also verified
without recompilation. The only new camera compile was the live owner check.

Prime authorized the corresponding prototype and call correction in
`src/promoted/code1_0022.c`. The declaration now places `f32 angleLimit` second;
the call is `func_001c9820(camera, 0.0f, 0, 1)`. These are that owner's only changes
in this recovery. Its official check reports **93 MATCH / 5 ASM**. Every one of
its 98 functions, every canonical relocation, and all allocated data are
identical to the retained before object. Both official checks completed in
terminal session **48636**, exit code zero.

Evidence root: `build/first-party-finish-20260920/camera-current-work/`.

- `9820-release/integrated-proof.json` and `official-receipt.json`: live camera
  target, sibling and data proof, retained compiler output, and official report.
- `9820-release/before.c`, `candidate/src/promoted/code1_001c.c`, and
  `change.apply_patch`: frozen complete camera source before and after.
- `9820-caller-contract/integrated-proof.json` and `official-receipt.json`:
  complete caller-owner proof, with `before.c`, `after.c`, `change.apply_patch`,
  frozen dependencies, and retained before/live objects alongside them.
- `remaining-worker1-audit.json`: earlier release receipts and saved 9820
  candidate object hashes verified without recompilation.

| Artifact | SHA-256 |
| --- | --- |
| Camera source before | `8b7ca8d4393509ced5acfac0b0bb26b799b56e1fe0bd65d186595630e47c5361` |
| Camera source after | `f4be6c5798c55d757ddf602a749e5e678d4fefb115dda1ec977c5d3df6fd016c` |
| Camera live object | `582aa54ad5824590301aabcde1571d28e3c9116fecbaf6bd54fee18fa3f47bd7` |
| Resolved 001c9820 bytes | `3466cb2662a10209bd6eb9ae769e621a2f08303c5b7125c416dd14ad2afe1d1f` |
| Caller source before | `09b5a1c9a3bff6c44792d553f431e2c03aa3125b1e8b7c8861ed538c150e16cf` |
| Caller source after | `9f983fdc810c0eb7918507596496eca3f7997652154277326ecc18da87e72b4e` |
| Caller before/live object | `c7932430f7d5c59ad8a428324b8688a315f492f2791a0736c0557515adc79c73` |
| MWCC b210 executable | `286548490e2e902cfef21dcf39cd5af23766731585d90dea747f8781eadcafd7` |

Configured flags remain `-O2 -Iinclude`, with r5900/eabi assembly through
`mwccgap`. `001c5b80` remains the sole assembly-backed function in this camera
owner and is being investigated separately.
