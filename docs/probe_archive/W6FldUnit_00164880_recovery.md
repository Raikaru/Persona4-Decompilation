# Spawn selection: 00164880

Recovered on 2026-09-20 in `src/promoted/k_fldUnit.c`. The actual integrated
owner now contains **39 MATCH and 3 ASM** functions.

## Source and proof identities

```text
before 09849cdd779745eb3342aefc26a6a490b4a9da9b16f01b8bad068c962ae801d3
after  5738ac0095c03481bd423c187f0a8332dec5ddad4c792d2a11bbaf2733495998
object 853ddd2a8a9818371971fcb74bdd2f4d7f0639ddc70961d0bddacf6fa8a175d4
resolved target aaf2ba389bd5bf7d86b68f440a1c8c0790b2f032e97f0e5a91ac5bfb8b87b5d2
```

The current target matches **1728 of1728 bytes** after all **45 relocations
are applied**, with zero unmasked differing words and no omitted suffix.
All **41 neighboring functions**, their canonical relocations and every
allocated data section remain unchanged from the accepted00167560 owner.
The random-selection helper emits no additional function, and the target
owns no jump table.

The actual owner compiler is MWCCPS2 3.0.1 b210 with `-O2 -Iinclude`, SHA-256
`286548490e2e902cfef21dcf39cd5af23766731585d90dea747f8781eadcafd7`.
The retail ELF SHA-1 is`4eeec0360cf2715535d9f7e52eb69d786fb0158c`.
The official current-owner verifier exited zero:42 functions,39 MATCH,3 ASM.
Its JSON SHA-256 is
`aff2d93f966f4ce13fe9c5e2fb60f04096a34921b10743d408bbe47081354166`.
The source whitespace check also passed. No full-image or full-tree job was
run by this worker.

## Correct behavior and genuine data lifetimes

Retail and both exports establish several corrections to the former draft.
The second field mode is an else branch. A candidate below the distance
threshold restores the previous best node/distance and exits its neighbor
scan. The first mode does not query the second mode when its population is
already full. The best-distance comparison preserves the unordered COP1
case with `!(distance <= bestDistance)`.

The previous explicit Frame padding is removed. Four instances of the
existing three-float V3 type hold the two positions and their differences.
Aggregate position copies retain the retail stores and natural stack layout
without padded structures or fake memory accesses. Top-tested loop entries
retain the retail condition and load-delay sequences. Each party-readiness
value and model-slot address has its actual lifetime across the distance
queries.

The genuine random-selection helper contains the list count, unsigned random
remainder, selected-node walk, party distance checks and retry predicate.
Its result and rejection flag use a shared comparison-state value. Position
components are read in the retail order. A node temporary is used for the
first mode's saved candidate and the second mode's current candidate in
mutually exclusive paths. This closes the final saved-register rotation
without extra state, volatile declarations, assembly or padding.

## ABI closure and provider reconciliation

The target remains`u8 *(s32,u32,f32)`. Its three real caller instructions are
in func_00164570 at00164660,001646e4 and00164788; that entire caller is
unchanged. The inspected providers retain their actual list, predicate,
matrix-pointer and vector-length contracts. The owner uses the native
`RwUInt32 RpRandom(void)` API, declared in
`include/rw/inc/rprandom.h:54`, through a file-scope u32 declaration of
003b7060. The former block-local shadow is no longer needed in this target.

Prime owns reconciliation of the separate legacy guarded provider definition
in `src/promoted/code1_003b.c`, which still spelled the result u64 when this
proof was sealed. Its captured source SHA-256 is
`dd8705a068184b64c1d175a69bb4b416ed46fff93673ec400aa000cd2126ccbf`.
The default provider remains the actual retail assembly. The new consumer
matches the native SDK contract and all applied relocation bytes. Publication
should include prime's provider-source reconciliation; this worker did not
edit that external owner.

The complete replay package is
`build/first-party-finish-20260920/field-worker6/continue-64880/`:
`proof.json`, `integration.patch`, before/after source snapshots,
`current-owner.verify.json`, `closure/ABI.md`, and
`func_00164880/current-integrated/audit.json`. Frozen input sources, headers,
retail assembly, both exports and compiler/config identities are retained.
The already verified00167560 object was reused as the baseline. Completed
probe jobs were not recompiled to reconstruct their evidence.
