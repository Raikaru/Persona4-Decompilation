/* object 3392B / window 3392B / normalized_diff 0; differing offsets: none (ASM baseline); classification: COP1 accumulator-chain floor; ruled out: extensive retail mula.s/madd.s/adda.s interpolation chains repeated across the 16-entry gradient/vertex loop, which plain MWCC C cannot emit; no source probing per floor policy. */
void func_00383f80(u8 *arg0)
{
    /* Retail's gradient renderer is built around COP1 accumulators. */
    (void)arg0;
}
