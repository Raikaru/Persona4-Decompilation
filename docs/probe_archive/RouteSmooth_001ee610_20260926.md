# Route smoothing 001ee610 recovered

`func_001ee610` (1600-byte window, 398 instructions, frame 0x4E0) is MATCH in
`src/promoted/code1_001e.c` (118 MATCH / 2 ASM, previously 117 / 3).
MWCC b210 `-O2` with `opt_loop_invariants on` scoped by push/pop.

Fresh body written from retail; the old goto draft scored 437 edits.
It builds the path from the goal node back through `parent` links, skipping
nodes that the last point still reaches (`func_001ece50`), rounds sharp
corners (dot <= cos 80 deg) with two extra points, then subdivides each span
into four cubic-spline points.

Measured steps (fnalign edits):

- straight rewrite with a two-float `RoutePoint`: 291;
- `opt_loop_invariants on` (retail hoists `&pts[n]` into `$s5` and
  `pts[0].z` into `$f20`): 178;
- locals declared in reverse stack order (the 128-point array lowest): 63;
- `if (i + 1 >= n)` orientation and the `return 1` shared by both tails: 46;
- `seg[2]` for the previous/current segment (`seg[0] = seg[1]` copies with
  lwc1/swc1 pairs; two plain locals copy through `ld`/`sd`): 31;
- `i` declared before `n` (closes the `$s2`/`$s3` swap): 7;
- `n > 1` instead of `n >= 2` (branch through `$at`): 5;
- start the walk as `node = goal` inside the else (retail keeps the empty
  `b cond` trampoline; `goal` is the value computed before the call): 0.

Pooled literals: fGpffff8334 = 0.173648f, fGpffff8338 = 0x3EB33334 which is
exactly `1.0f - 0.65f` (not 0.35f = 0x3EB33333), fGpffff80fc = 0.65f,
fGpffff833c = 0.226563f (the third spline row; the others use 0.2265625f).

`RouteNode` now carries `RoutePoint pos` in place of the `x`/`z` pair
(same offsets); func_001eed10's two reads became `->pos.x`/`->pos.z` and it
still matches.
