# Window frame fade 00383f80

`func_00383f80` in `src/promoted/code1_0038.c` is now C and verifies as MATCH
(847 instructions in retail's 848-word window). No other function in the file
changed status. The file-scope `func_00457120` prototype now returns `s32`, as
its definition in code1_0045.c does. The five users in this file only add
0x80 to it and all still MATCH.

## From the guarded draft (143 edits, 785/846) to exact

- **Alpha conversions are per use.** Retail multiplies `192.0f * scale` once
  (into `$f20`) but converts it to u8 again at each of the four vertices. Writing
  `(u8)(192.0f * scale)` at each use reproduces this: MWCC shares the product
  but not the conversion. The draft's `(f32)(u32)(u8)(u32)(...)` was shared
  whole, which left 61 instructions out. 143 -> 63.
- **Types from the constant stores.** `alpha` is `u8` (retail
  `daddiu $s0, $zero, 0xff`), `v18` is `s32` (`addiu`, no `andi` before the
  `mult1`), and `alpha = 255.0f * scale;` converts directly.
- `if (++*(u16 *)(state + 2) >= 0xA)` keeps the incremented value, as retail
  does.
- The vertex `.scale` store follows the four colour stores.
- **Two clamps are conditional expressions.** Retail's `beqz` to the else arm
  plus a `b` over an empty then arm is
  `v18 = (v18 < 3) ? v18 : 2;` and `var17 = (tmp < 6) ? tmp * 2 : tmp;`. 20 -> 10.
- **A separate counter for the second loop.** Retail gives it `$s0`, which
  alpha has freed. 10 -> 6.
- **The primitive table through an integer view:**
  `draw = (u32)D_00887310; (*(s32 (**)(s32, void *, s32))draw)(4, work, 4);`.
  This keeps the table address in `$s0` across the first draw, as retail does.
  A pointer-typed local is propagated back to `lui`/`lw %lo`, and
  `opt_propagation off` costs 162 edits. This is the same u32-cast hoist that
  code1_0035.c records for D_00887300. 6 -> 0.
