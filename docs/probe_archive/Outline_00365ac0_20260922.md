# Outline renderer 00365ac0

`src/shdMisc.c::func_00365ac0` is active C and officially MATCH. The installed
native owner reports **9 MATCH / 1 ASM**. The complete **1088-byte** function,
including all **14 resolved relocations**, equals retail without masking.
There is no alignment suffix or new owned data.

## Recovered geometry and interface

The renderer receives a real two-float position, depth, packed color, angle,
width, height and mode. Its ten vertices occupy a complete 160-float array at
a 64-byte stride. The two axis phases traverse the original eight-position
cycle; signed extent calculation and width/height contribution preserve the
retail floating-point instruction order. The original geometry parameters
remain live directly across the camera and trigonometric calls.

The color is decoded from most-significant red to least-significant alpha.
Depth, reciprocal camera depth, position and color stores precede primitive
submission. Texture state is disabled first. Mode with full alpha sets the
existing render flag around submission and clears only that bit afterward,
preserving any other callback-modified flag bits. No provider is declared
pure and no call, state load or arithmetic operation was moved across an
opaque call to obtain a score.

The shared declaration in `include/shd_misc_internal.h` is:

```c
void func_00365ac0(Vec2f position, f32 depth, s32 color,
                   f32 angle, f32 width, f32 height, s32 mode);
```

Six actual calls in three owners use this same declaration. The active
`func_0021a7b0` caller passes the position member of its existing packed-position
union. Guarded `func_0020ff00` now uses a real position object instead of
writing float bits through a pointer to an integer local. Guarded
`func_00386c00` uses the shared position type instead of a separate equivalent
local typedef. The old conflicting private declarations are removed.

## Native and caller checks

The original exact scratch candidates and coherent five-file proposal remain
under `build/finish-first-party-20260922/radial-drawing/outline/`. Recovery
authenticated their actual source, compiler, flags, dependencies and objects
before installation. It did not rerun completed scratch compiles.

The installed-source closure is
`build/finish-first-party-20260922/resume-outline-01/completed.json`.
Its `native/` directory contains fresh current-source native and official
receipts for all **15 first-party consumers** of the changed shared header.
The native profile is MWCCPS2 b210, `-O2 -Iinclude`. Each official invocation
explicitly reuses its just-compiled, source-bound native object.

All **349 functions** in the three actual caller owners retain identical
bytes and canonical relocations. All other header consumers also retain
their complete function sets and allocated data. The shdMisc closure
independently proves both the new outline and existing radial renderer;
its eight remaining functions and all allocated data are unchanged. The
preceding scratch comparison also preserves the radial as one of nine
unchanged siblings.

Both guarded callers were separately compiled before and after the shared
contract correction. Their lengths remain 3456 and 2220 bytes. The only
changes are five pairs of independent argument-setup instructions: loading
mode into the integer argument register and copying angle into the floating
argument register exchange order. Both argument values, all relocation
targets and every other function remain unchanged. The exact instruction
deltas are retained at
`radial-drawing/outline/contracts/guarded/*/changed-instructions.txt`.

The installed source SHA-256 is
`6fa1dfb94dc8a594d234418219929fcaa9d93c154bb9242ad7c970c64fdeb9ea`.
The complete native object SHA-256 is
`d96af3f4f64cdffe2f67990d13fe47ce4f506fc481595db53d8c2e993665bba0`.
This is a native code/data and source-interface verification; no gameplay
or emulator run is claimed. The remaining `func_00364680` fallback is not
counted as recovered.
