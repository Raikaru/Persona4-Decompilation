# Message-window procedure 002848c0

`func_002848c0` in `src/promoted/itfMsgProcedure_Window.c` is now C and
verifies as MATCH: 522/522 instructions, 0 differing words, 2076 bytes in the
2096-byte window. The other 20 functions in the file keep their status
(15 MATCH, 5 ASM).

## Starting point

The guarded body measured 182 words and 52 fnalign edits (plus 114
reloc-only). The instruction count was already within three of retail. The
residual was four branch-shape spots, recorded in the old floor comment:
case 8 tested the saved register instead of `$v0`, the case 7 scan loop had
`beqz` where retail has `bnez`/`b`, the two-flag test in cases 16 and 18
joined the wrong way round, and case 17 had a double branch.

## Levers

- **Three inlined helpers.** Each helper's `return` leaves the join that
  retail has:
  - `msgWinFindFreeEntry()` scans `D_008820B0` for a free entry. Its
    `return e` inside the loop is retail's unthreaded `bnez` then `b`.
  - `msgWinReady()` is `iGpffffb4d8 != 0 && iGpffffb4dc != 0`, returning 1
    or 0 in retail's order.
  - `msgWinResource()` is the body of `func_002833b0` for slot 0. It
    replaces the goto-dispatched copies in cases 4 and 5. Calling
    `func_002833b0(0)` itself is not inlined and scores 459.
- **`if ((p = func_00278fd0(arg0)) != NULL)`** makes the test use `$v0`.
- **Case 17 is `if (func_0027bec0(arg0) != 0) { ret = 0; }`.** Retail's
  `beqz` followed by a `b` to the same join needs a non-empty arm. An empty
  arm, or an arm that only breaks, is folded away and scores 71.
- **`#pragma opt_loop_invariants on`** (scoped with push/pop) is still
  needed. Without it the body scores 9.

Score by step: 182 for the guard, 71 with the helpers and `$v0` tests
(25 edits, all of them case 17), and 0 once case 17 had its arm.
