# Group and target camera interpolation: 001c80f0

`func_001c80f0` is integrated as ordinary C. The configured b210 `-O2` compiler
emits 2,012 exact executable bytes in the 2,016-byte retail window, with all
54 relocations resolved and four zero alignment bytes. The current owner
reports 55 MATCH / 8 ASM. All 62 neighboring functions and all allocated data
retain their complete bytes and canonical relocations, including the recent
001c5500, 001ca590 and 001cb970 recoveries.

## Reconstructed objects and behavior

The old padded frame overlay is replaced by five three-float vectors, four
complete 28-byte poses, a quaternion, a top-height scalar, and the existing
40-byte `P4CameraBlend`. The cache matches the independently inspected
`RtQuatSlerpCache` layout: two scaled quaternions, angular displacement, and
the near-zero flag. Every submitted pose has complete position and rotation
storage. Aggregate copies preserve the actual vector and pose boundaries.

Retail instructions and both decompiler exports exposed several material
errors in the guarded draft. The four raw half-FOV call sites use the actual
0044b868 provider. The sine approximation consumes the original angle and
its cube, rather than the squared values used by the draft. Quaternion XYZ
components are multiplied and then accumulated in separate stages. Position
blending reloads the original 8120/8124 weights after quaternion interpolation;
it does not use the sine-adjusted quaternion weights.

The individual-target path retains both normalization calls. Its 50-unit
minimum uses the retail negated ordered comparison. Group and individual
target modes retain the complete pose-copy or interpolation branch, the
25-unit output floors, visibility call, immediate application, and spline
submission with 0.75 duration.

The first real-object reconstruction was already 2,012 bytes with 18 differing
words. Separating the doubled-radius product, preserving the distinct float
lifetimes, and declaring radius/separation in the measured order removed the
register and operand differences. Staging the first position component's
multiply before storing it closed the last load-order pair. No padding,
volatile access, dummy effect, assembly, or new optimization pragma is used.

## Provider and caller contracts

The existing callers retain `void (u8 *, s32)`. Three declarations in this
owner now agree with their actual providers: the formation accessor returns
`u32` pointer bits, the unit-center getter takes `BtlUnit *` and `RwV3d *`,
and the group-bounds query takes `RwV3d *` for its output center. All 25
affected call sites in the owner have explicit conversions where required.
The guarded 001c2ee0 declaration's incorrect integer return was reconciled
with the same bounds provider's float return. The source remains guarded;
its compiled retail fallback is unchanged.

The target requires the valid camera/action/unit graph used by its caller and
a nonempty eligible group for the bounds query. The provider leaves outputs
untouched for an empty selection; no fabricated defaults were introduced.
The maximum reuses the existing `cameraSideRadius` helper. Its compiler-specific
comparison lowering preserves the exact retail instructions; portable IEEE
equivalence for arbitrary NaNs is not asserted.

## Validation and retained evidence

`build/first-party-next/camera-resume-80f0/release/integrated-proof.json`
contains the actual current-owner object, official report, all resolved code
references, source/dependency identities and preservation checks. The exact
patch is `release/change.apply_patch`; the retired guarded draft and its
historical notes are retained in `release/retired-floor.c`.

A native GCC execution of the accepted body passes 5,184 bounded caller cases.
It compares interpolation weights against an independent sine reference,
checks original position weights separately, exercises both cache branches
(including an unused NaN scalar on the near-zero branch), and verifies
complete poses, target normalization calls, floors, immediate/spline modes,
visibility and duration calls. These fixtures do not emulate EE arithmetic
or the complete RenderWare algorithms.

The 64-bit host compiler warns about the target's intentional 32-bit integer
to pointer conversion. The native fixture links its image below 4 GiB and
asserts that returned pointer bits fit before the call; the warning and exact
linker command are retained. Target MWCC output remains the authoritative
instruction proof. Scoped placement limitations are unchanged; whole-image
verification and publication remain prime-owned.

Integrated source SHA-256:
`d0233112118c2536940c47b836d4ab2121889b9b93ea9a67ed4f589aad520b37`.

Official object SHA-256:
`99c92de7bb7bb178c68b48cc3fef8af880e5857e8c5d5b3183c5a664095d161c`.

Resolved target SHA-256:
`71f7f47c295cd98f632cc9f9db57f1593e3591ca08b758bc72826c286039bf04`.
