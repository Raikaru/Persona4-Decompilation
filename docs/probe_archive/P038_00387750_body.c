/* object 1824B / window 1824B / normalized_diff 0; differing offsets: none (ASM baseline); classification: COP1 accumulator-chain floor; ruled out: repeated adda.s/madd.s trigonometric interpolation chains in both 4-entry vertex loops and color blends, which plain MWCC C cannot emit; no source probing per floor policy. */
void func_00387750(u8 *arg0)
{
    /* Retail animation vertex generation relies on COP1 accumulators. */
    (void)arg0;
}
