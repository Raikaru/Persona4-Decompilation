/* lane jof6, 002e6630, residual 120 words, object 648B/window 640B
   matches initial loads $a2/$a0; base lands in $v1 instead of $a1,
   swapped $a1/$v1 roles, end masks $a0/$v1 instead of $v1/$v0
   tried: aliases, opt_common_subs off, named off_a/off_b, field-first
*/
