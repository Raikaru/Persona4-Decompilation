# func_001ff490 (btlEPL.c) — MATCH (2026-09-26, campaign g1 round 4)

Size 2728B plus zero padding, in a 2736B window. The old draft stood at 374
words. It was rewritten using func_001fceb0's body shape (see
BtlEpl_001fceb0_20260926.md) and func_001fc630's model prologue.

Differences from fceb0:
- The blend target is the global palette `f32 D_00922CA0[8]`; each access
  re-materialises `lui 0x92` without any special spelling. The
  "separate D_00922CA4..CBC symbols" in the old draft were not needed and
  are gone. `target->start[n] = *(V4 *)&D_00922CA0[4 * n]` gives retail's
  four-loads-then-four-stores copy. Element-wise copies interleave (32 words).
- The target struct is { model; V4 start[2] at 0x04 }. The param struct is
  total/fadeIn/fadeOut, color[2], rotate at 0x10, scale at 0x14.
- func_0047a510 fills a BtlEplMatrix (RwMatrix layout) on the stack. If it
  succeeds, `D_00922C60 = matrix` produces the lq/sq loop; the declaration
  is now `extern BtlEplMatrix D_00922C60`.
- Each non-default case ends with `D_00922CC0[0] = 1`. Case 0 breaks when
  count != 0, so control still reaches the matrix copy.
- `#pragma opt_dead_assignments off`, as in fceb0.
