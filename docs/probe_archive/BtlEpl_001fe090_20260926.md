# func_001fe090 (btlEPL.c) — MATCH (2026-09-26, campaign g1 round 4)

Size 2796B plus zero padding, in a 2800B window. The old draft stood at
~652 words with a "sp70 stack-spill floor". This was a fresh rewrite on the
func_001fceb0 family shape (see BtlEpl_001fceb0_20260926.md).

- The `V4 color` local is on the stack at sp70 because of the
  `*(V4 *)func_00149ca0() = color` struct copies. That is not a spill.
- Each colour is `btlEplRgbaToV4(&color, &rgba.c)` followed by
  `color.v[i] = color.v[i] * mul[n].v[i]`, where mul = func_001b7020().
  Retail reloads only the first three components from the stack.
- Case 1 blends both pairs with the temporaries shape (b3 unscaled,
  `a3 + b3 * scale`) and writes the result back into `color` before each
  copy. Case 2 does the same with mul*inv + target*scale.
- `#pragma opt_dead_assignments off`. Without it the second pair fuses (138
  words). Putting the second pair in its own block with fresh temporaries
  and no pragma gives 254.
