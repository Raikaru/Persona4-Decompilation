# Event camera callback: func_00288af0

The interrupted worker result was recovered from
`build/continuation-20260921/event-lane/release-closure-v2/` and integrated into
the publication worktree on September 21, 2026. Its source inputs were checked
against the live files before applying the patch. The resulting nine owners
were then compiled again using their configured MWCCPS2 b210 `-O2` profile.

## Exact recovery

`func_00288af0` now emits **1,060 executable bytes in its 1,072-byte retail
window**. All **17 code relocations** are applied and compared to retail; the
remaining twelve bytes are verified zero alignment. The function is ordinary,
unguarded C. Its former assembly fallback is removed.

The callback uses the actual event-camera command layout, including the two
alternative payloads at offset `0x14`: direct float position/angles and curved
motion expressed as signed halfwords. Four independent three-float objects
represent the stack vectors. The two switches retain retail's dispatch and
case layout. The angle-wrap comparisons retain the retail unordered-comparison
behavior. Separate duration/easing snapshots and scoped `opt_propagation off`
preserve the measured call setup without fabricated effects or assembly.

## Shared Scene contracts

`include/scene_event_internal.h` gives the affected callers and providers one
set of declarations. The resource transform wrappers take their actual
16-bit resource IDs and pointers to complete three-float objects. Their
low-level lookup and transform declarations agree with the inspected
providers. The direct-position easing argument is signed byte; the rotation
easing argument remains unsigned byte. Both duration values remain full words.
The field-of-view and flag-reset providers explicitly forward their incoming
resource IDs to the lookup functions.

The affected caller inventory contains 28 direct retail calls across nine
owners. The separate scan of allocated non-code retail sections found no
aligned function-pointer references for these eight shared interfaces. The
source inventory includes guarded callers and the former uppercase alias in
`code1_0018.c`. The two low-level provider owners were retained as unchanged
controls.

The `evtModel.c` duration access uses the corresponding unsigned type of the
existing signed word object. Its three-float stack object replaces the byte
buffer. Guarded field callers now supply complete vector objects in place of
separate scalar locals. They remain guarded; their other pre-existing
reconstruction issues are not counted as new recoveries.

## Independent integration checks

The fresh objects for all nine integrated owners are byte-identical to the
reviewed worker proposals, including symbols, code, allocated data, and
relocations. The worker's before/after comparisons preserve 230 original
functions in those owners and another 131 functions in the two unchanged
controls. The recovered callback is separately checked against retail.

The prime repeated complete unmasked proofs for the callback and ten affected
provider/caller functions:

| Function | Executable / window bytes | Code relocations |
| --- | ---: | ---: |
| `00288af0` | 1060 / 1072 | 17 |
| `00268f20` | 216 / 224 | 1 |
| `00269340` | 248 / 256 | 3 |
| `00269690` | 176 / 176 | 1 |
| `00269740` | 220 / 224 | 1 |
| `00146e60` | 124 / 128 | 3 |
| `00146ee0` | 112 / 112 | 3 |
| `00294be0` | 1496 / 1504 | 20 |
| `00295740` | 456 / 464 | 19 |
| `00292ef0` | 488 / 496 | 8 |
| `00177b30` | 468 / 480 | 18 |

Every omitted suffix in this table contains only retail zero alignment.
The official nine-owner verifier reports **231 functions: 216 MATCH and
15 ASM**, with no other statuses. This is a scoped verification result, not
a full-image or full-first-party completion claim.

The preintegration audit checked eleven owner inputs, 359 recorded dependency
hashes, compiler identities, the UTF-8 patch, and every saved object receipt.
Evidence from the fresh integration is in
`build/resume-live-20260921/event/`: `audit-before.json`, `audit-current.json`,
per-owner compiler receipts, `integrated-proof.json`, `resolved/`, and
`official.json`. The verification script is
`build/resume-live-20260921/event_integration.py`. The original worker package
and all unsuccessful later Event experiments remain intact.
