# Unlock rows: exact `func_0032e570`

`func_0032e570` in `src/Event/Fcl/y_fclCombineDraw.c` is now exact over its
full 2800-byte window, with every relocation resolved. The old guarded body
was at 594 words.

## Contract change: `func_00330e50` takes an `FclVec2`

`func_00330e50` is defined in the same unit. Its second parameter is now
`FclVec2`: the body keeps `FclVec2 *p = &arg1; func_0025ecd0(p->x, p->y, ..)`.
Its only other caller, `func_0032c480`, passes `sp38.xy`. Both still verify
MATCH.

This lets every point be built inside the call that consumes it:
`func_00330e50(0x1D2, func_002b2970(135.0f, y), 43.0f, func_002b2a30(..),
..)`.

- **The lever.** The by-value form alone removes the pooled float constants
  (`135.0f`, `43.0f`, `1.0f` held in `$f21`-`$f23`). Those pooled constants
  were the old body's `mov.s` surplus.
- **Why not a helper.** An `s64` packet helper (inline union) keeps the
  packet in `$s0` instead.

## Other levers, in the order they closed the diff

1. **Buffers.** They are declared `char sp130[128]; char spF0[64]; char
   spB0[64];`. Retail has `spB0` at `0xB0`, `spF0` at `0xF0` and the
   128-byte `sp130` at `0x130`. The old sizes were swapped.
2. **String tables** are declared as `char *D_[]` arrays, read as
   `D_00644D18[0]`. They were plain `char *` externs, which are
   gp-relative; retail uses `lui`/`lw` pairs.
3. **Row selector.** It is read as `((s8 *)(t + 0x2D4))[*(s8 *)(t + 0x2D6)]`
   for retail's `addu idx, t` operand order.
4. **Else branch.**
   - It assigns `k = 0; x = 153.0f;` before the three-line loop, and
     `x = 153.0f` in the empty-text path.
   - The three-line loop body still spells `153.0f`. Only the five-row loop
     and the empty-row line use `x` (`$f21`).
5. **Three-line loop.**
   - It indexes the table as `(D_00643220 + ent[2] * 3)[k]`.
   - It loads `ent[6]`, `iGpffffb44c`, `ent[4]` and `iGpffffb440` after the
     `strcpy`, in that order.
6. **Loop init order.** Both five-row loops initialise with `for (j = 0,
   n = 0; ...)`. Retail clears the row counter first.
7. **Separate row/count locals in the `v == -1` branch** (`j1`/`n1`, declared
   after `j`/`n`/`k`). With shared locals both branches colour `n/j/copy` as
   `$s3/$s5/$s1`. Retail uses `$s1/$s3/$s5` in the first branch and
   `$s3/$s5/$s6` in the second. This closed the last 24 words (fastprobe
   score).

This is the same lever as `func_0031fa20`'s per-block handles: splitting a
local across disjoint regions changes b210's colouring priority.
