# Selector rows: `func_0031e5b0` still at 30 words

`func_0031e5b0` is still not matched. The draft `FclDraw_0031e5b0_body.c` is
unchanged. Installed with the prerequisites listed in its header, it measures
30 words:

- extern `(u8 *, FclVec2, s32, s8, s32, s32, s32)`;
- callers `func_0032b770` passing `*(FclVec2 *)&spC8`, `func_0032b9d0` passing
  `sp118.position` and `func_0032fbc0` passing `sp100.position`. All three
  callers stay MATCH.

`func_0032b9d0` is a third matched caller that the old header did not list.

## What decides the residual

- **Declaration order has no effect on `t5`.** Moving `t5` first, last or
  between the handles gives 30 every time. The same holds for eight orders of
  `id/idB/t4/t5/t6/h0`.
- **Why `t5` lands in `$s5`.** b210 folds `t5 = (s8)arg5` into arg5's
  register as a param-derived value (see docs/matching.md, "second clause").
  Retail does not.
- **Spellings that still coalesce (all 30):**
  - `t5` as s32, s64, s16 or s8;
  - `(s8)(s16)`, `(s32)(s8)`, `(s16)` casts;
  - an s8 `arg5` with no `t5`;
  - `arg5 = (s8)arg5`;
  - storing into `h0`'s variable;
  - a dead `t5 = 0`;
  - an initialised block-scope `s8 t5`;
  - `(s8)arg5` in the later blocks;
  - `opt_propagation off`, which gives 1070.

## Two shapes that break the coalescing (not yet usable)

1. **Assign `t5` inside the block-1 condition:**
   `if ((t5 = (s8)arg5) == 1) { id = 0x28A; }` with `s8 t5`.
   - Everything through offset 1956 then matches retail: `t5` in `$s7`, the
     handles in `$s5`.
   - b210 then re-extends `t5` once, at block 2's `t5 == 1`
     (`dsll32 $s6,$s7`), and CSEs that copy for blocks 3-6. That is +8 bytes,
     735 words by count.
   - With s32/s64 `t5` the copy is gone but `t5` coalesces again (30).
   - Rewriting the later compares as `(s8)t5`, `(s32)t5`, `1 == t5`,
     `t5 == (s8)1` or `(t5 = t5) == 1` keeps the extra extension.
2. **Reuse `arg5` as the handle for h1..h5** (param reassigned).
   - This gives retail's structure: `t5` is separate and the handles sit in
     arg5's register.
   - It recolours every other saved register (156 words).
   - It is not natural source, so it was not pursued.

The next attempt should find a spelling that keeps shape 1's non-coalesced
`t5` without the block-2 re-extension. The hard signature rule (2026-09-26)
also applies to the body's local `func_002b2970`, `func_002b6c30` and
`func_002b69f0` externs.
