# Campaign group 1, round 2 — unmatched targets (2026-09-26)

Matched this round (see their own notes): func_0037ef40, func_0037da60.
Banked earlier this round: func_0037ca60 (25 words), func_0037f6e0 (127 words).

## func_001b1d70 (code1_001b.c) — 4 words
The remaining words are argument conversions at the calls into the
func_00477c40 family. Retyping the callee contracts gives 0 differing words
in isolation: func_00477c40 taking u16, func_00145510 as
`s32 (u16, s32)`, and func_0047d170 taking `void *`. Those functions are
declared and called from many other files, though, and the retype changes
their callers' masking. Nobody has done that cascade yet, so the function
stays INCLUDE_ASM. Next step: retype the family across every caller, then
re-verify the whole affected set.

## func_002239a0 — 58 words
A full declaration-order sweep made no difference (every order gives 58).
The difference is in register allocation around the loop body. Draft bodies
are BRF*_002239a0_body.c.

## func_0034ddf0 — conversion register unresolved
A natural version with CSE on fixes the float-conversion register. It also
merges the repeated `171 * amount` product, which retail keeps separate.
Writing literals in place of the pooled gp globals had no effect. Draft body
is VNLN_0034ddf0_body.c.

## func_004b6030 (eff_afpack.c) — 93 words
Body: AfPack_004b6030_20260926_body.c. It uses the header/cursor split
(`hdr = arg0; arg0 += 0x10`), a separate counter per loop (i0..i5), and a u16
id in the colour loop but an s32 key in the bone-lookup loop. It also builds
`size` from separate `+=` statements and needs `#pragma opt_loop_invariants
on`. The best result had `hdr` declared after `i5`. A hill-climb over
declaration order from 93 found nothing better before it was stopped.

## func_002e5000 (y_list.c) — 97 words (the older YL_002e5000_body.c is 90)
Body: YList_002e5000_20260926_body.c. It uses inline helpers yListHead and
yListEntry, which reproduce the duplicated retail switch, with LICM on.
The object is 608B against a 624B window. Retail reloads
`D_00882F70[0]->0x38` (lui/lw/lw) for the `yListEntry(0, i)` argument on
every iteration. Ours keeps that address cached in a1 from the loop
condition. These changes did not fix it: `s8 *flag` (97), an outer bound
through `base` (138), an inner bound through D_00882F70 (97), `(s8)0` (97),
and `for (j = 0; ...)` in place of hoisting `j = 0` (104).
