# Rank-up callback 00252710, 2026-09-22

`src/cmmRankUp.c::func_00252710` is active ordinary C and reports MATCH in the
installed-source native and official checks. At this checkpoint the owner had **18 MATCH /
8 ASM**. Its 836 executable bytes, all 12 relocations and twelve zero suffix
bytes equal the complete 848-byte retail window.

## Private interpolation routine

The caller retains its context and special-color predicate in `$v0` and `$t0`
across `func_00252230`. That interpolation routine writes neither register.
Its previous externally visible C definition prevented the compiler from using
that knowledge and introduced extra saved registers and a larger caller frame.
Giving the real same-file routine internal linkage restores the retail frame
and preserved caller values without a register binding or assembly barrier.

The audit scans both retail code ranges for direct calls: all ten calls to
`00252230` originate in this source unit. No aligned stored copy of that
function address appears in the loaded retail image, and no authoritative C
file or header outside this owner names the function. These checks support
internal linkage; they do not infer it solely from a lower mismatch score.
The provider itself remains exactly 1244/1248 bytes.

At this checkpoint, five retained ASM siblings bound their calls to the local definition. Their
instruction bytes are unchanged, and complete unmasked relocation proofs also
remain exact: `00252a60`, `002566d0`, `00256be0`, `002570f0`, and `00257900`.
They remained ASM in this checkpoint's source and official report; this recovery
did not count them as additional C matches. Subsequent rank-up recoveries have
their own installed-source verification records.

## Actual draw and rotation interfaces

The existing sprite provider at `src/promoted/code1_0036.c::func_00366c70`
returns `s32` and takes depth as its third C argument, before the integer
width/height arguments. The old owner declaration placed depth later in the
list and declared a void result. The corrected declaration and all 22 calls
use the actual provider order. The two independent EE argument streams retain
their runtime values while the compiler reproduces the retail setup order.

The actual matrix rotation interface returns a matrix pointer and takes a
matrix, a three-float axis, a float angle and the combine operation. All eleven
owner calls now use that declaration. The interpolation state owns a genuine
three-float axis in place of three unrelated scalar members. Its layout and
every interpolation instruction are unchanged. Matrix storage in the new C
callback is the complete aligned `RwMatrixTag` layout from
`include/rw/plcore/bamatrix.h`.

The eight UV words are copied from retail `006361f0` into a complete 32-byte
union with word and four-coordinate views. Both copy cursors traverse the
representation of their complete arrays, and both words are read before the
two stores on each of the four iterations. The float view gives the sprite
provider the actual coordinate array. The values are the same finite floats
as retail; no extra object, synthetic padding or uninitialized read is used to
obtain the match.

## Callback behavior

The existing work counter advances unless its pause bit is set. The first
thirty frames interpolate the first state pair; later frames interpolate the
second pair. The comparison `frame > 30` restores the retail predicate
register without changing the signed comparison domain. Completion is returned
at frame sixty and later.

The special color branch retains its exact packed colors and mode update.
Texture selection follows the UV copy. Rotation then fills the actual matrix
before the sprite draw. The original render-state calls and the mode-zero
versus mode-one draw paths retain their order and arguments. The incoming
unused first argument and work/context arguments keep the established callback
interface.

## Current-source evidence

All work is retained beneath
`build/finish-first-party-20260922/continue-rankup-02/`.

`private-leaf-audit.json` records the ten retail call sites, source references,
matrix/UV layout evidence and seven fully relocated function proofs.
`proposal-final.json` binds the readable proposal and its final patch.
`installed/compile.json` binds a fresh compilation of the actual installed
source, not a synthetic single-function file. The final formatting control
and installed source produce the same whole native object.

`installed-closed/completed.json` records the completed official verification,
the exact C target, private provider and five preserved ASM reference proofs,
all other sibling byte comparisons, and unchanged allocated data. The initial
generic gate invocation correctly refused to treat the five protected ASM
siblings as C promotions; its successful compiler output was retained and used
by this dedicated closure instead of recompiling or weakening that gate.

Final installed source SHA-256:
`08d24bcccea0edcd3c235fd0243a81de38007f55b4e3294ad7cd011977798c3f`.
Actual native object SHA-256:
`de7b68a05a27d57dee9ce46d8e2a2615602f92ebbee713b5a43dc62626cec4d5`.

No vendor implementation, provider implementation in another owner, compiler
flags or runtime call semantics were changed. The retained remaining guarded
drafts still require their own object/behavior review before C promotion.
