# Screen fade/wipe effect: func_00355920 recovered, 2026-09-26

`src/promoted/code1_0035.c::func_00355920` is active C and **MATCH**
(2076-byte object in the 2080-byte window; the tail word is alignment).
code1_0035 went from 75 to 76 MATCH.

Relocations: `D_00887300` (render-state table), `func_00489f80`/`0048a000`,
`RpSkyRenderStateSet` (`func_003f6440`), `func_0045c870`, `cosf`/`sinf`,
`func_00356170`, `func_003561d0`, `func_0046d730` + `D_0064B310`, and gp floats
`fGpffff8504/8540/8548/84a4`, `iGpffff8544`.

## Starting point

The guarded draft was 2096/2080 bytes, 306 masked words (fnalign 213 edits).

## Levers, in order (masked words)

1. Switch-shaped dispatch where retail compares a value against several
   constants and branches to the bodies in listing order (compares come out in
   reverse listing order): the `*(s16 *)p` style selects in the mode-0 branch and
   the tile loop, and the flag-1 block written as
   `*(s16 *)(p + 0x10) += 1; if (*(s16 *)(p + 0x10) <= *(s16 *)(p + 0xC)) {...}`
   so the new counter comes from the store, not a local (no `nw`/`lim` locals).
2. The "past 3/4 (style 0) / 1/2 (style 1) of the duration" test is one static
   inline helper used at both sites; retail's shared `return 0` block (one
   `daddu $3, $0, $0` for every failing path) is its inlined return: 374.
3. Callee prototypes from their definitions and the caller's argument order:
   `func_003561d0` never reads its old fourth integer parameter and retail never
   sets `$a3` for it, so it is now `(Vec2f, s32, f32, f32, f32, s32)`;
   `func_00356170` takes its colour word before the third float,
   `(s64, f32, f32, s32, f32, s32, s32)`. Both definitions' bodies are unchanged
   (floats and integers use separate register files); the only other caller,
   code1_0014 `func_001424b0`, was reordered to match and stays MATCH.
4. The mode-0 ratio reads the start fraction through a local
   (`start = fGpffff8504`): retail forms `fcnt - start * flimit` as
   `adda.s/msub.s` instead of reusing the compare's product. Every spelling that
   used the global in both places (operand orders, `>`/`!>=` compares, inline
   conversions) stayed at 374: 0.
