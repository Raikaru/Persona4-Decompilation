# Battle menu callback: func_00207320

The current native MWCCPS2 b210 `-O2 -Iinclude` owner compiles this callback
to **2,008 bytes in the 2,016-byte retail window**, with **88 code relocations**
fully resolved and **eight zero alignment bytes**. Its complete 32-byte switch
table at `0x00747630` is independently relocated and compared with retail.
The official current-owner comparison reports **138 MATCH / 8 ASM**. All 145
other owner functions retain their bytes and canonical relocation meanings;
existing allocated data is unchanged. Only the proved target table is added.

The old 72-word residual was mostly a saved-register exchange. The real
`func_00203420` dispatcher supplies the callback through
`s32 (*)(u8 *, s32, u8 **)`, reading the work address as an integer from the
panel. Giving the target that actual signature, then initializing a local
`u8 *work` from the address, reduces the residual to three words. Changing
the count store at work offset `0x18C` to its unsigned halfword view places
the store before the separate signed-halfword argument conversion and closes
the remaining three words. The count starts at zero and is masked to 16 bits
after incrementing; the menu extent still receives `(s16)partyCount`.

Private declarations now agree with the existing direct providers and callback
definitions. The `001f0620` second argument is wide; `001f56d0` takes an integer
address and a halfword; `0023dfe0` accepts the actual unit pointer; `00117780`
returns the pointer stored in the work object; and `002019f0` consumes an
integer address. Those providers are unchanged. No argument, call or state
update was removed. The selected-panel publication, following draw callback,
and flag updates retain their order. The measured loop-invariant setting is
scoped with `push`/`pop`. No assembly, padding or dummy state was introduced.

The two decompilers, complete retail body, current dispatcher and providers
were inspected. An independent source review confirmed the callback contract,
initialized work view, count conversions and call ordering. This is a scoped
callback recovery, not a claim that every legacy callback type in the owner
has been reconciled.

Evidence is under `build/continue-first-party-20260922/menu207320/`. Controls
`baseline` and `canonical-providers` remain 72 words; the six explicit work-view
controls remain three; `store-u16-count` and `named-callback` reach zero.
`integrated/compile.json` records a fresh native compile of the actual current
owner. `integrated/official.json` scores that same native object through the
official verifier, with the reuse explicitly recorded in `completed.json`.
`func_00207320-proof.json` covers all relocated code and owned table bytes.
These completed compiler jobs must not be rerun merely to recover their output.

Integrated source SHA-256:
`d4fd543c252d850220a4d2bc56edab00df020d465be8b48641daf8ebdaa79121`.
Native object SHA-256:
`eb64bb6dedeebe0f403f3e68b48c1babb38eedff6f82efa0a3c441b488f9edd2`.
Resolved target SHA-256:
`c4ec546816f01697ace816a6253804832a902b2aa788175fc4e8c4e58d400c30`.
