/* object 752B / window 752B / normalized_diff unmeasured (prior attempts nd 151-157); differing offsets: unrecorded; classification: saved-register coalescing + t-register rotation floor; ruled out: full logic including 8-word copy loop and identity-matrix blocks matched, but retail keeps raw arg1 in $s5 and u16 mask t20 in $s4 live across func_004740c0 while b210 coalesces t20 into arg1's $s4 via self-mask, rotating every following temporary; no further probing per durable source note. */
