# Fcl combine state machine 002f6cf0

`func_002f6cf0` in `src/Event/Fcl/y_fclCombine.c` is now C and verifies as
MATCH: 12084 executable bytes in a 12096-byte window, 0 differing words, and
391 resolved relocations. The file now has 29 MATCH functions.

## Starting point

The guarded m2c draft measured 1842 fnalign edits. A body written from the
retail assembly measured 1147 edits at nearly the right size (3018 against
3020). The table at `jtbl_007491E0` gives the case bodies in this source order:

0x51, 0x52, 0x53, 0x54, 0x55, 0x56, 0x58, 0x57, 0x5F, 0x59, 0x5A, 0x5B, 0x5D,
0x5C, 0x5E

The 0x60 to 0x62 arms are empty but must exist, because they set the table
bound. The item switch in 0x5C tests its labels in reverse source order.

## Levers, in the order they paid off

- **`#pragma opt_lifetimes on`** (push/pop): 1147 -> 628 edits. This is the same
  lever as `002e90d0`.
- **Colour builders passed straight into `func_002ba5d0`**, which takes an
  `FclDrawColor` by value: 628 -> 130.
  - Written `color3 = func_002b2a60(...)` inside the argument list, the named
    local keeps its retail stack slot (130 -> 85).
  - Retail evaluates the `e[4]` argument before the colour call, which the
    separate statement got wrong.
- **Signed-byte reads with `(s8 *)`:** `func_002e4870(...)[i + 0x14]` and
  `[p[0x2FA] + 0x2E4]`.
- **An `s8` third parameter for `func_003144d0`.** Retail passes the `lb` value
  unconverted (-3).
- **`func_003139d0` declared `(s8, s8)`.** Retail sign-extends the loop index
  before loading `p[0x1A]`.
- **The slot step multiplier.** It is set by `switch (p[0x1A] + 1)` and copied
  into an int local (`stepInt = step`) before `func_002b6150(obj)`. This gives
  retail's sign-extension at the join (-14). The `&4` arm uses its own
  `step2`/`obj2`/`stepInt2`, declared in that order (-15).
- **Object ids `k + 0x297`/`0x28B`/`0x2A3`.** Computed into locals before their
  colour calls, they reproduce retail's placement and the `sq`/`lq` spill at
  `0xD0` (-16).
- **The 0x59 item id is an `s32` local.** A `u16` local picked up an `andi`
  under lifetimes (-35 together with the id locals).
- **`row = i` at the start of both reward branches in the first 0x53 loop**
  gives retail's `move $s5, $s7` (-4, the last edits).

## Consistent callee contracts (2026-09-26)

The 0x59 item id is a `u16` (it feeds `func_00105f50(u16)` and
`func_00310a10(u8 *, u16)` with plain moves). Against the old
`func_00313ae0(s64, s32)` definition that left one word: b210 masks a `u16`
local when it widens it to an `s32` parameter (`andi $a1, $s1, 0xffff`), and
retail passes it with a `move`. The consistent contract, verified on both
sides, is:

- `s8 func_00313ae0(s8 kind, u16 id)`. b210 does not assume a `u16`
  parameter is zero-extended either: compared inside the loop it is masked
  on every iteration. Retail's single `andi` on entry is a widened copy,
  `s32 id = arg1;`, taken after `i = 0` and before the table pointer. The
  loop then matches as a plain `for` with the file's `func_0031_ne` inline
  (`!func_0031_ne(entry, id)`); `entry == id` swaps the `bne` operands.
- `s32 func_00313a80(s8, s8)`. With the `s8` return above passed on to an
  `s64` parameter, the caller re-extends it (`dsll32/dsra32`, 280 words
  from the shifted layout). With `s8` parameters it passes `$v0` as is, and
  the definition's own `(s8)` casts become the parameter types.
- `s32 func_003139d0(s8, s8)`: the definition takes `s8` and drops its
  `(s8)` casts; still MATCH.

With those three definitions changed in `src/promoted/code1_0031.c` (all 16
of its MATCH functions kept) and the two local externs updated, the draft
measures **0 differing words**. It stays parked only because other local
externs in the draft still disagree with their definitions (H011:
`func_003147d0`, `func_002b2a30`, `func_002bb1c0`, `func_002b2aa0`,
`func_0011c610`, `func_0011d140`, `func_003144d0`); those wait for the
declaration cleanup. The change is banked as
`FclCombine_002f6cf0_contracts_20260926.patch`.
