# func_002f0f00 (y_fclCombine) — mechanical cleanup, not matched

Measured 2026-09-25. Production stays `INCLUDE_ASM`; best body is
`FclCombine_002f0f00_20260925_body.c`.

Retail: 24048 B = 6012 instrs, `jtbl_00749170`, states 0x38..0x50 (25 cases).

| measure | guarded draft | this body |
|---|---|---|
| object instrs | — | 6094 |
| fnalign edits | 3249 | 1751 |
| differing words (probe_archive.py) | 5070 | 5051 |
| per-case aligned edits | 2984 | 1461 |

## Semantic bugs found in the guarded draft

- `(f32)(s32)((f32)(s32)(...))` chains around float loads and around
  `func_002b2aa0`'s result: these truncate and emit extra `cvt` pairs.
- Hex float bit patterns passed as integers to `f32` parameters
  (`func_00275820(..., 0x422C0000)` converts 1.1e9, not 43.0f). Replaced with
  `43.0f`, `32.0f`, `60.0f`, `156.0f`, `255.0f`.
- `(s64)(x << 0x30) >> 0x30` on `s32` (undefined; folded away).
- `u8 sp3FC; u8 sp3FD; ...` byte stack slots for colours: now `FclDrawColor`.
- A `default: return;` label in the middle of the switch.

## Applied

Struct `FclCombineCtl` for the work block and its 11 re-read aliases;
18 `for (; (s32)((s32)(((s16)(v)))) < ...;)` loops normalised to `for` with
`s16` counters; `FclVec2f` title positions (11 pairs); implicit `s8` row
arguments plus `slotByte`/`partyCount` temps; `temp_f20` title `y` hoist.

Largest remaining cases (retail/object/edits): 0x40 1332/1368/483,
0x46 714/713/161, 0x3A 364/379/139, 0x3F 449/461/116, 0x3B/0x3D (+16/+15
instrs). Cases 0x39 and 0x3A are the same shapes as 002ed430 0x24/0x26 and
can be ported from `FclCombine_002ed430_20260925_body.c`.

## Second pass (2026-09-25, after func_002ed430 matched): 1461 -> 865

Body re-banked (object 6009 / retail 6012). Levers, each with the casealign
total after it:
- `func_00275820`'s real float-first signature (1440).
- 0x39/0x3A head ported from 002ed430 0x24/0x26 (1421). The 0x39 miss path
  is a `break`, not a fall-through into 0x3A. Loop counter `var_16` is s16.
- **`func_002b2aa0` was undeclared**, so it was an implicit `int` function.
  Declaring it `f32 (s32, f32, f32, f32, f32)` restores retail's inline
  float -> u8 conversion (1303).
- 0x3B/0x3D colour loops rewritten in the 0x26 form (1236).
- The 0x3A tail rewritten from the retail listing (1189):
  - `(f32)(u32)func_0010b5b0() / 12.0f`;
  - `FclVec2f` points;
  - `func_002b6c30(0x7E, pos, 156.0f, 0x56)`;
  - `f32` sprite scale stores;
  - `func_00331560` returns s32;
  - `temp_f22` declared before `temp_f21`.
- A row-draw normalizer (script lever, 866):
  - typed `FclRowList { pad[0x154]; u8 *list; }` for `temp_17 + i * 4`;
  - the discarded 2nd `func_0034ae50` result is the position source
    (`*(FclDrawPosition *)(posSrc + 0x28)`);
  - entry colours are `*(FclDrawColor *)(e + 0x75)`;
  - no `fclPacketColor`/`fclDrawPositionValue` wrappers around named stack
    points and colours.
- `x > 5` for the `func_00314660` range tests (857).
- `func_002b2d00`/`2cb0` declared `(s32, s32, s32, s32, s8)` and passed the
  raw `f11E`. Retail uses `lh`, and this fixes those rows. The total moves
  857 -> 865 through colouring noise elsewhere, but it is the honest
  contract.

Largest remaining cases:
- 0x40 (~322): the row-header `rowX` ordering, as in func_00304580 0x7B.
- 0x46 (151).
- 0x50 (94).
- 0x3F (66).
- The frame: 0x500 against retail 0x400. Named m2c stack locals inflate it.
