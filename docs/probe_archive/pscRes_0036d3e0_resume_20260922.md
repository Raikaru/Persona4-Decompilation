# pscRes func_0036d3e0: resumed source hypotheses, 2026-09-22

This is an unaccepted reconstruction. Native owner-context probes reduced the
fully resolved residual from 49 to 11 instruction words. The function still
requires its assembly fallback. `src/Main/Social/pscRes.c` and its existing
NON_MATCHING body were not changed. No shared headers or provider implementations
were changed.

## Measurement and provenance

Worktree: `build/first-party-publish-latest`, starting HEAD `5a1bcf7`, with the
pre-existing dirty work preserved. All new probe artifacts are under
`build/first-party-resume-20260922/social/` relative to that worktree.

The native compiler is MWCC PS2 3.0.1 b210 at
`D:/mwcps2-3.0.1b210-060308/mwccps2.exe`. The real pscRes context selects `-O2`
and the project include directory. `probe.py` reuses
`build/finish-current-20260921/encounter_probe.py` and
`build/campaign-checkpoint/gates.py`, compiles isolated copies through
`tools/probe_variants.py::_compile_in_context`, and retains complete sources,
objects, compile receipts, logs, resolved bytes and results in unique directories.

The original owner and its 29 siblings were frozen in `before.c`, `before.o`
and `inputs.json`. The final inventory is `review-summary-final.json`:
112 successful native candidates, zero exact matches, and one retained compiler
failure from a source-generator identifier error. That error was corrected in a
new destination. Completed probe destinations were not overwritten or rerun as
fresh compiler work.

Reference review covered the current owner and its callers, retail assembly,
`docs/ida_headstart/src/Main/Social/pscRes.c`,
`docs/ghidra_headstart/src/Main/Social/pscRes.c`, the old
`docs/probe_archive/SPSC_0036d3e0_body.c`, census evidence and existing matching
notes. `src/Kernel/sdkUttmx.c` was inspected read-only: its mapped TMX functions
00463930 and 00464100 have the same header and format conventions, but do not
provide an existing shared decoder suitable for this loader. Raster contracts
were checked read-only against `src/renderware/core/baraster.c`.

## Source changes that moved the residual

| Retained destination | Source hypothesis | Unmasked words | Emitted bytes |
| --- | --- | ---: | ---: |
| `baseline-current` | Existing retained body | 49 | 1144 |
| `depth-decode-helper` | Separate format-to-depth decoding | 26 | 1144 |
| `depth-decoder-local-phase` | The same value split in a local block, without a helper | 26 | 1144 |
| `depth-leading-loop-reversed-declarations` | Declare the first word-copy column counter before its row counter | 20 | 1144 |
| `local-depth-palette-descriptor` | Group the palette source and flags as a local view | 11 | 1144 |
| `reviewed-palette-view-final` | Readable names, typed palette colors and actual private raster declarations | 11 | 1144 |

The palette view is a reconstruction hypothesis for local state, not evidence
that a particular original structure existed. Its fields are real source values;
no unused filler field or artificial memory operation is present. The compiler
scalarizes the view and retains the retail 0x70-byte frame. Its color pointer is
read only after depth 4 or 8 initialized it and set the palette-present bit.

Typed header fields and word cursors, complete inline palette/pixel copy phases,
several useful counter-sharing lifetimes, pointer-versus-integer source addresses,
and distinct decoded metadata phases were measured. Their exact individual
results are in `review-summary-final.json`. Shared counter changes and wider
metadata views can change allocation considerably, but did not close the target.
Byte flags and a one-bit field added unwanted normalization or storage. Separate
pixel locals often removed a required entry copy; retaining propagation restored
the shape while changing register assignments. Do not repeat these families as
an undirected declaration or pragma sweep.

One separate candidate, `palette-cursor-initial-header-role`, gives the correct
registers throughout the body by using the descriptor's source to initialize the
header. It emits 1148 bytes with an extra entry copy to `$s2`. Its five aligned EE
edits include two trailing padding words; its actual unmasked score is 278 words
because the extra instruction shifts the body. This is not a closer byte match
and must not be promoted. The union spelling and targeted aggregate lifetime
settings retained that extra copy.

## Remaining mismatch and verification

The reviewed body emits 1144 bytes in the 1152-byte retail window. All eight
remaining retail bytes are zero padding. Pixel source `$s0`, depth `$s3`, raster
`$s4`, header `$s5`, and all loop counters agree with retail. Palette source and
flags remain exchanged between `$s1` and `$s2`, with the two first initialization
instructions also ordered differently. There are no other differing words.

| Offset | Address | Retail word | Candidate word |
| --- | --- | --- | --- |
| `0x020` | `0036d400` | `0000882d` | `0080802d` |
| `0x024` | `0036d404` | `0080802d` | `0000902d` |
| `0x118` | `0036d4f8` | `0200902d` | `0200882d` |
| `0x128` | `0036d508` | `36310001` | `36520001` |
| `0x134` | `0036d514` | `0200902d` | `0200882d` |
| `0x144` | `0036d524` | `36310001` | `36520001` |
| `0x1e8` | `0036d5c8` | `32220001` | `32420001` |
| `0x230` | `0036d610` | `8e430000` | `8e230000` |
| `0x238` | `0036d618` | `26520004` | `26310004` |
| `0x264` | `0036d644` | `8e430000` | `8e230000` |
| `0x26c` | `0036d64c` | `26520004` | `26310004` |

The final fixture is `reviewed-palette-view-final/pscRes.c`; its native object is
`reviewed-palette-view-final/owner.o`. `result.json` records all 29 unchanged
sibling bodies and canonical relocations. Whole-owner allocated data and its
relocations are unchanged.

`review.py` has an independent relocation pass. It resolves every emitted target
relocation from actual named external symbols, compares every instruction word
without masking, checks zero padding, and independently compares allocated data
manifests. It agrees with the first resolver on every byte. All 11 target
relocations are external; the target references no owned data section. Evidence:
`reviewed-palette-view-final/independent-comparison.json` and
`reviewed-palette-view-final/independent-resolved.bin`. The existing strict exact
proof rejects this object with `Unresolved instruction words differ`.

The official scoped verification command was:

```text
python -B -S -u tools/verify.py src/Main/Social/pscRes.c --json build/first-party-resume-20260922/social/owner-verification.json
```

It returned exit code zero: 29 MATCH, one ASM, all 30 first-party, zero Sony SDK
and zero other third-party functions. This verifies the unchanged production
owner, not acceptance of the 11-word candidate. There is no other guarded game
function in this owner.

| Identity | SHA-256 |
| --- | --- |
| Unchanged owner | `ec9da450c5755f434ed21d84a6123a2a9d127250d1bbcae802e5819d3ac93e55` |
| Native compiler | `286548490e2e902cfef21dcf39cd5af23766731585d90dea747f8781eadcafd7` |
| Reviewed full source | `0879b74924ec7e962f7ca38d27271516f164485b11d1f78f7147a8fdab4dfc49` |
| Reviewed object | `6cd4f29ff7652199f2c542ff320713faabc36ac337313000b89c8cc4b699a5f9` |
| Resolved candidate bytes | `1f152152d6b9dc201b495c3823de47f63d3569be092b813772093d613f813cf2` |
| Full retail window | `0f8209f2d996bf5f0fae35f035e232b0913fcb1270f5a5845fad9efb8a6192f5` |

Further experiments must use a distinct destination and real owner context.
The completed probe subcommands are historical measurements, not a pending work
queue. The remaining question is how the source gives the palette pointer and
flag their retail lifetimes and entry order without adding another move.

## Retained candidate and private declarations

The complete measured candidate body is included below so its source survives
outside ignored build artifacts. The full fixture contains the rest of the
unchanged owner. The private declarations were corrected in that fixture only;
the ignored return values and zero pixel-lock level compile identically in all
29 siblings. Do not replace the production guard with this body until a new
candidate passes exact unmasked relocation and data proof.

```c
typedef struct RwRaster RwRaster;
extern RwRaster *func_003ec590(s32 width, s32 height, s32 depth, s32 flags);
extern u8 *func_003ec3d0(RwRaster *raster, s32 lockMode);
extern RwRaster *func_003ec2e0(RwRaster *raster);
extern u8 *func_003ec6a0(RwRaster *raster, u8 level, s32 lockMode);
extern RwRaster *func_003ec2a0(RwRaster *raster);

typedef struct PscPaletteView {
    const u32 *colors;
    s32 flags;
} PscPaletteView;

s32 func_0036d3e0(u8 *source) {
    PscPaletteView paletteView;
    s32 depth;
    RwRaster *raster;
    u8 *header;
    u8 *paletteOut;
    u8 *pixelsOut;
    s32 rasterFlags;
    s32 paletteIndex8;
    s32 paletteIndex4;
    s32 column32;
    s32 row32;
    s32 row24;
    s32 column24;
    s32 row8;
    s32 column8;
    s32 row4;
    s32 column4;

    paletteView.flags = 0;
    header = source;
    {
        s32 decodedDepth;
        decodedDepth = 0;
        switch (source[0x16]) {
        case 0:
            decodedDepth = 0x20;
            break;
        case 1:
            decodedDepth = 0x18;
            break;
        case 2:
        case 10:
            decodedDepth = 0x10;
            break;
        case 19:
        case 27:
            decodedDepth = 8;
            break;
        case 20:
        case 36:
        case 44:
            decodedDepth = 4;
            break;
        }
        depth = decodedDepth;
    }
    if (depth == 0) {
        func_0046d730(D_0064E4E0, 0x409);
    }
    source += 0x40;
    switch (depth) {
    case 8:
        paletteView.colors = (const u32 *)source;
        source += (u32)header[0x10] << 10;
        paletteView.flags |= 1;
        break;
    case 4:
        paletteView.colors = (const u32 *)source;
        source += (u32)header[0x10] << 6;
        paletteView.flags |= 1;
        break;
    }
    rasterFlags = 0;
    rasterFlags |= 4;
    switch (depth) {
    case 0x20:
        rasterFlags |= 0x500;
        break;
    case 0x18:
        rasterFlags |= 0x600;
        break;
    case 8:
        rasterFlags |= 0x2500;
        break;
    case 4:
        rasterFlags |= 0x4500;
        break;
    default:
        func_0046d730(D_0064E4E0, 0x431);
        break;
    }
    raster = func_003ec590(*(u16 *)(header + 0x12), *(u16 *)(header + 0x14), depth, rasterFlags);
    if (paletteView.flags & 1) {
        paletteOut = func_003ec3d0(raster, 1);
        switch (depth) {
        case 8:
            for (paletteIndex8 = 0; paletteIndex8 < 0x100; paletteIndex8++) {
                *(u32 *)paletteOut = *paletteView.colors;
                paletteView.colors++;
                paletteOut += 4;
            }
            break;
        case 4:
            for (paletteIndex4 = 0; paletteIndex4 < 0x10; paletteIndex4++) {
                *(u32 *)paletteOut = *paletteView.colors;
                paletteView.colors++;
                paletteOut += 4;
            }
            break;
        }
        func_003ec2e0(raster);
    }
    pixelsOut = func_003ec6a0(raster, 0, 1);
    switch (depth) {
    case 0x20:
        for (row32 = 0; row32 < (s32)*(u16 *)(header + 0x14); row32++) {
            for (column32 = 0; column32 < (s32)*(u16 *)(header + 0x12); column32++) {
                *(s32 *)pixelsOut = *(s32 *)source;
                source += 4;
                pixelsOut += 4;
            }
        }
        break;
    case 0x18:
        for (row24 = 0; row24 < (s32)*(u16 *)(header + 0x14); row24++) {
            for (column24 = 0; column24 < (s32)*(u16 *)(header + 0x12) * 3; column24++) {
                *pixelsOut = *source;
                source += 1;
                pixelsOut += 1;
            }
        }
        break;
    case 8:
        for (row8 = 0; row8 < (s32)*(u16 *)(header + 0x14); row8++) {
            for (column8 = 0; column8 < (s32)*(u16 *)(header + 0x12); column8++) {
                *pixelsOut = *source;
                source += 1;
                pixelsOut += 1;
            }
        }
        break;
    case 4:
        for (row4 = 0; row4 < (s32)*(u16 *)(header + 0x14); row4++) {
            for (column4 = 0; column4 < (s32)*(u16 *)(header + 0x12) >> 3; column4++) {
                *(s32 *)pixelsOut = *(s32 *)source;
                source += 4;
                pixelsOut += 4;
            }
        }
        break;
    }
    func_003ec2a0(raster);
    return (s32)raster;
}
```
