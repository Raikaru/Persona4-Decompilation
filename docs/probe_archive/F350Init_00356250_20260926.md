# Fcl work initialiser: func_00356250 recovered, 2026-09-26

`src/promoted/code1_0035.c::func_00356250` is active C and **MATCH**
(1488/1488 bytes, frame 0x80, seven saved GPRs). code1_0035 went from 74 to
75 MATCH.

Relocations: `D_0064BDA0` (0x1C-byte float/byte records, hoisted base),
`D_0064CBE0` (sprite ids), `D_0064CC98` (assert file, lines 0x1E2..0x1FC),
`D_005E5830/5850/5810/57F0` (archive names), and the calls
`func_00107180`, `func_00248760`, `func_00107ac0`, `func_00107ea0`,
`func_00107c80`, `func_0046a770`, `func_0046d200`, `func_0046d730`,
`func_00359400`, `func_00246970`, `func_002bc010`, `func_00354a50`,
`func_0035adc0`, `func_0035bf10`.

## Starting point

The guarded draft (`#pragma opt_loop_invariants on`, s16 loop counters) was
1480/1488 bytes at 217 masked words; the rest was saved-register colouring.

## Levers, in order (masked words)

1. Callee contracts from their definitions: `func_00107180(s16)` (retail passes
   the counter register itself, not the loop test's re-extended copy),
   `func_0046a770` returns `u8 *`, `func_0046d200(u32, u32)`. The kind field is
   written and re-read through a `u16 *kind = p + 0x3C` (retail keeps
   `p + 0x3C` in `$s4`): 72, and the object reached 1492 bytes.
2. `func_002bc010`'s second parameter is the message pointer it hands to
   `func_002774d0(u8 *)`. nmCmdList.c declared it `u64`, which makes this caller
   sign-extend (`dsll32/dsra32` against retail's `move`). It is now `u8 *` there
   (nmCmdList stays 19/19 MATCH): 43 at 1488 bytes. `n > 0x15` gives the
   `slti $at` retail uses.
3. Declaration order drives the saved registers here (first declared gets the
   highest `$s`): `kind, id, n, m, p` for the table pass: 12.
4. The fourth handle is stored and tested as
   `*(u8 **)(arg0 + 0x1304) = h3 = func_0046a770(..)` (retail stores `$v0`),
   and the handles are declared `h0, h1, h3, h2` (24 permutations measured;
   only this one is 0).
