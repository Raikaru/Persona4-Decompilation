# Font raster drawing 00275d80

This callback builds a four-vertex glyph rectangle, obtains the appropriate
cached raster when its palette changes, updates the old raster's reference
count, and submits the textured vertices when opacity is nonzero.

MWCCPS2 b210 with configured `-O2 -Iinclude` emits **1,124 executable bytes in
the 1,136-byte retail window**. All **19 relocations** resolve exactly, and the
twelve remaining bytes are zero alignment. The complete owning object preserves
the other seven functions and allocated data. The coherent caller owner also
preserves every function's instructions and canonical relocations, plus all data.

## Source recovery

The former guarded body expanded unsigned-to-float conversion into manual
branches. Native unsigned casts recover those operations directly. The four
channel inputs are bytes: converting each extracted byte directly to `f32`
lets b210 place its mask, low bit and raw floating-register transfer together.
Inserting an intermediate unsigned-word cast moves those masks ahead of the
individual conversions and changes the register allocation. This was the final
31-word residual; direct byte-to-float conversion closes it without assembly.

The scoped loop-invariant pass retains the original hoisted conversion inputs;
propagation control preserves the three shifted packed-color values. Vertex
depth and reciprocal stores precede texture-coordinate addressing, and position
addressing follows color writes, matching the original evaluation phases.

The local storage consists of 256 bytes of vertex data and two complete
32-byte coordinate buffers. A union provides the texture buffer's word-copy and
float-coordinate views. The original byte-clear initializer uses a byte pointer
and unsigned count, including retail's null check. No pointer advances outside
these complete buffers, and no instructions or dummy objects are added as padding.

## Shared call contracts

The raster takes signed screen coordinates, depth before the glyph pointer,
a signed palette index, unsigned-word extents and packed color, and returns zero.
`frFont.c` captures the glyph's byte palette/extent values and passes them through
that shared mixed-ABI order. Its complete native object remains unchanged.

The raster's font-cache declarations now agree with the actual definitions:
`func_00271f50(u8 *, u32)` returns the replacement pointer, and
`func_00271310(u8 *)` returns the updated signed node count. The camera getter
returns an address word. The memory-clear declaration uses a pointer result and
unsigned size, consistent with retail's returned destination pointer and
unsigned length comparisons. These changes affect first-party declarations;
the runtime implementation is untouched.

## Evidence

The exact executable's resolved SHA-256 is
`d8dae576d1fc884119c0f2b872fb786c349267e3ad290c687adf108dc3632adf`.
Discovery, rejected source forms, complete-buffer review, provider checks,
caller proofs, compiler receipts and final installation/publication records are
retained under `build/finish-first-party-20260922/continue-25-font/`.
The original recovery attempt remains in `docs/probe_archive/FR_00275d80_body.c`.
