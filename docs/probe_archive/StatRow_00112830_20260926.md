# func_00112830 (code1_0011.c): stat row number/arrow draw, matched 2026-09-26

Window 2192 B (2180 B of code plus padding), frame 0x120, nine saved GPRs
plus f20-f22. Relocations: `D_005E4750` (the per-row colour table),
`func_00106c80`, `func_001068b0`, `func_001068e0`, `func_00106940`,
`func_00106970`, and `func_0046d4c0` (the glyph drawer).

The guarded draft was 494 words with a hand-built frame struct. A fresh
rewrite from the retail listing matched in four probes. The retail
function repeats one block pattern: copy the colour word into a new slot,
`ld`/`sd` the position into a new slot, then draw. That is what b210 emits
when it inlines a helper that takes `Vec2f` and a 4-byte colour struct
by value.

- **Two static inline helpers.** `func_00112830_number` prints a u16
  right to left: x starts at 30 or 22 past pos.x, depending on
  `value / 100`, and the glyph is `value % 10 + 0x1E`. `func_00112830_arrow`
  draws glyph 40 or 41 at `pos.y - 1` when the new value differs from
  the old one. Case 0 uses them with `func_001068b0`/`func_001068e0` and
  case 1 with `func_00106940`/`func_00106970`. Case 2 draws four glyph-60
  pips.
- **The number helper's parameter order is (pos, depth, value, colour,
  tex).** b210 binds inline parameters right to left, so retail's copy
  order is colour, then value, then pos. With `value` after colour, it
  was 12 words.
- **`value` is the loop variable itself.** `value /= 10` on the u16
  parameter gives retail's `divu` + `andi`. `value / 100` and `% 10` stay
  signed `div`.
- **Case 2 uses `0xFF - alpha`**, the u8 parameter. This keeps alpha in
  `$s0` across the kind call, and it is the value that was stored into
  colour byte 3.
- `#pragma opt_loop_invariants on` hoists g/b/alpha and the row y out of
  the digit loops. Without it the object is 2088 B.

Contract changes in this file: the definition's first parameter is now
`Vec2f`, and the only caller, `func_00112300`, declares it that way and
passes `xy.position`. That caller still matches. The file-scope
`func_001068b0`, `func_00106940` and `func_00106970` now return u16, as
their g_data.c definitions do (H011 11 -> 8). All 47 functions in
code1_0011 MATCH. `tools/build.py`: both SHA-1 OK, 594 linked.
