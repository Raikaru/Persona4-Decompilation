# Unmatched Fcl draw targets: 003147e0, 00323d00, 003218a0, 00320b80 (2026-09-25)

These four stay `INCLUDE_ASM`. The best draft bodies are archived beside this
note as `FclDraw_<addr>_body_20260925.c`. Scores are reloc-masked differing
words from the permute scorer (`permute.Target.score`, same as
`probe_variants`), measured on top of commit `e95deaf` (14ef0, 2b9d0 and 18840
matched, with the s8 mode contracts in place). The extra contract edits each
draft needs are listed per function. They were measured with all other
functions in the affected files unchanged, but they were not committed because
their functions do not match yet.

## func_003147e0: MATCH (was 16 words)

Closed by re-deriving which variables own the loop registers, not by
permuting names. Retail's loop counter sits in the register of the early
resource id (`$s0`), and the row resource sits in a web of the multiply
assigned `base` local, so the loop is
`for (id = 0, o2 = order + 4; id < 2; id++) { r = id + 0x174; base = r;
func_002b77d0(base, ..., r, ...); }`. Declaration order has no effect here
(60 random orders all tie). A fresh counter or resource local either rotates
the registers or is propagated away (562 words). Staging `o2 = order + 4` as
its own statement before the loop fixed `o2` into `order`'s `$s4` but moved
the init order (19 words). Dropping the colour locals that are assigned
inside the 77d0 arguments costs 8 words, and dropping `opt_loop_invariants`
costs 601. The history below is the pre-match state.

### Pre-match notes (16 words)

Needs: `include/fcl_combine_internal.h` prototype
`func_003147e0(u8 *, s8 slot, FclVec2 position, s16 resource, s16 argument, s8 mode)`.
Retail passes `argument` to the stack `sd` and `mode` to `$t1` without
conversion, and forwards both to `func_00314ef0` as-is.

The top half reuses the 14ef0 recipe (FclByte4 colours, `o2 = order + 2` after
the first 7750, `*(T *)(func_002b81f0(*slot) + off) = v` stores,
`(u8 **)(t + 0x258) + slot`).

- An `s16 base` spilled at 0xC0 plus an `s32 order = base` copy in `$s4`
  reproduces the spill and reload. Any extra named value live over the 0x160
  branch flips the allocation (about 370 words).
- The loop resource is `r = i + 0x174` (raw, saved) with `res = r`. Passing
  `(res, ..., r)` moves the `$a2` copy ahead of the depth constant.
- The 0x160 branch was fixed with the struct-return lever: points and
  colours are built inside the `func_002b77d0` arguments. That branch now
  matches.
- The remaining residual is the 0x174 loop only. `i`, `r`, `o4` and `res` take
  s3/s4/s5/s0 where retail uses s0/s3/s4/s5, i.e. retail reuses `res`'s top
  register for `i`. No effect from 300 declaration orders, block-scope loop
  variables (they are propagated away), `order += 4` (propagated into the
  loop), `opt_loop_invariants on` (hoists the floats but not `order + 4`), or
  the permuter (2000 iterations).

## func_00323d00: 32 words (was 29 against an incompatible provider)

Needs:
- `func_002b6c30(s16 resource, ...)` in the header and in the y_draw.c
  definition. The definition already narrows with `(s16)`, so y_draw stays at
  55/3.
- `func_003205f0(u8 *, s16, s16)` without its four block-scope `s64` externs;
  it stays MATCH.
- `func_003191c0`'s seventh parameter as `s8` (extern and guarded
  definition).

Retail passes the `s16` resource unconverted to `func_002b6c30`, and the
`s64` contract adds `dsll32/dsra32` at every call.

Recipe: `FclVec2 *p = (FclVec2 *)D_...` for the table points, integer `313`
passed to `f32` (runtime `cvt.s.w`), `(s16)(k + 0x2BB)` resource,
`*(FclDrawColor *)(func_002b6150(id) + 0x85) = c`, and `count` / `w` / `b`
loaded into variables in the first loop.

The only residual is a three-way GPR permutation. Retail has resource `$s1`,
shown `$s3` and hidden `$s4`; the draft has `$s4`, `$s1` and `$s3`. No effect
from 216 type/statement variants of shown and hidden, 200 declaration orders,
block-scope resources, struct-return points, `opt_loop_invariants`, or a
1500 s permuter run.

## func_003218a0: 171 words / 144 alignment edits (was 264)

Signature `(u8 *, s32)`. `fade = order + 3` is computed before the loop and
passed as `(s16)fade`. The row pointer `t + i*4`, `i*12` and `x + 0x7F` are
staged before the inner loop. `*(FclDrawColor *)(e + 0x75) = c` gives the
grouped byte copy. The final 83e0 is built from nested lookups.

The remaining diff is which values spill (retail spills fade, x+0x14E, i*12,
x+0x7F and three element pointers with `sq`, and keeps `(s16)i` in `$s0`).
Not yet tried: the struct-return `func_002b2970` lever, and passing arg1 to
`func_002b69f0` without conversion (a block-scope `s32` extern cost words).

## func_00320b80: 357 words / 141 alignment edits (was 338 / 178)

A fresh rewrite in the current style (FclByte4 colours, `FclPackedPosition`
points, `s8` mode local, `j + i` delay). Retail keeps `task` and `mode` in
`sw` stack slots, spills the row pointer `t + i*12` with `sq`, and
re-extends `i` for the `func_002b6a70`/`func_002b6af0`/`func_002b69f0` calls
but not for `func_002b83e0`. A draft with an `s32 i` and explicit casts is
worse (154 edits).
