# Battle encounter transition: func_00212270

Recovered the ordinary C implementation in `src/promoted/code1_0021.c`.
With the separately reviewed portrait recovery, the native b210 owner is
**107 MATCH / 1 ASM**. The target matches all **5,248 retail bytes** after
every relocation is resolved, with no trailing alignment bytes. All 108
owner functions pass full unmasked proof. The 107 other functions retain
their bytes and canonical relocations relative to the portrait proposal;
106 functions are unchanged relative to the production 105 MATCH / 3 ASM
owner at `693a76f`. All allocated owner data and its relocations
are unchanged, including the existing jump tables.

The source restores the missing complements in the initial exit fade,
entering arrows and label fade, the icon's `90 - 180 * eased` exit rotation,
and the first departing label's `170 + 28 * progress` Y coordinate. A raw
integer frame is converted once and remains distinct from its clamped ratio.
Each direction owns that ratio, while only its result survives the branch.
The complete inline exit-easing operation takes the quadratic coefficient
and fraction, returning `1 + coefficient * fraction - fraction * fraction`
with the eased expression evaluated before the baseline addition. It emits
no separate function. No CPU assembly, synthetic state or new optimization
pragmas are used.

The real frame is 0x3A0 bytes. Its objects are six 64-byte sky vertices at
0xB0, four UV pairs at 0x230, the 64-byte matrix at 0x250, four sky vertices
at 0x290, and the eight-byte Vec2f position at 0x398. These use the existing
shared vertex/matrix/position definitions. The untextured draw paths write
the screen coordinates, reciprocal depth and RGBA fields consumed by the
renderer; unused shared layout fields are not invented padding. All UV
pairs are assigned, and the replacement matrix rotation initializes the
transform before post-concatenation. Floating opacity remains live across
the two strip submissions and is independently converted through u8 for
each affected vertex. Sprite scale is converted to the providers' u16 type
at each call.

The actual dispatcher at 004623a0 calls a start callback with node+0x1C,
the signed address stored at node+0x10, and the callback identity. The new
declaration is `void(u8 *drawPayload, s32 workAddress, void *callback)`.
The payload and identity are unused, and the work address is explicitly
converted. Factory 002138a0 allocates and zeroes 0x48 bytes, installs this
callback at work+0x10 and its own address at work+0x18. Update 002136f0
appends work+8 using the actual sdkOt list provider and advances the real
animation state; its callers control the start/finish flags, and destructor
00213840 releases the file and work object. These five owner functions are
byte- and relocation-identical in the proposal.

The camera accessor 00457120 returns the existing s32 address global;
its result is explicitly converted before the camera-depth load. The
0034f460 and 0034f4a0 sprite declarations agree with their current providers,
including their floating positions, u8 color channels, u32 opacity and u16
scale fields. The matrix/UV quad provider 00366c70 consumes the complete
objects. The raster lookup 0046d5f0 takes a file pointer and tile index.
The rotation declaration follows RwMatrixRotate's matrix, axis, angle and
combine-mode contract. Primitive table calls use the existing shared enum,
sky-vertex pointer and vertex-count signature.

The two complete consumed axis vectors and three angle constants were read
from the validated retail ELF and hashed. The depth and callback-slot reads
also have exact resolved addresses; their saved ELF bytes document initial
contents without assuming their runtime values are constant. The four UV
pairs are recorded separately. All 34 target relocations
are resolved without masks, and the callback dispatcher and first-party
provider instruction windows are retained for review.

Receipts are under
`build/finish-first-party-20260922/resume-07-battle/ready-transition-v2/`.
`completed.json`, `all-function-proofs.json`, `func_00212270-proof.json`
and `abi-data-review.json` contain the native input identities, complete
owner proof, source/provider hashes, callback ABI, frame and data audit.
The official verifier reused the authenticated final object without a
second native compile. Earlier sealed versions remain in their own paths.

Final combined source SHA256: `bc7f38d3333b03d88b60edf0be93c6a2f9d15808ebb7966bf2fa20f544b745d9`.

Final object SHA256: `1aec5ef8bc40060beaeadcbf3a3376baa69d052fdb647ac774f2f1b8f3f7e05c`.

Resolved target SHA256: `76e88ba864e653d071e07327cc34fcaf16998f0ad8ad39c496a0ce59da8e76ab`.

`change.patch` applies both recoveries and both archive documents to the
unchanged production owner. `change-after-portrait.patch` applies this
recovery after the portrait source `42a2182711093d64162f40c80bc997e0220e7fbc02746c46b8b68f7c39103874`.
Both routes are applied in independent scratch copies and checked against
the same final source hash. The remaining owner ASM function is 0021be80.
