/* lane jof7, 002e6630, residual 121 words, object 636B/window 640B
   initial loads match; base still in $v1 (table in $a1) vs retail $a1 base.
   end improved but missing $v0 andi at tail. tried: base declared early,
   s32 va/vb with u16 cast.
*/
