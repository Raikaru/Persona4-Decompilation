# `func_00324680` second loop: register rotation (not matched, 15 words)

This round starts from `FclDraw_00324680_body.c`, which is still 15 words
reloc-masked on the current branch. It ends with no improvement after about
45 probes. `tools/permute.py` ran twice, with the default seed and with
seed 7, for 1714 and about 1700 compiles. It never got below its base
score.

## What the residual is

Registers in the second loop, retail against b210:

| Value          | Retail | b210 |
|----------------|--------|------|
| counter        | `$s0`  | `$s3` |
| `(s16)` copy   | `$s3`  | `$s0` |
| `a`            | `$s5`  | `$s5` |
| `on` / `off`   | `$s1` / `$s2` | `$s1` / `$s2` |

Changing declarations only rotates the named variables. The compiler's
sign-extension copy always takes `$s0`:

- `s16 k; s16 a;` gives counter `$s5`, `a` `$s3`, copy `$s0`.

The copy only leaves `$s0` when the loop counter is the parameter `arg1`
(whose home is `$s0`). With `for (arg1 = 0; (s16)arg1 < 2; arg1 = (s16)(arg1
+ 1)) { k = arg1; ... }` and an `s32 arg1`:

- the counter is `$s0`, as in retail;
- the copy `k` now outranks `on`/`off` (`$s1` against retail's `$s3`);
- the result is 22 words.

## Measured without effect (`fastprobe` score, base 18)

- `k` declared at function scope, in the `on`/`off` block, or in the loop
  body.
- `k = 0` hoisted before `func_0031ac10`, before the `(s8)arg2 == 0` block,
  before the `0x71` sprite, or into the `on`/`off` block: 139 to 580, far
  worse.
- A named copy `kk = k` or `k = j` in the body.
  - With an `s16` counter it is coalesced away.
  - With an `s32 j` counter and `(s16)` loop casts it scores 26, because the
    copy moves ahead of the `D_00644290` base load. It stays in `$s0`.
- Reusing `i` for the body value.
- `a` split into `a/b/c`, block-scoped per sprite, or `v` split per sprite.
- `k++`, `++k`, `k = k + 1`, and `(s16)` casts in the loop header.

## Idea not yet tried

The b210 colouring order seems to be: sign-extension temps first, then named
locals (later declaration takes the lower register), then the counter. Retail
behaves as if the counter were the temp. A loop whose counter is a genuine
compiler temporary might produce this. Examples are a `while` over a pointer
or a count derived from an expression, with `k` as a named local declared
after `on`/`off`.

## More probes (2026-09-26, still 15 words)

The archived body was installed on the landed FclVec2 contracts. The
baseline is 15 words.

| Variant | Words |
|---|---|
| Second loop counts in the first loop's `i` | 15 (same swap; b210 splits `i`'s webs) |
| Counter in `m` | 244 |
| Counter in `a`, with `k` as the body value | 30 |
| `do { } while (++k < 2)` | 241 |
| `k != 2` | 18 |
| `s8 k` | 16 |
| `u16 k` | 236 |
| `s32 k` | 244 |

The swap is still the counter (ours `$s3`) against its `(s16)` copy (ours
`$s0`). Even at 0 words the body would not install as is. Several of its
local prototypes disagree with their definitions (for example
`func_002b6c30(s16, ..)` against y_draw's `s64`, and
`func_00324680(u8 *, s64, s64)`), so those contracts need reconciling first.
