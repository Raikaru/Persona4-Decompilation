# Two-sprite fade callback: func_00266cc0 recovered, 2026-09-26

`src/promoted/code1_0026.c::func_00266cc0` is active C and **MATCH**
(2028-byte object in the 2032-byte window; the tail word is alignment).
code1_0026 went from 57 to 58 MATCH.

Relocations: `func_00452560`, `func_001060b0`, `func_00110d30`,
`sinf` (`func_0044b7b0`), `func_0025f2c0`, `func_0025f430`, gp float
`fGpffff84a4`.

## Starting point

The guarded m2c draft measured 1984/2032 bytes and 437 masked words (plain
`if`/`return` ladder, one set of function-scope temporaries).

## Levers, in order (masked words)

1. Rewritten from the retail control flow: the state test is a one-case
   `switch (*(s32 *)work) { case 2: ... }` (retail `beq 2 -> body; b -> end`),
   the time ladder is an `if/else if` chain, and `func_001060b0` is declared
   with its real `s16` return (the draft relied on an implicit `int`): 449 at
   2008 bytes.
2. The alpha conversions are `(u8)` casts (the per-path `andi` after each
   `cvt.w.s` branch), the float temporaries are declared per branch (retail
   keeps `sin` in `$f0` in the last branch but in `$f20` in the middle ones,
   i.e. separate variables), and the width from `func_0025f2c0` is its own
   statement before `1.0f - s`: 376.
3. Each alpha is stored in an `s32 alpha` before being passed to the `u8`
   parameter; that is the second `andi` retail has at the join: 60.
4. In the last branch the fade ratio `(t - 40) / 5.0f` goes through the branch's
   float temporary (retail computes it before loading 255.0f); a fresh local
   for it measures 60: 0.
