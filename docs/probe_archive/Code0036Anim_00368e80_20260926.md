# UV/rotation animation step: func_00368e80 recovered, 2026-09-26

`src/promoted/code1_0036.c::func_00368e80` is active C and **MATCH**
(1468-byte object in the 1472-byte window; the tail word is alignment).
code1_0036 went from 31 to 32 MATCH.

Relocations: gp floats `iGpffff8040`, `fGpffff8198`, `fGpffff838c` and
`D_007612D0` (the gp -0x7E20 angle scale), `sinf`/`cosf`
(`func_0044b7b0`/`func_0044b610`) and `func_0036ae90`.

## Starting point

The guarded draft was 1436/1472 bytes, 152 masked words (fnalign 56 edits):
the per-mode arithmetic was right, the tail was not.

## Levers, in order (masked words)

1. In both modes the second copy of the `0.5 + d` value is stored after the
   `0.5 - d` pair (`0x4F4, 0x518, 0x53C, 0x560`), as retail orders the stores: 138.
2. The two corner loops are `for` loops over an element pointer
   (`pa = &stack[i * 2]; pa[0] -= 0.5f; pa[1] -= 0.5f;`); the earlier
   `do/while` conversion was wrong for this function (retail branches to the
   bottom test first): 113.
3. `if (mode == 0)` and the `0x48` test are single/two-case `switch`es; retail's
   `beqz → case; b → end` (and `beq 1 → end` first) is switch dispatch, not an
   `if`: 128 (size now 1476, over by one word until the next lever).
4. `#pragma opt_loop_invariants on` (scoped push/pop): the 0.5f constant is
   hoisted above each corner loop: 7.
5. Rotation: `x` declared after `y` (register pairing), and the sines and
   cosines called inline:
   `nx = x * cosf(a) - y * sinf(a); ny = x * sinf(a) + y * cosf(a);`.
   Named `c = sinf(); s = cosf();` temporaries and every operand order of them
   tie at 3 words (b210 orders the second MAC by operand, not by source): 0.
