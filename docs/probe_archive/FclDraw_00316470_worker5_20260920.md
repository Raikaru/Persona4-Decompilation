# Combination-menu panel transition: 00316470

Recovered `func_00316470` in `src/Event/Fcl/y_fclCombineDraw.c` on 2026-09-20.
The actual configured MWCCPS2 b210 compiler at `-O2 -Iinclude` reproduces
**2572 code bytes in the 2576-byte retail window**, including all **93 applied
relocations** and four zero alignment bytes. The official owner verifier now
reports **43 MATCH / 27 ASM**, previously 42 MATCH / 28 ASM.

## Mechanism and behavior

Constructor outputs and their arguments now use ordinary `FclVec2` objects.
The color packets use the existing four-byte `FclByte4` aggregate, preserving
the retail byte loads and stores. The displacement value becomes live within
the first transition branch and is re-established at the later common panel;
both paths retain the value needed by subsequent rows.

The final difference was the pointer used for the five constant position
pairs. A `FclVec2 *` with `.x` and `.y` reads preserves the shared address on
its first constructor call, where the former `f32 *` spelling rematerialized
two absolute addresses. The existing external data declarations and addresses
are unchanged. The aggregate view contains exactly the two floats read by
retail. No new pragma, padding, volatile access, helper state, or assembly was
introduced.

Call order, resource identifiers, transition flags, colors, digit calculations,
and both branch directions remain those of the inspected retail body. The
actual `00104c70` provider is `s32 func_00104c70(s32)`; the existing local
declaration remains compatible. The position constructor and draw providers
retain their previously proved shared aggregate contracts.

The full retail assembly and both IDA and Ghidra exports were inspected. The
bounded measurements below use frozen complete-owner inputs, not a standalone
function context. Differing words are relocation-masked; alignment edits are
reported separately and are not acceptance criteria.

| Source representation | Differing words | Alignment edits | Emitted bytes |
| --- | ---: | ---: | ---: |
| Current guarded body | 514 | 83 | 2612 |
| Real position homes | 512 | 54 | 2604 |
| Real position and color values | 512 | 27 | 2604 |
| Branch displacement lifetimes | 532 | 22 | 2612 |
| Typed bounds pointer | 0 | 0 | 2572 |

## Caller closure

The provider retains its existing signature:

```c
void func_00316470(u8 *task, s64 layout, s64 transition);
```

All twenty actual retail call sites were inspected. The four external C
callers contain eighteen calls with constant layout/transition values; the
two owner-local callers contain one call each. The actual task argument is
retained at every site.

| Caller | Retail call addresses |
| --- | --- |
| `002ed430` | `002ED6D0`, `002EEADC`, `002EED9C`, `002EF614`, `002F02E4` |
| `002f0f00` | `002F1158`, `002F4364`, `002F4638`, `002F4E4C`, `002F6090` |
| `002f6cf0` | `002F6E44`, `002F7F5C` |
| `00304580` | `00304B64`, `003058B4`, `0030693C`, `00306BC8`, `00307254`, `00307650` |
| `00321e60` | `00323350` |
| `00324680` | `003246BC` |

Two existing local declarations used `s32` for the flags. The declaration in
the assigned `00324680` owner is now reconciled to `s64`. Its separately
enabled guarded witness grows from 2304 to 2312 bytes: the only added
instructions are `dsll32 $a2,$s1,0` and `dsra32 $a2,$a2,0` at offset `0x38`,
which convert the caller's `s32` argument to the provider's `s64`. Every other
instruction and every shifted relocation is identical. This caller remains
guarded; its draft is not another recovery.

The separately enabled `00321e60` witness remains identical at 5452 bytes.
Prime integrated the one-line external `002f0f00` declaration correction from
`delivery16470/combine.patch`. Its five
constant calls retain identical code and relocations in the separately
enabled 23428-byte witness. The complete default combine object, all 41
functions, and its allocated data are byte-identical before and after that
proposal. This worker did not write the external owner. A direct compilation
of prime's resulting source confirms the same complete object; its official
verification reports 26 MATCH / 15 ASM with no other statuses. Source and
dependency hashes match the proved inputs.

## Current-owner proof

The integrated draw owner was compiled directly against current headers.
The dependency hashes were checked before and after compilation. All 69
other active functions preserve instruction bytes and canonical relocation
destinations. All three allocated data sections preserve contents, alignment,
and relocations. The new function owns no additional data.

| Protected function | Exact emitted bytes / window | Applied relocations |
| --- | ---: | ---: |
| `003297f0` | 1604 / 1616 | 50 |
| `0032a960` | 1684 / 1696 | 53 |
| `0032b000` | 1904 / 1904 | 54 |
| `0032f4d0` | 1368 / 1376 | 22 |
| `003233d0` | 2352 / 2352 | 59 |

Each target was compared after applying every relocation, and every remaining
retail suffix is zero. Official verification reports 43 MATCH and 27 ASM,
with no other statuses. Scoped lint reports zero findings; the owner passes
`git diff --check`. No complete-image or full-project build claim is made.

```text
python -B -S build/first-party-finish-20260920/fcl-worker5/deliver16470.py current
python -B -S tools/verify.py src/Event/Fcl/y_fclCombineDraw.c --json build/first-party-finish-20260920/fcl-worker5/delivery16470/current/official-verify.json
python -B -S tools/decomp_lint.py src/Event/Fcl/y_fclCombineDraw.c
git diff --check -- src/Event/Fcl/y_fclCombineDraw.c
```

These commands completed successfully. The current compilation is retained
and immutable; `deliver16470.py current` intentionally rejects an existing
output rather than overwriting completed evidence.

## Identities and artifacts

```text
Draw before: a730f8e5fe12be8b3a8ca5e5173727e1ecab6ecfa288da56977fa01ca814b18e
Draw after:  e6e1fa81dc713b59595ce2a9d3403ba43f3521f73b60f6052e9d14d634dac06e
Current draw object:
a55b7043496707f844ccecc7b4a872b803eb43696eace3d1eaf8e3964d71b154
External combine before:
5fc86bb4923509d3cc2f183c02dfa9acf88c7a467d9ea3c034f8d0ffe9fc3bf8
External combine after prime integration:
0c81e2fff00fc6c1497cf5f440fc8febb969596f62c8570c86a00ad74d3c1cc0
```

All scratch artifacts are under
`build/first-party-finish-20260920/fcl-worker5/`.
`followon16470/` retains the frozen source/includes, every candidate, complete
translation unit, object, receipt, and alignment. Its `typed-bounds-pointer`
result includes the full relocation proof. `delivery16470/closure-proof.json`
contains the complete owner and separately enabled caller comparisons.
`delivery16470/current/proof.json` contains the actual integrated-owner proof;
`current/caller-conversion.json` isolates the two conversion instructions.
`delivery16470/draw.patch` is already applied in the assigned owner.
`delivery16470/combine.patch` was applied by prime. The subsequent
`delivery16470/external-current/proof.json` and `official-verify.json` record
the direct external-owner compilation and verification. Prime owns publication.
