# code1_0036.c `func_0036ae90`: MATCH (round 3, 2026-09-26)

Window 1504 B, object 1504 B, frame -0x140, under `opt_loop_invariants on`
(without the pragma: 87 words). Starts from
`Code0036Light_0036ae90_body_r2_20260926.c` (23 words, see
`Campaign_g3_unmatched_20260926.md`).

Relocations: `D_0064E490/4A0/4B0/4C0` as `RwV3d` struct copies (the `+8`
half resolves as addend 8), calls to `func_00457120`, `func_003e9700` (x2),
`func_003e0f80`, `func_0036abd0`, `func_003e4320` (x2), `func_003e42a0` (x3),
`RwMatrixRotate`, `RwV3dNormalize` (x4), `func_003e0f40`, and one pooled
float literal.

## What closed the last 23 words

The residual was a three-way saved-register rotation (`cam`, `arg1`/`dst`,
`inner`). Both loop locals had been block-scoped; the r2 note measured
declaration order only with that scoping.

- `inner` at function scope, declared first: `inner` reaches retail's `$s3`
  (23 -> 22). Declared last it is worse (29).
- `dst` at function scope too (`dst = arg0 + 4;` before the outer loop), and
  declared before `cam`: `cam` takes `$s1` and `arg1`/`dst` share `$s2`
  (22 -> 0). Any order with `inner` first and `dst` ahead of `cam` matches;
  `cam` ahead of `dst` stays at 22. The installed order is ints, then
  pointers, then floats.

## Cleanups made on the way in (all measured at 0)

- `fGpffff83fc` is the pooled literal `7.68f` (image value at gp-0x7C04);
  written as the literal, and the unused file-scope extern is removed.
- Block-scope prototypes follow the real contracts: `func_0036abd0` takes
  `(struct PanelMatrix *, const struct PanelTransform *)` as defined in
  `src/Main/Social/gc_model.c` (forward-declared tags); `cam` is a
  `struct RwMatrixTag *` (RwMatrixCreate/Destroy shape); `RwMatrixRotate`
  uses the RenderWare order `(matrix, axis, angle, combineOp)` (the float and
  int arguments take separate register files, so the swap from the draft's
  `(.., s32, f32)` is byte-identical); `func_003e42a0` and the file-scope
  `func_003e4320` share the `(RwV3d *, const RwV3d *, const struct RwMatrixTag *)`
  form; the draft's conflicting block-scope `func_003e4320` is gone.
- Frame test is `*(u8 **)(arg1 + 0x38) != NULL`.
