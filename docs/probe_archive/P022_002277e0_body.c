/* MATCH: production definition is in src/promoted/code1_0022.c.
 * Object size 1620; retail window 1632; normalized differing bytes 0;
 * code relocations 50; seven-entry jump table; 12 zero tail bytes.
 * The initial complete C reconstruction measured 1604 bytes / nd1129.
 * A u32 target ordinal with explicit u16 wrapping, opt_common_subs off,
 * a materialized subordinate predicate and a separate party ordinal
 * close the function. No register pinning or artificial padding.
 * Each local pose pair contains exactly two contiguous seven-float poses.
 * D_0060A0E0 is the twelve-byte vector {0.0f, 1.0f, 0.0f}.
 * The callback at 0x006352A4 receives the existing camera pointer from
 * func_001bc660; no additional public argument is invented.
 */
