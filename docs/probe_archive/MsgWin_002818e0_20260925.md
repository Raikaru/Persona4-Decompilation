# Message-window procedure 002818e0

`func_002818e0` in `src/promoted/itfMsgProcedure_Window.c` is now C and
verifies as MATCH: 602/602 instructions, 0 differing words, 2416-byte
window. The other 20 functions in the file keep their status (16 MATCH,
4 ASM). `func_002848c0` still matches after its free-entry helper moved up.

## Starting point

The guarded body was built under `opt_propagation off` plus `peephole off`
and measured 485 words (149 edits, plus 37 reloc-only). It carried the
frame counter in a local and dispatched case 7 through gotos.

## Levers

- **Written like its sibling `func_002848c0`, with no propagation or
  peephole pragmas.** Case 7 uses the shared `msgWinFindFreeEntry()`
  helper, which is now defined once, ahead of both procedures.
- **The frame counter is read straight from `D_00882044[0]` at every
  test.** Retail reloads it (`lh`) after each call. The increment is
  `D_00882044[0]++` followed by the `< 5` test, which reuses the stored
  value.
- **The float arguments are written in retail's evaluation order**, for
  example `200.0f * (1.0f - f) + 70.0f` and
  `9.0f - 60.0f * (1.0f - f)`. They come out as the `adda`/`madd`/`msub`
  chains with retail's constant materialisation.
- **`if ((p = f(arg0)) != NULL)` in cases 1, 5, 8 and 10**, so the test is
  on `$v0`. Cases 8 and 10 have block-scoped handles. Case 4 keeps
  `tex = f(); if (tex != NULL)`, because retail tests the saved copy there.
- **`D_00882044[0] > 3 && D_00882044[0] < 11`.** Spelled `>= 4`, the first
  compare lands in `$v0` instead of `$at`. This took the body from 5 edits
  to 0.
- **`#pragma opt_loop_invariants on`** (push/pop). It hoists the
  `D_008820B0` base out of the free-entry scan (34 -> 5 edits).
