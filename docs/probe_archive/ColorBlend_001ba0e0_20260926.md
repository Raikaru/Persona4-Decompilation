# Colour blend step `001ba0e0` recovered, 2026-09-26

`src/promoted/code1_001b.c` `func_001ba0e0`: MATCH, 1096 bytes of code in the
1104-byte window (the last two words are padding). The unit goes from 117 to
118 MATCH, and no other function in it changed status. This replaces the
guarded draft that had been 174 words (and 20 bytes short) under
`opt_common_subs off` + `opt_propagation off`.

The function is the per-frame step of a colour fade. It is installed by
`func_001ba530`, which writes the target colour (packed) at +0 and the frame
count at +8. The work is now typed:
`{ BlendColor target; BlendColor start; u32 frames; u32 frame; }`.
On frame 0 it snapshots the current colour from `func_00457130` into `start`.
While `frame < frames` it lerps each channel and calls `func_00457140`. On the
last frame it sets the target and returns 1.

Relocations: `func_00457130`, `func_00457140`, and the pooled float literal at
gp-0x7E0C.

## Levers

- The `fGpffff81f4` "global" at gp-0x7E0C is the pooled literal 1/255
  (0x3B808081). Writing `(1.0f / 255.0f)` instead of the global name removed
  both pragmas: with no pragmas, the 174-word draft dropped to 8 words and
  reached the exact size (the 20 missing bytes were the zero-register and nop
  placement around the second MADD). The group-2 round-2 lever applies here.
- The start snapshot is a single 4-byte struct copy
  (`work->start = *(BlendColor *)func_00457130()`). Retail loads bytes 0..3
  into a1/a0/v1/v0 and then stores them. Four byte locals give v0 to byte 0
  (8 words), and four direct byte copies give 7.
- The channel maths is a static inline helper, `blendChannel(start, target,
  t, inv)`, which computes `s = start/255`, `e = target/255`,
  `m = s*inv + e*t` and returns `(s32)(m*255 + 0.5) & 0xFF`. The four results
  must go into locals before the call: passing the helpers directly as
  arguments makes them evaluate right to left (12 words).
- The forward declaration at the top of the file uses the struct tag
  (`struct ColorBlendWork *`), so the one signature holds without moving the
  type.
