# Title ring screen `00126090` recovered, 2026-09-26

`src/promoted/code1_0012.c` `func_00126090`: MATCH, 1288 bytes of code in the
1296-byte window (the last two words are padding). The unit goes from 76 to 77
MATCH, and no other function in it changed status. `tools/build.py` still links
all 594 objects, including this unit, and both SHA-1 checks are OK.

Relocations: `D_005E5590`, `D_005E55A0`, and this function's own literal pool at
gp-0x7DD0..-0x7D64. The draft named those pool words `fGpffff8230`..`829c`.
The called functions are the remaining relocations.

## What the pool says

The 28 pool floats are four per ring: radius, x, y, scale. Every radius is
`64 * scale`, and x/y are the centre minus that radius, rounded as float32. The
centres are (35,152), (233,102), (377,93) and (626,110), and each is drawn
twice. The first pass has scales 2.4/1.8/1.4/1.1 and angle `360 * phase`. The
second pass has scales 2.2/1.45/1.0/1.55 and angle `-360 * phase`. The body now
states that directly: an inline `titleRing(x, y, color, scale, angle,
texture)` with `size = 64.0f * scale`.

- b210 folds the products and differences into the pool, but it still converts
  the radius at run time (`cvt.w.s`). That is why retail loads 153.6 and then
  converts it. The scale-1.0 ring folds to `lui` constants and `313.0f`
  (materialised as `addiu 0x139` + `cvt.s.w`), so no pool entry is needed.
- As externs, the scale pair was loaded first as a common subexpression
  (`lwc1 $f16` hoisted to the top of every call). As literals it is
  rematerialised in argument order, so the load follows `mov.s $f15`. This was
  the 16-word "f16 load-order" residual.
- The screen colour and the two rectangles are copied into scratch locals
  (`fillColor = black; rect = upper; rect = lower;`), and the scratch locals
  are declared ahead of their sources. That gives retail's frame: scratch
  0x7C/0x60 above black 0x78, shade 0x74, clear 0x70, upper 0x50 and lower
  0x40, with the argument address taken before the copy. The earlier pointer
  locals (`q7C`/`q74`/`q60`) got the address order right but stored through
  `$a0`. Inline by-value helpers got the scheduling right but put their
  parameter slots below the locals, which grew the frame to 0x90.
- No pragmas are used.
