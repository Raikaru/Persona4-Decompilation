# Battle strip renderer 00204dc0

`func_00204dc0` is recovered as 940 native executable bytes in the 944-byte
retail window. All 14 relocations resolve exactly; the final four bytes are
zero alignment. The whole owner retains the other 145 functions and all
allocated data. This is the last ASM function in `src/promoted/code1_0020.c`.

The routine builds four 64-byte immediate vertices for a rotated panel strip.
It disables texturing, computes reciprocal depth and the rotation, brackets
the first draw with the existing display helpers, and optionally draws a
transparent extension while temporarily enabling render state 8. The extension
updates XY and color while preserving the depth and reciprocal values in the
vertex buffer, including any changes made by the first draw callback.

The remaining code-generation differences came from the lifetime of a real
two-dimensional corner offset. Its X value follows the positive, negative,
then positive edge; its Y value follows the upper and lower strip endpoints.
Keeping this as a local point recreates retail's repeated positive-edge
calculations without caching away the floating-point operations. Staging
`y + offset.x * sine` before adding the vertical offset also preserves the
operand order of the two positive-edge Y additions. The scoped
`opt_propagation off` setting is retained around this routine.

The function's six-argument interface and its three existing owner callers
are unchanged. The initial render-state callback, camera lookup, trig calls,
display-helper calls and both draws retain their retail order. No assembly,
register binding, artificial side effects or patched executable bytes are used.

The independent worker proof is retained at
`build/finish-first-party-20260922/continue-55-strip/reviewed/proof/`.
Its resolved target SHA-256 is
`40891e468eb061b23855c781b0c383b4a89bccac1ccef5374d0b6edceb5f3882`.

The publication review under `continue-57-integration/reviewed/` removes the
obsolete nonmatching floor comment and reproduces the same proof. The native
decoder also establishes the required angle constant `fGpffff836c` at
`0x0076145C`. Its curated definition is included so the C owner remains
eligible for linkage. Installed verification, full-build hashes and actual
C-linkage evidence are recorded under `continue-57-integration/`.
