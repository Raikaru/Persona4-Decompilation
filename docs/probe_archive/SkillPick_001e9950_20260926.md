# AI skill pick 001e9950: structured draft, 97 edits

`func_001e9950` (1488-byte window, 372 instructions, frame 0x1A0) stays
INCLUDE_ASM. `SkillPick_001e9950_20260926_body.c` is a goto-free rewrite from
retail that scores 97 fnalign edits (367/371 instructions), against 130 for
the guarded goto draft in `code1_001e.c`.

Structure: loop over the unit's skills (`i == 0` -> `func_0023dfe0`, else
`table[i - 1]`), filter by `func_0023d6e0(kind) & 0x7E`, mode 1/2 against
`kind == 0`, the `func_0023df70` / `datCalcChkBadStatus(0x80008)` /
`func_0023ddc0` gates, then either single-best scoring (`found == 0`) or
accumulated scoring over the `func_001d7f10` target list, then the
`bestScore <= score` / tie-break on `func_0023d9b0` cost.

Levers measured (fnalign edits):

- rewrite with the guarded draft's callee types: 335 (u16 hp/maxHp convert
  through the unsigned path; retail uses `cvt.s.w` on s32 locals): 176;
- opt_loop_invariants on, `(u16)func_0023e130(...) + 1`: 171;
- callee parameter types that the callees themselves mask: skill as `u16`
  for `func_0023df70`, `func_0023ddc0`, `func_0023dd90`, `func_0023d9b0`
  (retail passes the u16 register unconverted) and `s16` for
  `func_00242800` (its definition narrows `(s16)arg1`): 166;
- `u16` returns for `datCalcGetHp` / `func_00231f80` (their real
  definitions), score/bestScore declaration order, `score += x; score +=
  1.0f;`: 114;
- declaration order (`work` last, `i` first): 97.

Blockers:

- retail sign-extends `kind` three times (the `kind == 0` test in `$v0`,
  then separately in each scoring branch's preheader, spilled with `sw` to
  0x120 / 0x130). MWCC here CSEs all three into one spilled value. Block
  scoped `element` copies, `(s16)` casts on an s32 `kind`, `!kind`, split
  `&&` tests, opt_common_subs / opt_propagation off did not separate them.
  The old draft's `s64 kind` with a `(kind << 48) >> 48` test is the only
  known separator and is not acceptable source.
- follow-on colouring: retail spills `skillId` (sw 0x110) and keeps
  `target` in `$fp`; this draft does the reverse.
- installing the draft also needs the u16/s16 parameter types above in
  datCalc.c's definitions (cross-owner contract change; each callee masks
  its own argument, so the change is evidence-backed).
