# Day-change outline renderer

Owner: `src/cldDayChange.c`, `func_00266050`.
Compiler: MWCCPS2 b210, `-O2 -Iinclude`.

The recovered function emits all 1,600 bytes in its retail window, with 41
fully resolved code relocations and no alignment tail. Its eleven local
two-dimensional point-array initializers contain 758 points, or 6,064 bytes.
Every point and each array's placement and alignment match retail. The
thirteen-entry switch table, including all thirteen relocated destinations,
also matches. All nine sibling functions and all existing allocated data are
preserved; their data relocations are compared independent of record order.

The main source error was treating compiler-generated array initialization as
eleven hand-written copying loops. The local arrays contain ordinary floating
point drawing coordinates. Writing them as local initialized point arrays
reproduces the original pair of word loads, decrement, stores and pointer
increments in each copy loop. This also restores the original temporary and
saved-register lifetimes.

The renderer and its drawing helper take their depth argument third in source
order. This order is applied coherently to declarations, definitions and all
calls. The ABI retains separate integer and floating-point argument registers;
the actual native helper and its caller remain unchanged. The final two
instruction differences disappeared when the renderer's own definition and
caller used this same observed ordering.

The month-12 branch uses the second, 61-point outline for its second draw,
with a horizontal scale of approximately 0.82. The old guarded draft used the
first, 40-point outline with a 61-point count and substituted a scale of 1.0.
The corrected source restores the actual array and GP-relative scale load.
The scale's alias `iGpffff8570` resolves to `0x00761660`: the retail instruction
at `0x00266618` loads from GP `0x007690F0 - 0x7A90`. Both symbol maps record
that independently verified address so the owner remains eligible for C
linkage.

The shared point type describes the actual coordinates used by the helper.
The final black shade uses a complete sixteen-byte rectangle union with
signed-coordinate and float-transport views, matching the primitive provider's
existing interface. No additional drawing effects or arithmetic were added.

The native source, experiment receipts, full unmasked proofs, linker preflight
and installation records are preserved under
`build/finish-first-party-20260922/continue-35-daychange/`.
The preflight proves all ten functions and every one of the owner's fifteen
allocated sections, and invokes the real build eligibility planner with the
single proven scale alias. Production linker behavior is unchanged.

The final installed-source checks and publication build are grouped with the
script and primitive recoveries under
`build/finish-first-party-20260922/continue-36-batch/`.
