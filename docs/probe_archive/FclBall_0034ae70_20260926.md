# func_0034ae70 (y_fclCmbBall.c): fusion ball quad renderer, matched 2026-09-26

Window 1296 B (1284 B of code plus padding), frame 0x60. Relocations:
`func_00457120`, `func_003315e0`, `D_00887300` (the device's
RenderStateSet slot, hoisted into `$s0`), `RpSkyRenderStateSet`,
`func_002b2a60` x3, and the near screen Z at `D_008872F8` (retail's
`lui $a0` above the vertex loop, `%lo` folded into the in-loop `lwc1`).

Started from the round-3 banked body (`FclBall_0034ae70_20260926_body.c`,
3 words). The unit changes it needed are now installed:

- `#define FCL_COLOR_ARG u8` before `fcl_color.h`. Retail loads the
  `0xE6E` alpha byte in argument-slot order and masks the `>> 1` alpha
  with `andi 0xFF`. This is the same per-unit colour contract that
  y_fclShopDraw.c, y_fclCombine.c and code1_0033.c use.
- `func_00457120` and `func_003315e0` are declared `s32 (void)`, which
  agrees with their definitions in code1_0045.c and code1_0033.c. This
  removes the unit's two H011 warnings.

The last 3 words were the load order in the vertex loop (`nearZ` before the
`0xE14` float). Retail order came from reading the near Z through a cast of
the RenderWare globals block, the way `RWSRCGLOBAL(dOpenDevice).zBufferNear`
expands:

    extern u8 D_008872E0[];
    ... ((RenderDepth *)D_008872E0)->nearZ - *(f32 *)(arg1 + 0xE14)

A typed struct global (`extern RenderDepth D_008872E0`) loads nearZ last
(3 words), and so do a struct view of `arg1` for 0xE14 and all of the
round-3 variants. The cast form also keeps the round-3 behaviour: the load
is treated as aliased, so `opt_loop_invariants on` hoists only the `%hi`.

Both pragmas are required: without `opt_loop_invariants on` it is 64 words,
and without `opt_propagation off` it is 270. All six y_fclCmbBall functions
MATCH. `tools/build.py`: both SHA-1 OK, 594 C objects linked.
