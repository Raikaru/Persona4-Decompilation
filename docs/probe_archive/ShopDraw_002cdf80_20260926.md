# Shop buy list draw + input: func_002cdf80 at 2 edits, 2026-09-26

`src/Event/Fcl/y_fclShopDraw.c::func_002cdf80` is still `INCLUDE_ASM`. The
best body is `ShopDraw_002cdf80_body_20260926.c`:

    python3 tools/fnalign.py src/Event/Fcl/y_fclShopDraw.c func_002cdf80 \
        --candidate docs/probe_archive/ShopDraw_002cdf80_body_20260926.c --quiet
    retail 3459 instrs, object 3459 instrs, 2 edits (plus 88 reloc-only)

The previous draft (`ShopDraw_002cdf80_body_20260925.c`) measured 140 edits,
and 135 once it was adapted to the current prototypes.

The body needs one source change: the `shopDrawLabel` static inline must be
defined above `func_002cdf80`. It is currently defined further down, next to
its other callers. Moving the block up leaves those callers matching.

## Levers, in the order they were applied (fnalign edits)

- **135 to 82: declare `top` late.** Put `top` (the `func_002e2700` result)
  after `flags`, or last. Retail's `$s2` (top) / `$s3` (display temporaries)
  colouring then follows. The 20260925 note tried the four orders of
  `work/item/top/i`, but never moved `top` below the other locals. A later
  declaration gets the lower saved register.
- **82 to 76: one handle local per font block.** `r0/r1/r2` for the three
  `func_0046d200` handles, instead of one shared `r`. This is the same lever as
  func_002da0a0.
- **76 to 64: nested temporaries and `shopDrawLabel`.**
  - The item name goes through `shopDrawLabel(115.0f, name, func_002b2970(..),
    textColor)`. The inline's `Vec2f pos` parameter copy is the 0xD8 slot
    below all the temporaries. A named `pos`, whether function-level,
    block-level, an array or scalars, lands above the temporaries or in
    `$f20/$f21`.
  - The positions from `sp198` onward are nested as call temporaries, and so
    are the header-loop pair. The three row positions `sp1E0/1D8/1D0` stay
    named locals, and so do `sp1C8/1C0/1B8` (nesting those costs 48). All
    colours stay named.
  - Nesting the 0x3A-loop positions puts the `&obj[i + 0x3A]` recompute
    before the two `func_002b2970` calls, as retail does.
- **64 to 12: the dead counter survives when its variable is a loop counter
  elsewhere.** `for (i3 = 0x43, j = 0; i3 < 0x49; i3++, j++)` with `j` used
  nowhere else is removed by b210, as the old note says. If `j` is also the
  counter of another loop (any loop, even one in an unrelated branch), or `j`
  is read after the loop, the increments are kept.
  - `i3` is function-level, shared by the 0x43 and 0x3A loops.
  - `j` is declared after `i3`; declared before it, the two swap `$s2/$s3`.
- **12 to 6:**
  - `u32 fC20` (it is passed to `func_002cacd0`'s u32 parameter), which fixes
    the load order;
  - `func_002d4760(arg0, (s32)work->sel)`, which fixes the argument order
    around the `(s8)` extension, the same lever as func_002be530.
- **6 to 5: a `return;` after the inner `if` of the repeat-down handler**
  (`D_008C027A & 0x4000`). Retail's `blez` goes to a `b` to the epilogue
  instead of being threaded to the epilogue. The explicit `return` reproduces
  that.
- **5 to 2: the 0x43 loop tests `j < 6`.** In the 12-edit shape `j` was also
  the 0x82 loop's counter. Var-level allocation then put that loop in `$s2`,
  where retail has `$s1`. With the 0x82 loop on its own block counter and the
  test on `j`, every register matches.

## Residual (2 edits)

The 0x43 loop test is `slti j, 6` where retail has `slti i, 0x49`. Retail
tests `i` and still keeps `j`'s increments, with no read of `$s2` before the
next definition. Nothing tried reproduces both at once:

- **`j` shared with another loop.** The 0x82, 0x8C, 0..3, final 0..3,
  prologue, slide and 0x3A loops were each tried. b210 gives a variable one
  register, so that loop's counter moves to `$s2` (or the dead counter to
  `$s3`). Retail has no loop counter in `$s2`.
- **Reads of `j` that the compiler can remove.** Neither `j = work->f1170`,
  the font-value temporaries, `top`, nor a dead `x = j` keeps the
  increments.
- **`j` as an argument after the loop.** Passing `j` for the `6` of the next
  two `func_002e0660` calls keeps it, but b210 passes `move $t0, $s2` rather
  than folding to 6 (2 edits).
- **Rewriting the test or the body:**
  - body indexed by `j + 0x43`;
  - `j != 6`, which b210 turns into `slti 6`;
  - compound tests (`&&`, `i < j + 0x49`);
  - swapped increment order;
  - `opt_strength_reduction` on/off/strict and `opt_unroll_loops`.
- **Pragmas.** `opt_dead_assignments off` keeps `j` but changes other code
  (13 edits). `opt_loop_invariants on` (the draft's push/pop) is required:
  without it the body is 1311+ edits.

## Declarations to reconcile before installing

The body still carries block-scope prototypes that disagree with the
definitions elsewhere in the tree (per the one-signature rule):

- `func_002e05a0` (s8 here, s32 in code1_002e.c);
- `func_002e3560` (s32 x4 here, `(u8 *, s32, s32, s8, s8)` in y_list.c);
- `func_002b3050` (`s16, s16, s32, s32, s32, ...` here, `s32, s32, s32, s64,
  ...` in code1_002b.c).

Rewrite these against the cleaned file-scope declarations once the
declaration cleanup lands, then re-measure.

## Declarations reconciled (second pass, 2026-09-26)

The three prototypes now agree with their definitions, and the body file
carries them. The draft still measures 2 words, with the same `slti`
residual and nothing else.

- `func_002e05a0` returns `s8` (code1_002e). The value is always 0 or 1, and
  the code is the same. y_fclShopDraw's file-scope `s8` view now agrees
  (H011 -2).
- `func_002e3560(u8 *, s32, s32, s8, s32)`. The y_list definition is
  NON_MATCHING and read `s8 arg4`, but code1_002e's matched caller
  `func_002e2410` needs `s32` there: with `s8`, the `lb $t0` moves ahead of
  the other argument loads (10 words). The callee narrows it at use
  (`dsll32 24` late in the body). The definition now says `s32 arg4`.
- `func_002b3050` is declared as its definition, `(s32, s32, s32, s64, s32,
  s16 *, s16 *)`. The s16 cursor/scroll arguments are already sign-extended,
  and the constant 5 is the same `li`.

### More residual probes (0x43 loop, 2 words baseline)

Each probe tests `i3 < 0x49` with a dead second counter, except the
LFTR-shaped ones.

| Shape | Words |
|---|---|
| `for (j = 0; j < 6; j++) { i3 = j + 0x43; .. }` (LFTR shape) | 785 |
| `obj[j + 0x43]` directly | 769 |
| `do { } while (j < 6)` | 767 |
| `for (j..., i3++)` orders | 8 to 10 |
| dead counter `top` | 768 (increments dropped) |
| dead counter `row` | 81 |
| dead counter `cur` | 67 |
| dead counter `i`, the first loop's counter | 22 |
| live `i`, dead `i3` | 28 |
| `j = i3` copy at the top of the 0x3A loop | increments dropped |

With `i` as the dead counter the increments are kept, but `i` then takes
one register across the function. Retail has the first-loop counter in
`$s3` and the dead counter in `$s2`. In retail, `$s2` otherwise holds only
`top` and compiler temporaries. No natural shape found; production stays
ASM.
