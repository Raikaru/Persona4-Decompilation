# First-party continuation recovered on September 20

The interrupted matching work was recovered from branch
`work/first-party-0360227` at `6d950a1`, with its dirty owner changes preserved.
The earlier worktrees and all saved candidate/compile receipts remain intact.
Two sealed worker deliveries were then integrated into the actual source:
the camp skill-menu state machine and the action-state initializer. The camera
contract audit and its three previously missed grouped alias callers were
integrated alongside them.

## Accepted C functions

These 22 first-party functions use assembly in published `0360227` and now
compile as ordinary C. Each row was independently verified from the combined
current owner after applying every relocation. The suffix, where present, is
retail zero alignment rather than omitted executable code.

| Function | Executable / window bytes | Code relocations |
| --- | ---: | ---: |
| `001356d0` | 1332 / 1344 | 42 |
| `0013c700` | 852 / 864 | 22 |
| `0013ea20` | 2724 / 2736 | 94 |
| `0013f720` | 1060 / 1072 | 30 |
| `0013fb50` | 1060 / 1072 | 42 |
| `001441e0` | 2268 / 2272 | 109 |
| `0015fb00` | 1052 / 1056 | 18 |
| `001a0b00` | 1088 / 1088 | 26 |
| `001a4800` | 1148 / 1152 | 39 |
| `001be050` | 2164 / 2176 | 49 |
| `001be990` | 2564 / 2576 | 73 |
| `001bf5e0` | 1416 / 1424 | 29 |
| `001bfc00` | 2256 / 2256 | 53 |
| `001c09b0` | 1156 / 1168 | 37 |
| `001c6f40` | 1180 / 1184 | 26 |
| `001cde50` | 1336 / 1344 | 34 |
| `002411a0` | 2588 / 2592 | 70 |
| `0028fc40` | 2088 / 2096 | 16 |
| `002e4090` | 1360 / 1360 | 33 |
| `002e8410` | 3256 / 3264 | 148 |
| `0033e5c0` | 500 / 512 | 14 |
| `00364fb0` | 2072 / 2080 | 11 |

The proofs include all referenced owned data, including the skill menu's
56-byte, fourteen-entry switch table; the action setup's table; the list's
two owned sections; and the general camera's four-byte literal. Literal and
table relocations are applied before comparing their complete contents.

## Recovered contracts and preservation

The camera paths use complete position/quaternion frames, true vector and
quaternion interfaces, and initialized interpolation state. Nine required
float aliases are recorded from retail GP-relative references in both symbol
inputs. The four camera-contract changes preserve the entire `code1_001b`
object. The three grouped default-distance wrappers retain their 48-byte
retail bodies under the corrected `FUN_001be050` declaration.

The camp recoveries preserve original item and selection lifetimes across
callbacks, explicit provider results, real query arities, and the message
pointer's initialization point. The skill-menu delivery preserves all 146
other functions and twelve existing data sections in its four owners; only
the independently proved switch table is added. The message-provider repair
also retains all callers' actual pointer values and result behavior.

The action initializer owns the complete 0x20-byte P4 result payload. Its
three packet constructors expose the real result pointer, unsigned flags,
and allocated return value. The combined five-owner proof preserves all 453
non-target functions and sixteen allocated data sections. All eight directly
affected guarded callers compile; the two large old drafts required explicit
pointer-to-address-carrier conversions. Their earlier failed attempts remain
in the evidence history.

The card-effect diagnostic format uses the actual six-byte `D_00763A28`
array. This retains the completed repair of the earlier 171-versus-172 C-link
regression; the source does not rely on an unplaced scalar alias or lower the
link floor. The later completed `recovered-camera-pair` build already proved
that repair with both retail hashes matching. Its receipt describes that
earlier source fingerprint, not the subsequent ten-function integration.

## Current verification evidence

`build/first-party-resume-persist/assembled-proof/official.json` records the
unmodified official verifier over every changed owner and every newly
recovered target owner: **1389 functions, 1252 MATCH and 137 ASM**, with no
unexpected status or wrong relocation target. Compiler output is retained by
a wrapper that does not alter source, arguments, or compiler results.

Independent code/data proof uses those actual compiled objects and the current
build/verifier modules. Its `resolved/` directory records every row above and
the repaired existing providers. `inputs.json` binds the files and configured
profiles; `proof.json` records the target and owned-data checks. The full report
combines these fresh rows with unchanged inputs from the completed whole-tree
verification in `build/first-party-resume-now/recovered-camera-pair/`.

The resulting full report contains **8033 MATCH / 4687 ASM** over 12,720
functions. Its first-party subset is **6398 MATCH / 462 ASM** over 6860. This
is an explicit mixture of fresh owner checks and a sealed prior full run,
not a claim that every unchanged compiler job was repeated. The source-only
census additionally found `0052d9a0` in an excluded GCC owner; it is excluded
from these official counts consistently with the existing verifier.

Component records are retained in the named camera and camp archive notes,
`Action_state_001a0b00_20260920.md`, and these local evidence directories:

* `build/first-party-resume-now/camera-other/four-contracts/`
* `build/first-party-resume-now/donor-worker/live-integration-20260920/`
* `build/first-party-resume-persist/camp/`
* `build/first-party-resume-persist/side-camera/close-alias/`

Further side-camera, battle-status, window, and sprite candidates remain
unfinished. Their low scores are not included among the 22 exact recoveries.
