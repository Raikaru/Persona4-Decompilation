# Cursor rows: `func_00321e60` from 148 to 24 words

`func_00321e60` in `src/Event/Fcl/y_fclCombineDraw.c` is not matched. The
new draft is in `FclDraw_00321e60_body.c`. It scores 24 differing words
(reloc-masked, `probe_variants`). The previous body scored 148 words (150
fnalign edits), and its levers are in that file's header at commit `12e8b9a`.
The frame and stack slots are still exact. The residual is saved-register
colouring in the `0x40`/else arm only.

## Levers, in the order they closed the diff

1. **One set of loop locals per arm (148 -> 59).** The arms are `0x80`,
   `0x40` with `(s8)arg1 == 0`, `0x40` else, and `0x20`. Each arm gets its own
   `j/k/row/p/h/x/acc` (`ja`, `kb1`, `rowc`, ...). Retail gives `k` a
   different register in each arm, so one shared `k` cannot match. This is
   the same lever as `func_0031fa20` and `func_0032e570`.
2. **Keep `m` shared, never private to the `0x20` arm (59 -> 50).** If the
   `0x20` arm has a single-assignment `m`, b210 propagates `j * 23` past the
   `func_0034ae50` call, and the function loses a word. The `0x20` arm must
   share `m` with at least one other arm (tested: B2, B1 or A; all give 50).
   `m` is split only for arms A and B1.
3. **Arm A needs its own `i`, and `ka`/`ha` declared before `ia`/`ja`
   (50 -> 28).** Retail has `k`/`h` in `$s4` and `i`/`j` in `$s3`.
4. **Pass the `0x20` arm's `func_0034ae50` result inline (28 -> 24).**
   Write `func_002b83e0(func_0034ae50(..), ..)` instead of going through a
   named `pc`. This swaps `m`/`p` to retail's `$s0`/`$s6`. Declaration order
   had no effect on that pair.

## Residual (24 words, `0x40` else arm only)

| Value | retail | b210 |
| --- | --- | --- |
| row counter `j` | `$s5` (arg1's register, dead in this arm) | `$s0` |
| `(s16)j` loop-condition temp | `$s0` | `$s7` |
| `(s16)k` condition/index temp, then `alpha` | `$s6` | `$s5` |
| second `func_0034ae50` result | `$s7` | `$s6` |

Measured without effect on this arm:

- A single-move declaration scan (99 candidates) moving `jb2`, `kb2`,
  `rowb2`, `alpha`, `c21C`, `m`, `c` and `x`.
- Block-scope versus function-scope `cur` in four positions.
- Placement of `jb2 = 0` / `kb2 = 0`: every other placement was worse (30-41).
- Reusing another arm's `j` or `k` for this arm: 24 at best, 34-94 otherwise.
- `++jb2`, which costs 213 words.

## Before installing

The hard signature rule (2026-09-26) applies. The body's local externs must
agree with the real definitions: `func_003191c0` (s32 value parameters here),
`func_002b83e0` (FclVec2 position) and `func_0031e5b0`/`func_0031fa20`
(FclVec2 anchor). Reconcile them first, and expect some of the byte-shaping
to move.
