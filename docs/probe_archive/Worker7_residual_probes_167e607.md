# Additional worker-7 residual probes on upstream167e607

Date: 2026-09-20. These attempts preceded the successful ordinary-C recovery of
`00364fb0`, recorded separately in `ShdMisc_00364fb0_167e607.md`.

## Camera shake: 004a7830

The current accepted expression in `src/promoted/code1_004a.c` remains the
best body. A fresh configured-owner compilation confirms seven differing
unrelocated words, with 532 emitted bytes in the 544-byte retail window and
a zero suffix. No production source was changed.

Fifteen additional structural candidates were compiled in
`build/first-party-resume-167e607/fresh/worker7/shake/`. Three inline sign-update
store boundaries tie the seven-word floor. Two helpers returning the new value
regress to 80 words. Four real product/half-step live-range splits regress to
95 words and a shorter body. Four changes to the roles of current and magnitude
locals tie at seven. Two inline magnitude predicates regress to 90 words and
an oversized body. These are new structural tests, not another broad
permutation search.

The remaining words encode the float-register cycle among zero, current value
and absolute magnitude. The retail/IDA/Ghidra paths and actual RNG, camera,
translation and direct effect caller were read. No contract, flag or effect
was fabricated to make a candidate match. All compilations finished before
the owner was released.

Scripts: `shake_probe.py` and `shake_liveranges.py` in the worker's scratch
root. Each candidate retains its source, object, log, raw code, differing-word
report and compiler settings. The previous shake archives were read and left
unchanged.

## 0052d9a0: inspected only

The residual inventory lists this short statistics accumulator without a
current score. Its owner documents an ee-gcc scheduling floor, but the adjacent
timing routines are in the CRI group, no actual caller was found, and the
available IDA/Ghidra corpus has no corresponding source entry. First-party
provenance was not established. No compilation, source edit or status relabel
was performed for this candidate.

The older weighted selector `0024be40` was also screened out after reading its
extensive prior register experiments and unresolved actual RNG return-type
contract. No new probe or production edit was made there.

## Sprite attachment: 0046a7f0

After the quad recovery, a fresh configured-owner baseline confirmed 844
emitted bytes in the 848-byte retail window, nineteen masked word differences,
and eighteen aligned edits. Eight new structural candidates were compiled in
`fresh/worker7/attachment46a7f0/` using `attachment_probe.py`: three real angle
predicate boundaries, three Y-coordinate store/value boundaries, and two
Y-coordinate live-value forms. Seven tie the baseline. Reusing the existing
`h` local regresses to 32 words and 38 edits. No candidate was installed.

The full provider audit identifies a separate acceptance constraint. Current
`RwV3dTransformPoints` in `src/renderware/plcore/bavector.c` returns `RwV3d *`,
and `RwMatrixDestroy` in `bamatrix.c` returns `RwBool`; the owner currently
declares both `void`. The current rotate declaration also uses a legacy
formal-argument order. No incompatible declaration was introduced or used to
claim a recovery.

The retail input copy is definitively four records with twelve-byte stride:
destination `sp+0x80+i*12`, with stores only at offsets zero and four. The
actual four-call source caller in `sdkSpr.c` supplies two-coordinate points,
which this function copies into that three-coordinate local array. This is
not an eight-byte-stride transform input or an overlapping next-vertex X.
The current default transform callback, `VectorMultPoints` at `003e3d00`,
reads `pointsIn->z` at `bavector.c:396`. No legitimate initialization of those
local Z values was found in the retail function, IDA, Ghidra, or current
provider/caller chain. Zeroing Z would add stores absent from retail and was
not attempted. This unresolved input contract is independent of the nineteen
instruction-word residual.

The owner and SDK providers remain unchanged; all ten compilations, including
the active and guarded baselines, completed before release. The subsequent
`00268230` inspection read its current source, copy provider, decompilers and
retail loop, but performed no new compile or edit before the priority SDK
closure review.

## Dungeon entry: 00268230, after the SDK review

The separate SDK/Fcl review completed with an integration recommendation;
its independent evidence is recorded in
`SDK_Fcl_independent_review_167e607_worker7.md`. Matching probes then resumed
in `fresh/worker7/dungeon268230/` without changing the live owner while the
prime prepared SDK integration.

The probe owner uses the actual `u32 func_00452560(void *task)` getter and
passes each real task argument, including the destructor and update caller.
This avoids treating its old unprototyped pointer declaration as an exact
contract. The current copy provider was also read: `0043f810` returns its
original destination, including its sixteen-byte copy path.

The fresh guarded baseline emits 1,596 bytes in a 1,600-byte window, with
twelve relocation-masked word differences confined to the known address
construction region `+0x480..+0x4b0`. Its four-byte retail suffix is zero.
Six new structural candidates were measured by `dungeon_copy_probe.py`:

| Mechanism | Result |
| --- | --- |
| Four-copy inline helper with actual entry and output aggregates, three parameter groupings | All tie twelve words and 1,596 bytes |
| Helper containing the first three copies, leaving the target copy outside | Ties twelve words and 1,596 bytes |
| Return the entry pointer after the first copy, then use its current vector | Regresses to 370 words and 1,572 bytes |
| Use the actual `memcpy` destination result as the previous-vector source | Regresses to 370 words and 1,572 bytes |

The two shorter candidates leave nonzero retail bytes beyond their emitted
body. None was installed. No new complete relocation/table proof is claimed
for these negative probes; the existing fully resolved archived frontier
remains the relevant proof for the unchanged near match. The scratch owner,
source candidates, objects, logs and raw comparisons are retained. All eight
compilations, including the active and guarded baselines, completed before
release. The live `cldDungeonIn.c` remained unchanged by this worker.
