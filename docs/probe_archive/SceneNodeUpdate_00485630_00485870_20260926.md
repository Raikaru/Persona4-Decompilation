# Scene node update pair 00485630 / 00485870, round 2 (not matched)

Both stay `INCLUDE_ASM`. Round 2 worked on 00485630; 00485870 has the same
wall at the same instructions, so what holds for one holds for the other.
Best body: `KoD_00485630_20260926_body.c` (3 words, 564B/576B), which is the
round-1 propagation-off draft with the goto loop replaced by a plain `for`
loop with `continue`s. The `for` loop produces the same code.

## The wall

The restore after the func_00486970 / func_00486330 calls is
`addiu $vN,$sp,0x50; lq $vN,0($vN); sq $vN,0x50($s2)`. Retail uses `$v0` and
every body so far gets `$v1`. The save just before it matches (`$v1`
address, `$v0` data).

Measured in round 2, all still 3 words:

- restore spellings: a second pointer (`src = &save`), a scoped
  `u_long128 *p = &save; u_long128 v = *p;`, one value temp shared with the
  save (with one or two pointers), `save_slot[0]`, `u_long128 save[1]`,
  `((u_long128 *)arg0)[5] = *save_slot`, the save slot declared in the block,
  and the save through a named value temp;
- the callee declarations: `func_00486330` returning s32, `func_00486970`
  returning void (both diagnostic only). Neither moves the colour;
- deleting everything after the block (diagnostic only). The restore still
  takes `$v1`, so the choice does not depend on the loop;
- pragmas together with propagation off: `scheduling off`, `opt_dead_code off`,
  `opt_lifetimes on/off`, `opt_vectorize_loops off`.

Worse: `*(save_slot = &save) = ...` assignment-expressions (118), a named
`node_q = (u_long128 *)(arg0 + 0x50)` (120), an inline `copyq(dst, src)`
helper with or without propagation (117, 548B: both addresses fold), an
`f32[4]` slot with direct `*(u_long128 *)` casts (118), and `peephole off` (69).
