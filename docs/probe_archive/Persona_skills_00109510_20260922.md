# Persona learning range: func_00109510

The native current owner now reports **55 MATCH / 1 ASM**. The recovered
function reproduces **684/688 bytes**, all **11 unmasked relocations**, and
four zero alignment bytes. All 55 neighboring functions and all allocated
data retain their original bytes and relocation meanings. The only additional
sized symbol in the measured object is non-executable compiler metadata;
the inline range helper introduces no out-of-line code.

The first pass selects the existing learning-table interval above the persona's
current level and within its remaining reachable levels. It retains the party
and normal table layouts, their 32/16 entry limits, the terminating kind byte,
and the normal persona's base-level adjustment. `last` is read only after the
path setting `foundLast` has initialized it. Otherwise the selected count is
zero. The initialized skill pointer is retained across the known-skill query.

The second pass uses separate output and traversal counters. It emits kind-one
skills absent from the existing skill list, writes the optional adjusted-level
array only when supplied, and stops at 32 outputs. The private inline helper
computes the real inclusive range length, `(last + 1) - first`. The final
declaration order puts the output counter before the traversal counter. This
closed the remaining nine register-operand differences without dummy state,
undefined reads, assembly, or omitted work. Scoped propagation and loop-
invariant settings are retained with their actual compiler measurements.

The sole active external caller is `func_00113610` in `code1_0011.c`. Its
declaration now matches the pointer input and two output pointers, and its
existing input address is converted explicitly. Its two actual 32-entry
halfword output arrays replace byte-array views; both buffers remain 64 bytes.
The typed-array control and final current-source compile preserve the caller
owner's **entire object**, including all 47 functions and data. Independent
unmasked proofs also preserve `00113610` (288/288 bytes), the prior font recovery
`001130c0` (440/448), and its caller `00112300` (712/720). The caller owner remains
**46 MATCH / 1 ASM**.

The complete retail body, both decompiler outputs, actual providers and caller
were read. Detailed initialization and range reasoning is preserved in
`build/first-party-next-20260922/dat-persona/REVIEW.md`. The old claimed
89-word archive used an uninitialized skill pointer and was not the accepted
source. The completed zero candidate is `inlined-range-ordered-counters/`;
prime dispatched its single previously blocked job and then finished integration
after the worker's context ended. No completed job was repeated to recover its
terminal.

Final native and official evidence is in
`build/continue-first-party-20260922/persona109510/target-integrated/` and
`caller-integrated/`. Each `completed.json` binds the current source, compiler,
flags, dependencies, object, complete proof and official report. Official
scoring explicitly reuses the just-compiled native current-owner object.

Target source SHA-256:
`99e6fff3e75c3d31b59d516e26a02f0fbc3c4c71887e04afe3c0e74db70dc839`.
Target object SHA-256:
`7dd8d0d8e5986af6912f7f694ee775eaf17598ff0712becb7120ff68dada0d7a`.
Caller source SHA-256:
`3c764232a920e750d3fa427d396d6446d7a08418c85452dd00b14aed25aa8e16`.
Unchanged caller object SHA-256:
`edecbc4627bfdabfda09ad74e1656bab4a69a4fa688b3bdb30c3821609e7db7a`.
