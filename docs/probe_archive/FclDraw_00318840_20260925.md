# func_00318840 recovered (2026-09-25)

`func_00318840` (y_fclCombineDraw.c, 1776-byte window, 441 instructions plus
three zero tail words) now verifies MATCH from C. Prior floors: 394 guarded
words and 195 alignment edits (worker 5). An earlier note recorded the
FP saved-register rotation as a floor at nd 427.

## Shape

- Signature `(u8 *, s8 mode, s8 slot, s16 depth, s16 order)`. This matches
  the existing y_fclCombine.c caller declaration, and the `s16` depth
  converts with `mtc1/cvt.s.w` and no extra normalization.
- `pos = *(FclVec2 *)(func_002b6150(slot + 0x20D) + 0x38)` gives the
  `lwc1/lwc1/swc1/swc1` snapshot at 0x108. `pos.x` is reloaded for each new
  expression and `pos.y` stays in `$f23`.
- Colour stores `*(FclDrawColor *)(func_002b6150(id) + 0x85) = c` give the
  grouped `lbu`x4/`sb`x4.
- `scale = iGpffff8360` is loaded once and stored twice, and the 59.0f and
  1.0f stores stay inline. The 0xAB and 59.0f values are materialized before
  their lookups.
- The first `func_002b68d0` uses `arg2 + 0x20D` again, not the saved
  resource, so the `(s8)slot` re-normalization lands before it.

## The float allocation (the old "floor")

With named `f32` x0/x1/x2/y0 locals and `func_002b2970` writing through an
output packet, the best result was 14 words. That residual was a `$f20`/`$f23`
swap between `pos.y` and `33 + x`, and neither 840 declaration-order
permutations nor a 25-minute permuter run moved it. The fix follows the
coordinator's lever: a block-scope `extern FclVec2 func_002b2970(f32, f32)`,
with every point built inside the store or `func_002b6c30` call that consumes
it and every coordinate written inline (`6.0f + pos.x`, `62.0f + pos.y`,
...). The compiler's CSE of those expressions then colours the values exactly
as retail does (0 words).
