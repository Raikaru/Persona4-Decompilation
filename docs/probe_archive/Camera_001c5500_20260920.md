# Side framing camera: func_001c5500

The recovered C emits all 1,664 bytes of the retail window. Applying all
27 code relocations produces an exact byte comparison, with no suffix and
no new compiler-owned data. The integrated current-owner verifier reports
52 MATCH / 11 ASM for `src/promoted/code1_001c.c` and 46 MATCH / 0 ASM for
`src/Battle/btlCamera.c`.

## Source recovery

The initial live draft reproduced its saved 78-word residual at the correct
1,664-byte size. Three related source changes recovered the instruction
order and overlapping float lifetimes:

* The second horizontal dot product evaluates the X contribution before
  the Z contribution, as in the first dot product.
* Both eye-position branches capture `basePosition.z` immediately before
  calculating the eye's Z coordinate. The same initialized `baseZ` is used
  by the later projection query, across the quaternion calls. Its declaration
  precedes `unitY`, matching the two real, overlapping coordinate lifetimes.
* The positive-side branch computes its radius as `1.5f * (unitRadius *
  unitScale)` before assigning the factor used later by the distance query.
  Staging the radius in a mutable temporary left one commuted multiply;
  the direct expression recovers that final operand order.

The combination is significant: individual source changes could worsen
the old positional word score. The saved assertion that register allocation
could not be influenced by these source shapes did not hold for the complete
source and lifetime correction. No new optimization pragma, artificial
storage, volatile access, helper call, or assembly was added.

## Complete objects and interfaces

The function reuses the existing `P4CameraFrame` type for both complete
28-byte position/quaternion poses. The first pose is filled by `001bd560`;
the second quaternion is produced by the look-at operation before its use,
and its position is initialized before either complete pose is copied or
submitted. The projection helper receives complete two-float buffers. All
branches initialize the cached coordinate and selected unit pointers.

Sphere-center calls use the provider's `BtlUnit *` / `RwV3d *` interface.
The normalization call uses the canonical `RwV3dNormalize` interface, and
the pose-copy declaration matches its provider. Both direct calls are in
`001c6560`; its declaration now agrees with the camera-first `u8 *, s32`
definition and its camera pointer conversions are explicit. Both calls retain
their second argument of one. The existing visibility argument order and
float-returning tangent declaration are preserved.

The source keeps retail's negated ordered side tests, radius and height
thresholds, 150-unit alternate displacement, visibility modes, and duration
selection. As in retail, the caller must supply initialized camera/action
state, a valid actor, and a first target record; the recovery does not invent
fallback objects for invalid battle state. The source/storage review and
resolved instruction proof do not claim emulator execution.

## Preservation and receipts

`build/first-party-next/camera-resume-5500/` contains immutable input snapshots,
the original guarded source, each measured candidate, owner compiler commands,
objects, disassembly comparisons, and complete unmasked relocation records.
`release/integrated-proof.json` checks all 62 other owner functions and all
46 caller functions, including canonical relocations and every allocated
data section. The caller object is byte-identical to the previous encounter
camera seal. The official current-source compile exits zero, and
`git diff --check` passes for both changed owners.

```text
python tools/verify.py src/promoted/code1_001c.c src/Battle/btlCamera.c --show-mismatches --json build/camera-5500-verify.json

resolved target SHA-256
336abc4c1c0865cc7781a4ec38dcedc5f3d68e92d4427e9c51f50910ef64ad23

code1_001c.c source SHA-256 at this seal
9b93f3bb2814a61b69def10dfd0bc508e896ba69285fa285381863926f5e815e

btlCamera.c source SHA-256 at this seal
2f0f6d2fc3865c1d104141169e404d49c266451b24abe33165dd19ef1b0d7019
```
