# Header rows: exact `func_00315600`

`func_00315600` in `src/Event/Fcl/y_fclCombineDraw.c` compiles to its full
retail window (3684 bytes of code plus 12 bytes of zero padding in the
3696-byte window), with every relocation resolved.

The old guarded body with named `func_002b2970` output packets was at 755
words. Rewriting it in the point-by-value form from
`FclDraw_0031c2b0_0031cce0_20260925.md` took it to 788. Then four levers
closed it.

1. **f32 tables cast to `FclVec2 *`** (788 -> 690). With `extern FclVec2
   D_00643DA8[]`, `b = D_00643DA8` is folded into every load as
   `lui`/`lwc1 (v0)`. Declaring the tables `f32[]` and assigning
   `b = (FclVec2 *)D_...` keeps retail's `lui/addiu $s1` base.
2. **Local `extern void func_002b6c30(s16, FclVec2, f32, s32)`** (690 -> 11).
   The shared `s64` prototype re-extends the `s16` id at every call. The first
   id is passed as the bare `(func_002e78a0() & 0xFF) / 10 + 9` expression.
3. **A second pointer variable `v`** for the then-branch rows after the
   first block. Alone it was neutral (11).
4. **A separate `s16 id2`** for the then-branch
   `(s8)func_00110a60(..) + 0x13` index (11 -> 4 -> 0). Retail keeps it in
   `$s1`. The else-branch reuses `id` (`$s0`), and giving it `id2` as well
   left 4 words.

`opt_propagation off` (778) and `opt_dead_assignments off` (802) are no
longer needed. Both made the by-value draft worse.

Each point is built inside its consuming call:
`func_002b69f0(id, func_002b2970(300.0f + (24.0f + (13.0f + pos.x)), pos.y),
func_002b2970(24.0f + (13.0f + pos.x), pos.y), 0, 0xA, 0)`. In the else
branch, `pos = *(FclVec2 *)(func_002b6150(id) + 0x38)` is passed directly as
the first point.
