# Exact second rank-up palette: func_00256460

This record preserves the already completed second-palette integration from
the interrupted campaign. `00256460` emits **612 bytes in its 624-byte retail
window**, with **three fully resolved code relocations**, twelve zero alignment
bytes and no added owned data. It was integrated before the later first-palette
recovery described in `Rankup_002561f0_palette.md`.

The point-buffer correction uses the actual `PrimFloat2` point type through
explicit `PrimPointRow` union members. Source words are copied from the declared
`const CopyPair[16]` table and the point member is subsequently translated. The
consumer addresses each actual point subobject at its eight-byte stride. This
supersedes the earlier proposal that reinterpreted incompatible separate array
types through member pointers. Colors retain their eighty-byte allocation;
only the sixteen completely initialized entries are submitted.

The necessary game-code owner family comprised `cmmRankUp.c`, `sdkPrimitive.c`,
and promoted `code1_0012.c`, `code1_0026.c`, and `code1_0036.c`, with the shared
`primitive_point_buffer.h`. The actual initializer and point-consumer layouts,
positive counts, source-table extents and synchronous buffer lifetime were
reviewed together. Other affected active functions retain their code and
canonical relocations. Existing allocated data is preserved. The two explicit
36-word table declarations in `code1_0036.c` change only their undefined symbol
size metadata, not any emitted code or data.

The completed integration is preserved under
`build/game-resume-20260921/rankup/point-contract/integrated/`: its
`official-completed.json` records exit zero and the exact source hashes for all
five owners and the header. `official.json` reports 184 MATCH / 31 ASM for that
owner family at that checkpoint. `proof.json` binds current-owner compilation,
all target/protected-function relocation proofs, sibling and table comparisons,
and the separate guarded-caller checks. Those historical counts are not a new
global verifier result.

The independent representation review is preserved in
`build/game-resume-20260921/audit/RANKUP_UNION_ROWS_REVIEW.md` and
`rankup-union-rows-evidence.json`. The native compiler is MWCCPS2 b210 with the
configured `-O2 -Iinclude` flags; the measured palette scope retains
`opt_loop_invariants on` for the retail conversion ordering.

The subsequent first-palette integration recompiles this second palette and
independently proves it again, without changing its body, instructions or data.
That latest protection proof is
`build/first-party-continue-live/rankup/integrated/func_00256460.proof.json`.
