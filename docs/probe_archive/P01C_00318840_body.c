/* object 1776B baseline / window 1776B / normalized_diff 427 (prior preserved-body measurement); differing offsets: FP-saved-register/frame region (retail swc1 sp108 and f20..f24 assignment versus mwcc extra FP save and shifted frame), with downstream offsets shifted; classification: FP saved-register allocation floor (the adda.s/msub.s sequence itself is ordinary single-precision arithmetic, not the floor); ruled out all plausible f32 declaration-order permutations and fully inlined constant-expression variants (all nd 427), while call/branch/copy shapes matched; no new source probing. */
