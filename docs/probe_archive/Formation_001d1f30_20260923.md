# Formation placement 001d1f30

The recovered formation callback adjusts unit positions and facing according to
the action's target group, companion spacing, unit radii and formation flags.
The complete three-component vectors, four-component quaternion and separate
two-component line/projection buffers describe the actual retail workspace.

Configured MWCCPS2 b210 with `-O2 -Iinclude` emits 3,668 bytes in the 3,680-byte
retail window. All 73 relocations resolve exactly; the last twelve bytes are
zero alignment. The reviewed owner preserves the native bytes and canonical
relocations of all 33 other functions and its allocated data. Scoped
`opt_propagation off` and `opt_lifetimes on` retain the measured phase lifetimes.

The boss predicate `func_0022fb90` returns a full-word zero or one. Its definition
and sole authoritative caller now agree on `s32` and the two `BtlUnit *`
parameters. The provider change preserves all fifteen functions and all data in
`btlBoss.c`. Keeping the old `u8` provider contract made the formation caller
insert narrowing after the call and grow by one instruction.

Three missing initializations are retained because the corresponding retail
paths also skip those definitions. The source comments identify them:

* A null companion bypasses the normal `isClose` assignment. The later distance
  clamp sets it only when the adjusted distance is below 300; otherwise the
  original saved register is observed.
* The filtered-center provider returns without writing its output for an empty
  group. This caller replaces only Y before using X and Z.
* The farthest-target scan writes `best` only after a qualifying farther target,
  but passes its stack slot to the orientation helper even after an empty scan.

The respective retail evidence is the branch at `001D2260` and observation at
`001D25CC`, the center call at `001D2738` followed by the Y-only store, and the
conditional vector stores at `001D2B98..001D2BA8` followed by the unconditional
call at `001D2BD4`. No default values, synthetic targets or side effects are
introduced. These are preserved initialization omissions, not claimed runtime
invariants.

The resolved target SHA-256 is
`7b1c06f89361e1230c98b2f9ef79c831a03227f7c54ecd9f92e44c052fa598f5`.
The detailed source/retail audit, original exact proposal and incompatible
provider control are retained under
`build/finish-first-party-20260922/continue-24-formation/`.
The final reviewed sources, native compiler receipts, installed-source reports
and publication evidence are under `continue-25/formation/` in that campaign.
