# Shop draw state machine: 002be530 (not matched)

`func_002be530` in `src/Event/Fcl/y_fclShopDraw.c` is 49,728 bytes (12,429
instructions). It stays `INCLUDE_ASM`. The body banked in
`ShopDraw_002be530_body.c` was rebuilt from retail and replaces the earlier
m2c-derived draft.

## Measurements (tools/fnalign.py --candidate, MWCCPS2 b210 -O2)

| body | object instrs | edits | jal | frame |
| --- | --- | --- | --- | --- |
| guarded source draft (f490439) | 12378 | 20381 | - | -0x450 |
| same, `(u8)` float casts removed (first bank) | 8565 | 14608 | 888/1525 | -0x450 |
| fresh m2c, compiling (step 1) | 12891 | 5095 | 1567 | -0x6F0 |
| + conversions, loops, s16 temps, buffers | 12495 | 2674 | 1525 | -0x790 |
| + work-derived address temps inlined | 12480 | 708 | 1525 | -0x790 |
| + callee return/param types, index order | 12444 | 590 | 1525 | -0x790 |
| + int-typed id temps (current bank) | 12422 | 450 | 1525 | -0x790 |

The last rows are measured with the file-scope declaration changes listed
below applied. Every call target count now equals retail, the prologue saves
`$fp`, `$s0`-`$s7`, `$f20`/`$f21`, and `arg0 -> $s4`, `work -> $s1` as in retail.
The zero local lands in `$s2` (retail `$s3`).

## How the body was built

1. **m2c needs the jump tables.** Stage the asm with `.L` labels at all 49
   targets and a `.rodata` section holding `jtbl_00748930` (47 words) and
   `jtbl_00748910` (6 words). Give m2c a declaration-only context, with
   `func_002b2a60` written as `void (FclDrawColor *out, ...)`. Run it with
   `--valid-syntax` so field accesses come out as typed `M2C_FIELD` macros.
   The result has all 1,525 calls. The old draft was missing about 637 calls
   because it had been hand-mangled.
2. **Mechanical conversion.**
   - Turn `M2C_FIELD` into `*(T *)(work + off)`.
   - Turn `(s64)(x << 0x30) >> 0x30` into `(s16)`.
   - Colours: `c = func_002b2a60(...)` plus
     `*(FclDrawColor *)(t + 0x79) = c` (retail loads all four bytes and then
     stores them).
   - Positions: `sp = func_002b2970(x, y)` into `Vec2f` locals. Pass them to
     `func_002e0620` as `*(u64 *)&sp`, the idiom from func_002df4c0.
   - Rewrite unsigned-float conversion if/else pairs as `(u8)f` and `(f32)u8`.
   - `adda.s`/`madd.s` sites are `A + B * C`: b210 emits `adda 0,A`, and
     `msub` comes from `A - B * C`.
   - `ldr/ldl 0x2C` is `*(u64 *)((u8 *)func_002e04e0(h) + 0x2C)`.
3. **Structure.**
   - Move the default arm to the end and write `break` for
     `goto block_626`. States 0xD and 0x37 keep their own returns.
   - Write the 42 counting loops as `for` loops with `s16` counters.
   - `spC0` is `char[0x10]` and `spD0` is `char[0x80]`, the sprintf buffers.
     This is what put the frame at `-0x790`.
   - The eight `{id, 0}` message argument pairs at 0x668-0x6A0 are a local
     `{s32 v0, v1}` struct.
4. **The big lever was inlining m2c's `temp = work + i * 4` address temps**
   into the field expressions (`*(T *)(work + i * 4 + off)`), 28 sites. That
   alone went 2594 -> 708. It fixed the global colouring (`work` from `$s3`
   to `$s1`, `arg0` from `$s0` to `$s4`) because b210's own CSE temps colour
   differently from named locals.
5. **Table positions.** Retail loads each `D_0063F5xx`/`F6xx`/`F7xx` table
   entry into `$s0` before the draw call that precedes its two
   `func_002b2970` calls. Write `tbl = (f32 *) D_x;` at that point: 31 sites,
   placed after any colour store.
6. **Store-forwarded index.** State 0xE is `work[7] = 0; p = D_0063F9F0 +
   work[7] * 12;`. Retail's `dsll32 $v1,$zero,24` comes from forwarding the
   stored zero, not from a literal.
7. **Index order.** `lb idx; sll; addu idx, work` needs the integer form
   `(idx * 4) + (u32)work`, the de5a0 idiom. `lh 0xC16` indexing stays
   `work + idx*2`.
8. **Sign extension.** b210 re-extends every `s16` *local* when it converts
   it to int. Retail extends once, at the assignment, and then passes the
   value raw to int parameters. So the id temps are `s32` with an explicit
   `(s16)` at the assignment. Loop counters stay `s16`. Many callees return or
   take int in this unit (see the declarations below). That took 566 -> 469.
9. `0x62` alpha stores are `u8`, which lets 0xFF be CSE'd into `$s0` like
   retail. The Vec2f copy at +0x2C is a struct copy. The two bank-to-draw
   colour copies from `work + 0xF3E + i * 4` are `FclDrawColor` copies. The
   loop bound `work[9]` is re-read after state 0x15's loop (b210 CSEs it
   with the last loop test).

## Required file-scope declaration changes (not committed)

Applying all of these still leaves y_fclShopDraw.c at 28 MATCH / 10 ASM,
the same as the head without them:

```c
void func_002e0660(void *, u8, u8, u8, s16, s16);  /* last param was s64 */
s32 func_002e2830(void *, s32);                     /* was s16 return */
s32 func_00106600(s32 id);                          /* was s16 param */
s32 func_002e26a0(void);                            /* was s16 return */
s32 func_002b2cb0(s32, s32, s32, s32, s32);         /* s16 -> s32 return, */
s32 func_002b2d00(s32, s32, s32, s32, s32);         /* retail stores the  */
s32 func_002b2d50(s32, s32, s32, s32, s32);         /* result raw to s32  */
s32 func_002e2700(void *);                          /* result is an argument */
extern u16 D_008C024E[], D_008C0276[], D_008C027A[]; /* lui, not gp */
```

## Remaining residual (450 edits)

Largest remaining states: 0x28 (49), 0x15 (46), 0x16 (42), 0x25 (38),
0x2E (38), 0xA (23). About a third of the edits are branch-offset-only rows
that follow length differences.

- **func_00106880 conflict.** Retail passes `func_00106cd0`'s `s16` result
  to it unextended in state 0xA. That is seven sites, 14 instructions. It
  needs an `s16` parameter, but matched func_002df020 needs the current `s32`
  one.
- **The zero local is in `$s2`; retail has `$s3`.** This drives most of the
  register-only rows in states 0x12, 0x15 and 0x25.
- **The indexed-handle recompute in the 0xE/0x10 loops.** Retail computes
  `&work->obj[i + 0x3A]` into `$s0` right after the `if (i > 0)` block and
  passes `i` raw. This looks like `func_002e0620(work->obj[..],
  func_002b2970(..), func_002b2970(..), ...)` with Vec2f parameters, which
  needs `func_002e0620` to take `Vec2f`. That is a file-wide prototype
  change affecting matched func_002df4c0.
- The id/loop temps in states 0x28 and 0x2E still need the same int/`s16`
  split per variable.
