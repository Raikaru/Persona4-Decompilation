# Event model callback: func_00288170

The completed model-callback package was recovered from the interrupted Event
worker, reviewed, and integrated after the `00288af0` camera callback. Fresh
compilation of the actual changed sources reproduces the reviewed objects.

`func_00288170` is now ordinary unguarded C with **2,424 executable bytes in
its 2,432-byte retail window**. All **64 code relocations** resolve exactly;
the remaining eight bytes are zero alignment. Its native switch owns the
**48-byte, twelve-entry table at `0x00748440`**. All twelve table relocations
were applied and compared to retail without masking.

## Recovered source and contracts

The callback now has seven complete three-float vectors, a 304-byte path
record containing its header and twenty-five vector slots, sixteen bytes of
motion settings, and a complete 64-byte direction matrix. A matrix union
exposes its sixteen-word output interface and the actual direction vector at
offset `0x20`. These are real initialized objects used by the corresponding
providers, replacing disconnected scalar fragments and undersized buffers.

The command dispatch retains the native switches and source snapshots needed
for the retail argument order. The rotation duration at command offset `0x24`
is a signed halfword; case 11 reads its flag byte from that same offset. Those
two fields had been misread in the old guarded draft. Float multiplication,
division, comparisons, and call order retain their retail behavior. The
measured propagation setting is scoped to the recovered callback.

The six actual Scene/model interfaces are declared in the existing shared
`scene_event_internal.h`. The matrix builder accepts a position pointer;
its RenderWare translation and rotation declarations have their actual
matrix-pointer return types. The path wrapper receives the complete path
through its existing 32-bit address carrier and explicitly forwards its
resource ID. Sound/motion parameters retain their full-word motion value,
unsigned sound byte, and signed halfwords. The animation wrappers receive
unsigned 16-bit resource IDs; the duration wrapper retains an unsigned
16-bit duration.

The caller inventory covers thirteen direct retail calls across six owners,
including the guarded transform caller. No aligned reference to these six
function addresses was found in allocated non-code retail data. The six
additional shared-header consumers were compiled as unchanged controls.

## Current-source verification

Prime repeated the package's before-input audit, then compiled the six
changed owners from the actual live files. Every complete object is identical
to the reviewed proposal, including its symbols, instructions, data, and
relocations. All 426 original functions across the twelve checked owners
retain their bytes and canonical relocations. The previously recovered
`00288af0` remains exact at 1,060 executable bytes with seventeen relocations.

Prime also independently resolved the two existing Event switch-table owners
(`00286780` and `002898b0`). Together with the new table, these proofs account
for **all three allocated Event data sections: 308 bytes of contents and
eight zero inter-section alignment bytes**. The production data-placement
function accepts `.rodata` at `0x00748350`, size 316. No data, symbol, or
linker configuration change was needed.

The official twelve-owner run reports **427 functions: 405 MATCH and 22 ASM**,
with no other statuses. This is scoped current-source verification; the
full-image and whole-campaign checks remain separate integration gates.

Evidence is retained in `build/resume-live-20260921/event-model/`:
`audit-before.json`, `audit-current.json`, per-owner compiler receipts,
`integrated-proof.json`, `all-data-proof.json`, `resolved/`, and `official.json`.
The independent integration script is `event_model_integration.py` in its
parent directory. The original worker package remains intact under
`build/continuation-20260921/event-lane/release-model-closure-v3/`.

The other Event candidate, `00287360`, remains guarded. Its latest faithful
structural reconstruction and the remaining conditional/address-order
differences are recorded in the Event lane's `CONTINUATION.md`; it is not
counted as a further C recovery.
