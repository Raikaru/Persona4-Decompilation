# Camera-shake update: 004a7830

Starting revision: `0360227d0a4194cb84500a5cf63ac554b38a79c2`.
Owner: `src/promoted/code1_004a.c`; configured MWCCPS2 b210, `-O2`.

The guarded C improves from **19 relocation-masked differing words / 20 fully
resolved differing words to seven under either comparison**, at the same
**532 executable bytes in a 544-byte retail window**. The twelve remaining
bytes are zero alignment. This function remains `NONMATCHING` and retains
its assembly fallback.

## Retained source change

The step calculation now keeps its random call in the expression:

```c
temp_f21 = 0.5f * delta[idx] +
           0.5f * (delta[idx] * effMiscRandFloat(0));
```

Both amplitude reads refer to the same initialized local array element.
The real random provider cannot access this local array. Moving the random
call into the expression preserves its one call and the arithmetic grouping,
while allowing the compiler to reproduce retail's half-value materialization
before the call and its product after the call. Retail's MULA/MADD sequence
is preserved; separate multiply/add rounding is not substituted.

The remaining seven function-relative offsets are `0xB0`, `0xC0`, `0xC4`,
`0xD4`, `0xE0`, `0xEC`, and `0xFC`. They select different float registers for
zero, the current output and its absolute value. No instructions, branches,
calls or constants are missing at the retained floor.

Measured alternatives included separate half-value initialization, captured
versus reloaded amplitudes, genuine inline calculation/update boundaries,
current/magnitude/bound scopes, declaration positions, a shared zero value,
and comparison operand order. None closed the remaining register differences.
An inverse-sign diagnostic was excluded from acceptance because it changes
NaN behavior. These measurements do not establish that an exact source is
impossible.

## Actual provider and storage contracts

The random function is the active `effMiscRandFloat(EffRandState *)` in
`src/Graphics/Effect/effMisc.c`, marker `004BD0B0`. The owner now uses its
canonical name and a compatible forward-declared state pointer; its calls
pass the null state to use the provider's own random state.

The camera getter is actually `s32 func_00457120(void)`. Its owner-local
declaration now agrees, and every use explicitly converts the EE address word
to its previous byte-pointer view. The translation provider's existing
`u8 *(u8 *, const f32 *, s32)` declaration already agrees with the definition.

The two shake outputs occupy a real two-float array beginning at
`0x00764C78`. `D_00724C78` is declared as that array rather than a scalar
whose address is indexed beyond its storage. The existing second-word alias
`D_00724C7C` is typed as a float. Their recovered symbol addresses, rather
than the misleading address-like identifier spelling, determine placement.

## Verification

The candidate's sixteen relocations were applied without masking. Its
eight-byte constant pair was independently placed at `0x00764200` and its
complete bytes compared with retail. All 91 other functions retain their
instruction bytes and relocation destinations. Existing allocated data is
unchanged; enabling this C candidate adds only the verified constant pair.

After guarded integration, the current active owner was compiled again:
all **92 functions** and all allocated data remain unchanged. The target was
also separately enabled in the actual current owner and recompiled; the
seven-word residual and all 91 neighbor comparisons remain as above.
Canonical external symbol names were compared by their proven addresses;
anonymous local symbols were compared by section, offset and contents.

The source, objects, compiler logs, all measured variants and full receipts are
under `build/first-party-0360227/shake/004a7830/`. In particular:
`coherent-proof.json` records unmasked candidate resolution, and
`integrated/proof.json` records the final guarded integration.
`build/first-party-0360227/shake_verify.py` reproduces the current integration
checks against the retained immutable baseline.

The integrated owner SHA-256 at this checkpoint is
`a51edc0442f8b3d8f95bb204c55a81dc6bde1f8d1d17dbf807f95306e8f73fb1`.
The targeted compiler proof establishes an improved guarded candidate, not
an additional first-party C match or a whole-image build claim.
