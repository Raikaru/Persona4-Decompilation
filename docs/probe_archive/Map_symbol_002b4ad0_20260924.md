# Map-symbol drawing callback

Owner: `src/Yajima/y_symbol.c`, `func_002b4ad0`.
Compiler: MWCCPS2 b210, `-O2 -Iinclude`.

The recovered callback emits 1,236 bytes in its 1,248-byte retail window.
All 38 relocations resolve to the original destinations, and the final twelve
bytes are zero alignment. The fourteen other functions in this owner retain
their instruction bytes and relocation meanings. Allocated data is unchanged.

The callback projects a map symbol, checks whether its position is visible,
and submits the appropriate sprite. Its origin and projected coordinates use
complete eight-byte objects. The projected pair and the draw-call snapshot
have both floating-point and packed views; their copies preserve the observed
memory lifetimes. The origin retains its byte-addressed accesses within a
complete union containing two floats.

The call contracts are established from their actual providers. The map
availability query `func_002ac3c0` returns `s64` and accepts two signed words;
callers explicitly preserve the byte comparisons and masks they require.
The grid-coordinate functions consume a three-float vector by value, while
the projection functions `func_002b13e0` and `func_002b1480` take a float and a
vector pointer. Their float-first declaration order is applied to both
definitions and callers in `y_smap.c`. All 36 functions in that owner retain
their native instructions, relocations and allocated data.

The original worker package is preserved under
`build/finish-first-party-20260922/continue-29-symbol-contract/package-final-review/`.
Its complete coordinate-union refinements were measured independently and
together. Current-checkpoint review, compilation and integration records are
under `build/finish-first-party-20260922/continue-34-symbol/`. The final review
removes redundant duplicate pointer casts and replaces obsolete measurement
comments without changing the recovered instructions.
