# Field sphere-collision callback recovery: 00169320

The `src/Kosaka/Field/k_fldFrame.c` callback `func_00169320` is recovered as
1,112 bytes of native C code followed by the retail eight-byte zero tail.
All three code relocations resolve to the retail instructions. The callback
owns no data. The proposed full owner verifies at **12 MATCH / 4 ASM**.

## Source mechanism

Both collision branches search the collector's existing normals. The previous
inline equality expression merged the last failed comparison with the found
exit. A shared, inline plane-search function returns either the matching
index or `-1`. It preserves the retail three failed-comparison branches,
the separate found-index exit, and the existing-entry versus append behavior.
The helper performs the same reads and does not allocate or modify the collector.

The collector has point vectors at `+0`, normal vectors at `+0x300`, distances
at `+0x600`, and its count at `+0xB04`. Interior and edge handling retain the
retail address-evaluation order. In particular, edge distance and normal-record
addresses are formed with the collector base first. This closes the last four
operand-order differences without assembly, padding, or volatile accesses.

The complete `RpCollisionTriangle` layout replaces the old local prefix with
an unnamed gap. Its normal, point, index, and three vertex pointers agree with
the PS2 RenderWare header. Both interior and edge vertex-array addresses derive
from the complete collision object, not its normal subobject.

## Callback and provider contracts

The authoritative `include/rw/sky2/rpcollis.h` declaration is a five-parameter
world-triangle callback returning `RpCollisionTriangle *`: intersection,
sector, collision triangle, floating-point distance, and user data. The
owner-local callback typedef and both registered world callbacks now use this
contract. The distance argument is present even where the callback recomputes
its own plane distance. The existing `func_0016b260` callback retains identical
code and relocations after its signature correction.

The actual `func_00394d70` dispatcher selects the sphere path through
`func_00391c10`. At retail `0x00392448`, that path loads the callback distance
into `$f12`; its indirect call at `0x00392488` passes intersection, sector,
triangle, and user data in `$a0` through `$a3`. This agrees with the SDK header.

The interior containment provider has its actual
`s32 func_00168ec0(f32 *, f32 **, f32 *)` declaration. The length provider
`func_003e4180` has a floating-point return. Current source-bound native objects
for these providers and the two RenderWare dispatch functions have complete,
unmasked relocation proofs. RenderWare assembly fallbacks remain assembly
fallbacks; they are not counted as new first-party C recoveries.

## Validation and immutable evidence

Evidence is under
`build/continuation-20260921/field-retained6/00169320/`.

- `collision-base-final/compile.json` records the configured MWCCPS2 b210
  compiler, `-O2`, matching frozen include files, and the `r5900`/`eabi`
  assembly context. The retained native object is `collision-base-final/owner.o`.
- `seal-collision-base/manifest.json` binds original source, proposed source,
  object, compiler, input files, provider receipts, and patch identities.
- `seal-collision-base/owner-proof.json` and `functions/` prove all 16 owner
  functions after relocation resolution. All 15 neighbors retain their raw
  bytes and canonical relocations. Allocated data is unchanged.
- `seal-collision-base/provider-proof.json` and `providers/` retain the four
  source-bound provider proofs. Existing compilation receipts were reused.
- `seal-collision-base/official-proposal-verify.json` and its log record the
  official verifier on the proposed source using its already completed native
  object. Marker, byte, callee, and symbol checks are unchanged. No compiler
  was dispatched while sealing the proposal.
- `seal-collision-base/lint.json` reports zero errors and one pre-existing
  pragma warning in an unrelated function.

Source and object SHA-256 values:

| Artifact | SHA-256 |
| --- | --- |
| Original publication owner | `93bc2fc699472a7a3567efc6da732e2608625e63f849c4864f29bb3941967793` |
| Proposed owner | `31ed06ee18f692c6c0ed1ccddde0ea579186197e1a6b266f162de4146ab303cb` |
| Native proposed owner object | `c041bfca49854bc5e722df5babfcfe1672b027cb551d4276e45c2ff7b3b30cd7` |

The worker prepared source and documentation patches in isolated scratch.
The prime owns live integration and full publication gates. Subsequent
`00169780` experiments are separate and are not part of this recovery.
