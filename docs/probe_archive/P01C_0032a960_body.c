/* object 1696B baseline / window 1696B / normalized_diff 356 (prior preserved-body measurement); differing offsets: frame-size/saved-register region, per-block 0x66 materialization and 2b2970 11.0f+sp50.x f-register scheduling, plus FclVec2 copy load/store ordering (historical note did not enumerate individual offsets); classification: saved-register rotation + load-scheduling floor; ruled out named s32/inline 0x66, struct assignment, field-wise copies, cast forms, and otherwise exact six-block call/copy reconstruction; no new source probing because durable prior floor is in the hundreds. */
