# func_00304580 (y_fclCombine) — mechanical cleanup + partial rewrite, not matched

Measured 2026-09-25. Production stays `INCLUDE_ASM`; best body is
`FclCombine_00304580_20260925_body.c`.

Retail: 18240 B = 4560 instrs, frame `-0x300`, work pointer in `$s1`,
`arg0` in `$s4`. Switch on state 0x75..0x89 via `jtbl_00749360` (21 cases).

| measure | guarded draft | this body |
|---|---|---|
| object instrs | 4477 | 4623 |
| fnalign edits | 3065 | 892 |
| differing words (probe_archive.py) | 3982 | 3726 |
| per-case aligned edits | 1539 (after struct pass) | 796 |

The object is now *longer* than retail (+63): the draft's `(s64)` shift idioms
had been deleting code (undefined 32-bit shifts), so the honest body regained
it. Cases 0x7B (776/818) and 0x7E (1058/1077) carry most of the excess.

## What was applied (same levers as FclCombine_002ed430_20260925.md)

- Struct `FclCombineMenu` for `*(arg0+0x38)` (and its five re-read aliases);
  `f30C` is a `u8[5]` array (`f30C[i] = 0` loop, `+=` pairs per `switch`).
- `(s64)(x << 0x30) >> 0x30` -> `(s16)x`; `(f32)(s32)(load)` truncation around
  float loads removed (semantic bug in the draft).
- Colours as `FclDrawColor` values/struct stores; title positions as `FclVec2f`.
- All 21 `goto loop_N` loops -> `for` with `s16` counters (see the 002ed430
  note for why `for`).
- `func_003191c0`/`func_0031ac10` row params `s8` (implicit narrowing) and
  hoisted `slotByte`/`partyCount` temps: 974 -> 858.
- Title `y` coordinate `(f32)(i * 23 + 0x80)` computed into `temp_f20` before
  the `func_002e48a0` call (retail order): -33.
- Case 0x7A ported verbatim from 002ed430 case 0x26 (only `func_00275520`,
  no `0x15`, `count - 1`): 56 -> 5 edits.
- Case 0x77: the four `D_00749350` accumulation loops take an `s32 limit =
  b + 1` computed before the loop and a `u8 *table = D_00749350` base, like
  retail (`$a1`, `$a0` hoisted). Still open: retail re-extends the counter in
  the body (`dsll32/dsra32` then `addu table,idx`); ours reuses the loop-test
  extension. An `s32` counter with explicit `(s16)` casts did not change it.
- Case 0x82: `s8 sel` read into an `s32` so `(s8)sel` materialises.

## Second pass (same day, after func_002ed430 matched): 796 -> 706

The archived body now includes these changes (casealign total 706, object
4595/4560):
- Levers carried over from the func_002ed430 match
  (`FclCombine_002ed430_recovery_20260925.md`) moved it only 796 -> 787:
  - the real `func_00275820(f32, f32, f32, int, s8, int, const char *, int,
    int, void *, int)` signature;
  - `> 5` for the `func_00314660` range test;
  - `+ 0x270U` for the colour-branch sprite ids in 0x7E.
- **Semantic fixes in the 0x7B row draws** (787 -> 732):
  - `temp_17 + i * 4` scaled by the struct size. It must be
    `(u8 *)temp_17 + i * 4`.
  - The two `goto loop_N` loops become `for` loops with `s16` counters.
  - Colours and the position go straight to `func_002b83e0`, without the
    `fclPacketColor`/`fclDrawPositionValue` wrappers, which add stack copies.
  - The inner row draw reads its colours as `*(FclDrawColor *)(e + 0x75)`
    (retail `lwl/lwr 0x75..0x78`; m2c had a `u8` at 0x78). Its position is
    `*(FclDrawPosition *)(e + 0x28)` from the second `func_0034ae50` result
    (retail `ldl/ldr`; m2c had passed 0 and discarded that call).
- The row list as a typed `FclRowList { pad[0x154]; u8 *list; }` pointer
  (732 -> 706). Retail reloads `0x154($s0)` on every call instead of CSEing
  the field address.

Still open in 0x7B: the frame is too large, because there are too many named
`s64` point/stack locals, so every stack offset is high. The `$s2`/`$s3`
colouring of the two loop counters is swapped. The `0x25E` id sum is still an
`s64` `daddiu` from m2c's `s64` temps. The other row loops in 0x7E/0x81 have
not been given the 0x28/0x75 fixes yet.

## Third pass: 706 -> 472 (object 4563 / retail 4560)

Each lever is listed with its casealign total afterwards.
- **0x7E colour branches** rewritten in the matched 002ed430 0x2A form (576):
  - chained `+0x6E` alpha stores;
  - `FclDrawColor` struct copies between the 0x297/0x28B/0x27D/0x270 sprites;
  - `s32` ids with `+ 0x270U`.

  Also, `func_00275520` now has its real float-first signature from
  frFontEx.c.
- **Semantic bugs** (559):
  - `temp_17 + var_4` / `temp_17 + 0x30C` were scaled by the struct size.
    Fixed as `f30C[i]` / `f30C`.
  - `(s8)`/`(u8)` casts on the raw `f11E` passed to `func_002b2d00`/`2cb0`
    removed.
  - `D_008C024C` is an array.
- **0x77 accumulation loops** (544): plain `for (idx...) f30C[k] +=
  D_00749350[idx]` with an s16 `idx`, under a scoped
  `#pragma opt_loop_invariants on` for the function. This hoists `limit` and
  `table` and re-extends `idx` in the body, as retail does. Still open: retail
  colours `idx`/`limit`/`table` as `$a2/$a1/$a0`, ours `$v0/$a2/$a1`.
  - Declaration position of `idx`, reusing the zero-fill counter, and a
    value temp (u8/s32/u32) were all measured with no gain.
- **m2c `s64` temps retyped** (498):
  - five sprite-id temps to `s16` (514);
  - then a greedy type search over the remaining `s64` temps, which kept
    `temp_18_14` and `temp_3_18` (498).
- **0x7B `rowX`** (490). Retail computes `i * 0x17` into a saved register
  *before* the `func_0034ae50(f184, i)` call. MWCC's propagation sinks a
  single-use temp past the call; an inline helper taking `x` as a parameter
  sinks it too. A second use (`rowY = rowX + 0x7F`, the y coordinate the
  sibling branch really uses) keeps it early. This is a dead store here
  (lint H007), so it is a research lever only. The row header is probably a
  shared inline or macro in the original.
- **Real prototypes** (472):
  - `func_002b6a70(s16, u8, u32, u32, u32, s32)` from y_draw.c;
  - `func_0031c2b0(u8 *, s16, FclVec2f, FclVec2f)` with the points passed as
    `*(FclVec2f *)&spNNN`;
  - `basePos = (FclVec2f *)D_00640C10` (as in 002ed430).

Remaining, largest first:
- 0x7B 146: loop-counter colouring `$s2`/`$s3`, and the 0x40-flag branch's
  `spD0` spill (`sq 0xd0`) that we keep as a named `sw` local.
- 0x77 102 (colouring).
- 0x7E 73.
- 0x81 47.
- The frame: all stack offsets are still low. Named m2c locals such as
  `sp100`/`spF0`/`spD0` are compiler spill slots (`sq`) in retail and should
  become plain temps.

## Next steps

Port 0x7B/0x7E/0x81 from the 002ed430 0x2A/0x2C bodies (same inner draw loops,
`func_0034ae50`/`func_002b83e0` rows), then run the register-confusion
comparison to order declarations.
