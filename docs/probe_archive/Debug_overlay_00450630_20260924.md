# Debug overlay renderer 00450630

`func_00450630` in `src/promoted/code1_0045.c` now compiles to 1,044 exact bytes
in its 1,056-byte retail window under MWCCPS2 b210 `-O2 -Iinclude`. Its sixteen
relocations resolve without masking, and the twelve-byte tail is retail zero
alignment. The recovered source draws the debug panel and its visible text
rows while preserving the original rendering-state calls.

The panel origin and current text origin are complete two-float values.
Their copies retain the original eight-byte loads and stores. Four complete
64-byte immediate-mode vertices and four texture-coordinate pairs retain
the retail stack layout: vertices at `sp+0x60`, texture coordinates at
`sp+0x160`, text origin at `sp+0x180`, and panel origin at `sp+0x188`.
No placeholder stack object or extra padding allocation is needed.

## Source recovery

The state and draw callbacks are retained as pointers to their actual dispatch
slots. This restores the two saved callback-base registers and the 0x190-byte
frame. The row and column counters remain distinct, with the column reset
before each row's vertical-coordinate calculation.

The panel's horizontal extent is computed and copied before its sixteen-pixel
border is added. This keeps the original arithmetic and literal setup order.
Each vertex-initialization phase uses a real typed pointer to the current
vertex, rather than repeatedly indexing the outer array. That source-object
boundary preserves the color constants before the near-depth load and closes
the final seven differing instruction words.

Near depth comes from the shared render-device record at `0x008872F0`, offset
eight. The record and 64-byte vertex layout follow the existing Sky RenderWare
headers. The panel initializes gray, partially transparent vertices, then
reinitializes the same vertices for white text after binding the font raster.
Fields not written by the original routine remain untouched.

The glyph column uses a signed remainder helper and a separate signed result
before float conversion. Its row uses an unsigned shift. Texture coordinates
are copied through complete pair and vertex pointers in the original order.

## Validation

The combined owner also includes the exact immediate-text renderer
`func_00450a50`. All 58 other functions and canonical relocation meanings are
unchanged. Allocated data is unchanged; neither recovery adds a data section.
The resulting owner is 59 MATCH / 1 ASM once installed and officially checked.

Resolved executable SHA-256:
`fc742d9f35f7d73606d6cf0d709d1ec69d30797bbb7b675335520d4d9dac9d7b`.

Candidate history, exact native objects and the final shared-layout source are
in `build/finish-first-party-20260922/continue-40-font/`, with the final candidate
under `review-pair/`. Installed verification and the full publication gate are
recorded in `continue-40-integration/`.
