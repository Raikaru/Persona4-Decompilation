/* object 780B / window 752B / normalized_diff unmeasured (prior attempts nd 174/173/173/174; object oversized); differing offsets: unrecorded; classification: Address-CSE-into-s-register plus branch-placement floor; ruled out: logic and call sequence were fully transcribed, but b210 hoists elem+0x40/0x44 store addresses into saved regs across calls while retail folds 0x40($s0) per store; type==6/5 dispatch emits a range test or inline work block instead of retail's out-of-line beq 6/beq 5/b shape; `(arg0+2)==1` branch also inverts/inline-places differently; no further probing per durable source note and oversized-object policy. */
