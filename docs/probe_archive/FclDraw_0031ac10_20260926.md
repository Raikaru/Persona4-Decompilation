# Unlock rows: `func_0031ac10` float-cache probes

`func_0031ac10` is not matched. The best draft is still
`FclDraw_0031ac10_body.c`: fnalign 240 edits, 1450 against 1448 instructions.

## What retail does with `iGpffff8504`

The value is two locals, one per section. Each is loaded after
`func_002b6a70` in both arms of the section's first block:

- **Section 1 (blocks 0-3, `v30 != 2`).** It lives in `$f21`. Retail
  `lwc1 $f21` appears at asm lines 115 and 183, and blocks 1-3 reuse `$f21`.
- **Section 2 (block `0x2A3` and the two `v30 == 2` blocks).** It lives in
  `$f20`. Retail `lwc1 $f20` appears at asm lines 710 and 815.

## Probes

- **`ga = iGpffff8504;` before each section-first `func_002b6af0`.** Later
  calls use `ga`/`gb`.
  - This reproduces the loads, and `ga` lands in `$f21`.
  - The frame grows 0x10 (0x290). `gb` lands in `$f21` and the 2.0f
    constant in `$f20`, where retail has them swapped.
  - Result: 575 edits and 1456 instructions.
  - Declaring `gb`/`ga` in other orders or positions gives 575-738.
- **Loading the global once at the top (`g = iGpffff8504`).** 1497 edits.
  The load is not sunk.
- **Per-block handles (`h0`..`h6`).** 288 edits, worse than the draft.

## Evidence on arg6 (measured once, worse)

- `func_002b6af0`'s last parameter is `s32 delay` (fcl_draw_task.h).
- Retail passes arg6 raw to it (`move $a3,$s4`).
- Retail recomputes `sext16(arg6) + 3`, followed by `sext16`, at each
  `func_002b6a70`.

That points to an s32 arg6 and `(s16)arg6 + 3` as the argument, rather than
the draft's s16 parameter. The draft's s16 parameter is what b210 CSEs into
`$s5`. The only caller passes a constant.

Measured with the file extern switched to s32 as well:

- s32 arg6 with `(s16)arg6 + 3` on the draft gives 725 edits and 1465
  instructions.
- On the ga/gb probe it gives 360 edits and 1471 instructions.

Both are worse than the draft. The draft's local
`func_002b6a70(..., s16)` extern still narrows each argument again. It also
breaks the new one-signature rule, so it has to be reconciled first.

Retail's `$s5` holds the CSE'd `(s8)arg7`.
