# Statistics accumulator 0052d9a0

`func_0052d9a0` in `src/promoted/code1_0052.c` (an ee-gcc 2.96 `-O2 -G0`
unit) is now C and verifies as MATCH: 60 code bytes in a 96-byte window
(36 bytes of zero padding), no relocations. No other function in the file
changed status.

## Lever

The earlier floor (2 words, `GA52_0052d9a0_body.c`) loaded all four fields
into locals, did the min/max `if`s, then stored. Every order of those
statements scheduled movn-max, movn-min, daddu; retail has movn-max, daddu,
movn-min.

Writing the updates straight through the pointer, with the total first and
the min/max as conditional expressions, gives the retail schedule:

    acc->total += value;
    acc->min = (value < acc->min) ? value : acc->min;
    acc->max = (acc->max < value) ? value : acc->max;
    acc->count++;

Measured alternatives (instruction edits against retail): the same
four statements with plain `if` stores and max first: 16; ternaries with max
first and total last: 14; ternaries over `mn`/`mx` locals: 15; the original
all-locals form with `total` added between the `if`s: 8.
