# Shuffle card drawing 003753f0

`src/Battle/btlShuffleDraw.c` now contains the native C renderer for
`func_003753f0`. Configured MWCCPS2 b210 with `-O2 -Iinclude` emits 1,016 bytes
inside the 1,024-byte retail window. All thirteen relocations resolve exactly;
the final eight bytes are zero alignment.

The renderer prepares the active cards, queues their draw callbacks by depth,
dispatches that queue, then draws the mode-dependent effect. Each callback
payload points to an initialized slot in the complete thirty-card stack array.
The queue is dispatched before those slots expire. The callback reads one
card pointer from its slot. Its type is now `void (*)(u8 **)` consistently in
the callback definition, queue-provider declaration and definition, stored
node field, and dispatcher. The payload is likewise consistently `u8 **`.
These interfaces have no other authoritative source callers.

## Native source reconstruction

The phase counter is an unsigned word holding a loaded halfword; its direct
float conversion reproduces the original unsigned conversion sequence. The
first flag lookup uses an unsigned index product. Following the opaque card
update callback, the separate signed-index expression reconstructs the matrix
base at the point where retail reloads it.

The mode dispatch preserves its original branch order: modes four and three
use zero scale, mode two divides the group count by three, mode one divides
it by two, and mode zero directly reads the phase. The final zero/default
split is represented as a switch to retain both branches. The default path
leaves the phase counter without a new assignment, as retail does; the
recovery does not invent an initialization for unsupported modes.

The card pointer's declaration follows the unsigned phase word. This retains
the original saved-register lifetimes. No extra stores, dummy locals, register
bindings, assembly operations, or patched code bytes are introduced.

## Proof

The complete native owner retains all other functions' instruction bytes and
canonical relocation meanings. Allocated data is unchanged. The queue-provider,
callback and dispatcher retain their original native instructions despite their
corrected source types. The final reviewed source was compiled again after
renaming local values and archiving stale nonmatching comments; the target and
all sibling comparisons remain exact.

The resolved target SHA-256 is
`1ef659b1f01918a471f43fe9543244de74f03baf67f8a3651e81089f4acec4b5`.
The raw target SHA-256 is
`2a7895bd426778601fc41e89f8717ffc5a613b30242bd447ab47c4785ba2716c`.

The discovery and callback audit are retained in
`build/finish-first-party-20260922/continue-24-shuffle-draw/`.
Final source/object proofs are in `continue-24/persona/shuffle-reviewed/`;
installed-source and full-build publication receipts are in
`continue-24/shuffle/`.
