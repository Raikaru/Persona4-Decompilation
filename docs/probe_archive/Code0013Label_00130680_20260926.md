# code1_0013 `func_00130680`: label row draw, MATCH (2026-09-26, round 2)

Window 1456 B, object 1448 B plus 8 B of zero padding. Relocations: the
`func_0011fd30`, `func_0011fd50`, `func_0034f2e0` and `func_00104c70` calls, the
six `D_0064B2xx`/`D_0064B30x` colour rows and `fGpffff82cc`.

Started from the round 1 body (`Code0013Label_00130680_body_20260926.c`, 35
words). Levers that closed it:

- The file-scope `func_0011fd30`/`func_0011fd50` externs now agree with the
  code1_0011 definitions (`u8 *` and `(Vec2f, f32, s32, u8 *, s32)`); the old
  guarded `func_0013ad40` draft calls were updated to the same forms.
- Slot x/y reads as f32-array views, `((f32 *)(arg0 + 0x1618))[arg1 * 12]`.
  The byte-offset spelling `*(f32 *)(arg0 + 0x1618 + arg1 * 0x30)` made the
  hoisted slot pointer `arg0 + row`; retail adds `row + arg0` (35 -> 34).
  A `LabelSlot` view for x/y fixes the order too but hoists y as `px + 4`.
- The last sprite pointer is a local, `spr = *(void **)(arg0 + 0x1BE0);`,
  compiled with `opt_propagation off` (push/pop around this function only). Retail
  loads it before the `(u8)(alpha * dim)` conversion; with propagation on, the
  load sinks into the call's argument list after the conversion (34 -> 0).
  Without the local, the pragma does nothing (34).

The twin `func_0013ad40` has the same final-call ordering (`lw $4, 0x1330($20)`
before the conversion) and its slot pointer is also `row + arg0`, so the same
two levers should carry over.

## Twin `func_0013ad40`: MATCH (same day)

Window 1584 B, object 1580 B. Written from the matched `00130680` body: the
slot array is at `arg0 + 0x594` with `slot = arg2 ? 0x1C : 5` and
`slot2 = arg2 ? 0x18 : 1`, the digit count is an `s16` of the `u8` result
(signed `div`), and a third palette set is used when `arg2 == 0` and flag
0x10 is set. First compile was 1 word off: `beq $s3, $v0` against retail's
`beq $v0, $s3`. Reading the selected entry into a local
(`selected = ((s16 *)(arg0 + 0x5C))[arg2]; if (selected == arg1 || ...)`)
closes it; swapping the `==` operands or using a byte-offset address does not.
cmpSkill.c's `func_0013ad40` declaration now takes `u8 *` like the definition.
