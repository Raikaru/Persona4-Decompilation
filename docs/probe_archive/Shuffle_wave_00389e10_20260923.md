# Shuffle wave renderer: 00389e10

`func_00389e10` is recovered as ordinary C with **1,488 / 1,488 native bytes**
and **26 independently resolved relocations**. The other 94 native functions
and all allocated data in its owner are unchanged. The target introduces no
optimization pragma, assembly, volatile state or new calls.

The four wave groups retain their two direction channels, halfword counters,
normalization, sinusoidal displacement and 62-vertex rendering buffer. X and Y
interpolation products are distinct source values before the existing position
and normal contributions. The vertex counter belongs only to that phase; the
later 31-pair distance scan reuses the phase workspace through its actual union
member. Byte color snapshots and the existing render-state calls retain their
retail order.

The native frame is `0x1040` bytes. Normals occupy `sp+0x90`, direction/pair
storage occupies `sp+0xA0`, and 62 vertices of `0x40` bytes occupy
`sp+0xB0..sp+0x102F`. The distance vector starts at `sp+0x1038`. An eight-byte
allocation gap before it is explicitly represented but never initialized or
read. The direction and pair arrays each contain two complete vectors, and
all vertex indices stay within the 62-element buffer.

The reviewed source is retained in
`build/finish-first-party-20260922/resume-20-shuffle/package-00389e10-presentation/`.
The final installation also removes duplicate target-marker and obsolete
nonmatching prose; those old notes are retained in the integration scratch
directory. Native source/object hashes, complete target proof, official
installed-source verification and sibling/data preservation records are in
`build/finish-first-party-20260922/resume-20-integration/`.
