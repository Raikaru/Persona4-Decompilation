# Field next wave: exact world sphere and bounded dynamic frontier

Worker 1, September 21, 2026. Scratch root: `build/next-wave-20260921/field`.
No production source, include, config or tool files were changed by this worker.
The live field owner was released to prime after all compiler processes in this
lane exited. These results remain tied to the frozen inputs, independently of
any later coordinated integration.

## Authenticated starting point

HEAD was `188437938339ae6f47b39445e5c4f2b59a211dff`, with existing dirty recoveries.
The owner SHA-256 was `a3961825d1ccb35369832bda431fe85340725858f04451ed76afd512734bdaef`.
The published owner object was
`665cdfb74f8e49c1312c9bc41d647efa8a38e065f5a95698eb9d0f67bdf3e7d8`.
Current source/header/config/compiler identities matched the saved publication.
Every one of its 16 functions was re-resolved against retail. In particular,
native `00169320` retained 1112/1120 bytes, 3 relocations and an eight-byte zero
suffix; native `00169a30` retained 1668/1680 bytes, 8 relocations and a twelve-byte
zero suffix. The three remaining ASM functions were recorded as ASM, not native.
See `authentication.json` and the `00169780/production/proofs` receipts.

The previous sphere frontier was authenticated at 680/688 bytes with three
differing words at +0x1c0, +0x1c4 and +0x1c8, then rebuilt against current inputs
to the same object hash. The later dynamic `pair-count-more-than-one` frontier
was authenticated at 2124/2128 bytes and 21 differing words with 39 relocations.
32 prior completed sphere measurements and the dynamic family results were
inspected before new experiments.

## Exact func_00169780

`00169780/reviewed-sphere-native` is the review-ready native proposal.
It emits **680/688 bytes, zero differing words, 8 resolved relocations**, and the
remaining eight retail bytes are all zero. Its complete owner reports
**14 MATCH / 2 ASM** using the standard verifier with the completed, source-bound
object. All 15 siblings retain their bytes and relocations; all allocated data
is unchanged. No owned target data is present.

Final source SHA-256: `ae3f5d2d77c7a9734380152602ac47b84f2e76e07bf5862fc986791ec45b07e8`.
Final object SHA-256: `5e03e55dca092266bde5afc77471496f14254cba6aa446556ec0293d58626ec2`.
The target's resolved bytes hash is
`e9866360f07ac61cdc9866e58f85eea75859c2f47ab15eb5920ae1b55b77f543`.

The three-word residual was an ordering difference around the first Y product
and the zero comparison value. Enabling common-expression reuse allowed an
ordinary staged Y, X, Z projection to reproduce the retail accumulator chain.
Doing this for the whole function initially cached the distance address across
normalization. Retaining the post-normalization displacement as a signed
contact-index scale restored its separate source lifetime. Keeping that address
inside the complete work object's byte representation then matched the retail
three-instruction address construction. Both scales are valid for the bounded
contact index; no barriers, fake operations, volatile forcing or assembly were
introduced.

The work layout retains the complete shared collector and aligned sphere/query
copies. Normalization receives a genuine `f32[3]` union view. The obsolete
nonmatching floor and guard were removed only in this scratch native proposal.
The owner-local memset declaration is corrected to `void*(void*, s32, u32)`, as
confirmed by the complete provider ASM's pointer return and unsigned count.

Review files are `sphere-native.patch`, `sphere-native.apply.patch`, `seal.json`,
`before-inputs.json`, `compile.json`, `resolved-proof.json`, and the per-function
`proofs` and `official` directories. `git apply --check` passed. Lint returned
zero errors and two existing sibling pragma advisories.

## Related func_0016a110

The bounded best-current proposal is `0016a110/reviewed-dynamic-frontier`:
**2124/2128 bytes, 18 differing words / 18 edits, 39 resolved relocations**, with
a four-byte retail zero suffix. All 15 siblings were independently re-proved;
their bytes/relocations and allocated owner data are unchanged.

Native-probe source SHA-256: `c3c0db7b6f79d0aefe8c971622017171d441b77bc94376628d7421612fa326b2`.
Native-probe object SHA-256: `4b20319e05984fd30bed6e863b5db1cca3435d9f8405075fda7025044e498f05`.

Two genuine contracts were corrected without degrading the 21-word baseline:
the model getter takes a model pointer (its recovered provider is
`mdlGetClump(void*)`, retail `lw v0, 0xdc(a0); jr ra; nop`), and the per-contact
normalizer receives an actual three-float array. The address-name alias is kept
with the provider-compatible pointer prototype. A canonical-name experiment
compiled successfully but first exposed that the old scratch resolver reads
only recovered data symbols and address-encoded names. Analysis resumed on the
same completed object using the configured `mdlGetClump` address from
`config/symbol_addrs.txt`; the supplementary map/provider hashes are recorded.
The retained address-name proposal needs no resolver or production config edit.

Staging the opposed-plane projection made the stopped-vector zero register match
retail and reduced the word count from 21 to 18. Its scalar loads are now ordered
Y, X, Z rather than retail's up-front Z, X, Y loads. This is a measured frontier,
not an exact claim. The retained 21-word pointer-ABI proposal remains valuable
because it preserves the retail load order and differs mainly in FPR choices.

Remaining 18-word offsets:
`0x474, 0x478, 0x47c, 0x480, 0x484, 0x72c, 0x760, 0x764, 0x7a4, 0x7a8, 0x7ac, 0x7b0, 0x7b4, 0x7b8, 0x7bc, 0x7c0, 0x7c4, 0x7c8`.
They concern the initial distance base hoisted into a saved register and the
final pair search's threshold/zero order and projection loads/registers.

The review patch is **`dynamic-guarded.patch`**: its `NON_MATCHING` guard and ASM
fallback are retained. The separate `reviewed-dynamic-guarded` owner was compiled
and verified **13 MATCH / 3 ASM**, with every function equal to the published
baseline. That ASM validation is not evidence that the 18-word native probe
matches. Patch check passed; lint found zero errors and the same two existing
pragma advisories.

Both review patches are based on the authenticated owner and include the same
memset declaration correction. Prime must apply that shared hunk only once when
combining the native sphere change with the guarded dynamic source improvement.

## Toolchain and immutable evidence

Configured logical owner: `src/Kosaka/Field/k_fldFrame.c`; MWCCPS2 b210 with
`-O2 -Iinclude`. No owner profile or shared header was changed.

- Compiler SHA-256: `286548490e2e902cfef21dcf39cd5af23766731585d90dea747f8781eadcafd7`.
- Header corpus SHA-256: `3f176647e4c84c4469f10912b9444efc13d25ed64aa76170d032db4052fe1352`.
- Profile SHA-256: `c557ba464de3b52165cee896538d648c35a76746bd5b6adbd2ec9bf5eb5765e0`.

Every new compile receipt carries source, header corpus, compiler and profile
hashes, together with the exact command and its frozen full input manifest.
`run-index.json` authenticates all completed sources/objects and identifies the
guarded ASM measurement explicitly. Existing artifacts were not overwritten.

| Measurement | Emitted/window | Differing words | Alignment edits |
|---|---:|---:|---:|
| 00169780 / actual-normalizer-array | 680/688 | 3 | 4 |
| 00169780 / current-full-owner-frontier | 680/688 | 3 | 4 |
| 00169780 / reviewed-sphere-native | 680/688 | 0 | 2 |
| 00169780 / staged-contact-helper-level3 | 704/688 | 73 | 39 |
| 00169780 / staged-contact-scoped-expression-reuse | 704/688 | 67 | 28 |
| 00169780 / staged-projection-integer-distance-reload | 676/688 | 90 | 46 |
| 00169780 / staged-projection-signed-distance-displacement | 680/688 | 3 | 5 |
| 00169780 / staged-projection-signed-object-offset | 680/688 | 0 | 2 |
| 00169780 / staged-projection-with-expression-reuse | 668/688 | 90 | 48 |
| 00169780 / staged-projection-with-propagation | 724/688 | 76 | 42 |
| 0016a110 / model-pointer-and-normalizer-array-contracts | 2124/2128 | 21 | 21 |
| 0016a110 / pointer-abi-with-address-symbol | 2124/2128 | 21 | 21 |
| 0016a110 / reviewed-dynamic-frontier | 2124/2128 | 18 | 18 |
| 0016a110 / reviewed-dynamic-guarded | 2128/2128 | 0 | 0 |
| 0016a110 / staged-opposed-plane-projection | 2124/2128 | 18 | 18 |
| 0016a110 / typed-opposed-normal-predicate | 2124/2128 | 22 | 22 |

The alignment edit count includes omitted zero suffixes; exactness is decided
by resolved bytes, every relocation, the complete retail window and zero-tail
validation, not that count.
