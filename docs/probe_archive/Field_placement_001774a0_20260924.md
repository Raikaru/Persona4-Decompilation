# Field placement command 001774a0

The recovered command in `src/Kosaka/k_command/k_command.c` emits 1,384 native
bytes in its 1,392-byte retail window under the configured MWCCPS2 b210 compiler
with `-O2 -Iinclude`. All 59 relocations resolve exactly, and the eight remaining
retail bytes are zero alignment. The other 25 functions retain their native
bytes and canonical relocations.

The command obtains a resource identifier and a placement identifier from the
script, searches the field placement list, then updates the selected model's
translation and rotation. The three supported resource branches retain their
original call order and their respective model offsets. The character branch
also updates attached field work; the second branch scales its additional model.

## Source corrections

The rotation axis is the three-float initializer `{0.0f, 1.0f, 0.0f}`. Its
compiler-owned 12-byte constant is independently proved at retail `0x005F1868`.
The previous split external loads and a record containing an integer pair and
a float did not reproduce the original aggregate-copy order. A local vector
initializer, scoped after the three initial calls, restores the paired load
and the float load before their stores.

The matrix API's tolerance record contains `Normal`, `Orthogonal`, and
`Identity`, exactly three floats. Replacing the four-float workspaces with this
record restores the remaining stack positions. The declarations now agree with
the actual matrix API: the tolerance getter returns an integer predicate, and
the optimizer and updater return matrix pointers. These return values remain
unused by this command.

The field-work test keeps the two conditional memory reads and converts its
integer status to a normalized predicate through an inline value boundary.
That boundary emits the retail unsigned nonzero test. A fused logical expression
instead emitted two separate nonzero tests and omitted part of the retail
branch sequence. No volatile accesses, new side effects, register bindings, or
assembly are used.

## Verification

`continue-63-command/named-source/` retains the final source, native object,
compiler receipt, instruction comparison, complete target relocation proof,
and the axis constant proof. The fully resolved target SHA-256 is:

`4ebc05dfbb84b8231eac1f2bf61b4020730753c9d3e89a04e99136e9b53cd1ca`

`continue-63-command/preflight.json` proves all 26 owner functions, all five
allocated data sections, and actual C-link eligibility with no missing symbols.
The four previously owned data sections are preserved; the only addition is
the independently verified axis initializer. The directory paths above are
relative to `build/finish-first-party-20260922/`.

Installed-source verification, full-build hashes, linkage preservation, and
publication receipts are recorded separately in `continue-63-integration/`.
