# Campaign group 0, round 4 (2026-09-26)

Scorer: aligned instruction edits (difflib over normalised disassembly).
Compiler: MWCC b210 `-O2`. All five drafts are fresh rewrites from the retail
listing, not edits of the old guarded drafts.

## Matched

- func_0015f000 (k_fldFBN.c, FBN part loader, 1536 B). See
  FldFBN_0015f000_20260926.md. Contract change: func_0015d1a0
  (k_fldNpcTable.c) now takes `(u8 *entry, s32 index)`, the same pair as
  func_0015d100.

## Banked (not installed)

| Target | Old draft | Now | Body / blocker |
| --- | ---: | ---: | --- |
| func_0015e960 | nd 331, 1712 B | nd 71, same size | FldFBN_0015e960_20260926_body.c. Needs the u16 func_00478140 contract (same as func_00162680). `out` / entry-pointer colouring still differs. |
| func_001a43a0 | 83 edits | 45 edits | Code1a_001a43a0_20260926_body.c. Five-register rotation, in-memory s16 `kind`, index recompute. func_001fa320/func_002027e0 are defined `void` but return their task. |
| func_00138490 | 309 edits | 68 edits | CmpSkill_00138490_20260926_body.c. sel/extended-sel colouring and the dedup source pointer. |
| func_002add90 | 203 edits | 93 edits | YSmap_002add90_20260926_body.c. Saved-register roles are permuted. |

## Measured, not changed

- func_002af3e0: a fresh rewrite reached about 202-234 edits (the old draft
  was about 254). Early loops match up to register choice. The label / YVec2f
  block still differs: retail copies each func_002b2970 result from a
  temporary and CSEs the 1.0f/0.1f arguments into f12-f15. Not banked. The
  levers found are recorded here:
  - `arg1` is passed raw to every callee and sign-extended only for the
    `== 0` test. The callees' `u8` definitions add an `andi` that retail
    does not have. A local `s8` prototype removes it, but that disagrees
    with the y_symbol.c definitions, so it is a contract question.
  - The -0x7AFC gp float is the pooled literal 0.1f.
  - 60008 is passed as an int to an f32 parameter; the conversion is not folded.
- Not attempted this round: func_0017acc0, func_00179fc0, func_004aed70
  (153 FPU ops), func_002b1520, func_002ac750, and the large code1_001a / 0016 /
  CmbCardEff targets.

## Reusable levers

- b210 keeps a dead loop counter when the same C variable is also live in
  a later loop (func_0015e960). With block-scoped counters it is deleted.
- Retail sequences like `sltu x, $0, live; li 1; bne` come from
  `(u8)(live != 0) == 1`. Plain `(live != 0) == 1` folds to beqz.
- Struct-array accesses written as byte offsets
  (`(T *)((u8 *)base + i * sizeof(T))`) give retail's `addu base, off`.
  Array indexing gives `addu off, base` (func_0015f000).
- Caller-side coalesced copies (`else if (arg1 != NULL) fbn = arg1; else
  return 1;`) reproduce retail's `beqz; b` pairs.
- A fast scorer that skips fndiff's whole-tree marker scan (about 0.3 s per
  compile) makes declaration-order hill-climbs practical. Callee-saved
  ranking was usually last-declared-highest in these targets, but block
  locals and CSE temporaries do not follow it.
