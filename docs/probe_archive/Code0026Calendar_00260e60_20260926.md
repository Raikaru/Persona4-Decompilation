# code1_0026.c `func_00260e60` (1792 B): MATCH

Fresh rewrite from the retail listing (round 4 of campaign group 3). The old
guarded draft was 401 words off; it was replaced, not edited.

- Signature from the prologue move order (a0, a1, f12, a2, a3, t0, t1, f13,
  f14, t2, t3):
  `(s32 x, s32 y, f32 depth, u8 alpha, s32 kind, s32 variant, s32 highlight,
  f32 scaleX, f32 scaleY, s32 font, s32 setupStates)`. `alpha` is `u8`: it is
  passed unmasked to two `u8` parameters.
- `func_00260600`'s real order is the same shape
  `(x, y, depth, color, u8 alpha, variant, scaleX, scaleY, font, 0)`. Retail
  passes `alpha` without `andi`, so its fourth integer parameter is `u8`. The
  guarded 00260600 definition and a file-scope prototype now say so (both are
  ABI-identical to the old `s32 x7 + f32 x3` order). The six calls to
  00260e60 in the guarded 00261560 draft are rewritten to the new order.
- Relocations: `D_00887300` (render-state table, base kept in `$s6`),
  `D_006375C0` (30 `{s32 x, y}` offsets, copied to the stack), gp `-0x7E2C`
  (the pooled literal `0.707f`, written as the literal; the unit still links,
  594/594 and both SHA-1 OK), `func_003f6440`, `func_00260600`,
  `func_0025f430`.

Levers:
- `#pragma opt_propagation off` (push/pop) keeps `states = D_00887300;` in a
  saved register inside the `if (setupStates)` block, the same as the matched
  sibling `func_00262de0`. With propagation on, the base is either rebuilt
  per call (assignment inside the block) or hoisted above the `beqz`
  (assignment before it; 4 words).
- The sparkle table copy is `sparkle = *(CalendarSparkleTable *)D_006375C0;`
  with `D_006375C0` declared as `CalendarSparkle[30]`. A struct-typed global
  gives a 16-byte `lq`/`sq` copy loop; the cast gives retail's 8-byte
  `lw/lw/sw/sw` loop.
- `spot = &sparkle.offset[i];` as its own statement gives retail's full
  `sp + 0xB0 + i * 8` address before the two loads (propagation off keeps it).
- With propagation off, the `0.707f` load is hoisted to the top of the
  scaled cases (into `$f2`) unless the position arguments are staged first:
  `posX = ((f32)x + 0.0f) - 3.0f * scaleX; posY = (f32)(y - 4);` then the
  call. 32 -> 0 words. Staging only `posX` gives 23; a `half` local or
  swapped operands give 32.
- `u32 i` (`sltiu`), `case 3: break;` in the kind switch (retail compares
  4, 3, 2, 1, 0), and three identical 00260600 calls per variant.
