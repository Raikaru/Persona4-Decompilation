/* object 2240B / window 2240B / normalized_diff 0; differing offsets: none (ASM baseline); classification: COP1 accumulator-chain floor; ruled out: retail mula.s/msub.s and repeated adda.s/madd.s interpolation chains across all animation branches, which plain MWCC C cannot emit; no source probing per floor policy. */
void func_00386c00(u8 *arg0)
{
    /* Retail's animation interpolation is COP1 accumulator based. */
    (void)arg0;
}
