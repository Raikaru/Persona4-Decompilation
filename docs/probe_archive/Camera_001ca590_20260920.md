# Framing camera: func_001ca590

The recovered C emits 1,852 bytes, exactly matching the executable prefix
of the 1,856-byte retail window after all 40 code relocations are applied.
The remaining four retail bytes are zero alignment. The current-source
verifier reports 53 MATCH / 10 ASM for `src/promoted/code1_001c.c` and exits
zero. All 62 other functions retain their bytes and canonical relocations;
every allocated data section is unchanged. This includes the `001c5500`
recovery integrated earlier in this run.

## Objects, operations, and lifetimes

The original guarded draft emitted 1,860 bytes and had 259 fully resolved
word differences. Its explicitly padded `Work` aggregate is replaced by
complete, independently declared vectors, two `P4CameraFrame` poses,
two quaternion objects, a real `RwMatrix`, and the two-float query buffers.
The compiler now reproduces the retail stack layout through ordinary
object allocation. The selected base position uses a normal `RwV3d`
assignment rather than casts to an integer-copy helper.

The horizontal and three-dimensional dot products use coordinate order.
This recovers the retail fused operations and removes an extra instruction.
The combined framing extent is accumulated in three stages, then reused
for the selected unit's radius after the camera distance is calculated.
The half-height uses direct multiplication, and the final lateral offset
is scaled in two stages, preserving retail's operand order. The existing
small multiply helper remains in the two height contributions, where its
evaluation order agrees with retail; no helper was added.

These changes reduced the residual to a swap of `$f21` and `$f23` across
the half-height and angle/distance lifetimes. Declaring the distance before
the height/extent values and the half-height after the tangent values
recovered the final allocation. The final implementation has no new
optimization pragmas, artificial padding, volatile accesses, or synthetic
operations. The measured scoped lifetime control did not close this
candidate and was not adopted.

The alternate-view condition retains retail's negated ordered comparison:
`!(fabsf(alignmentDot) <= fabsf(alternateDot))`, followed by both nonzero
checks. An ordinary `>` comparison from the old draft loses the unordered
case. The signed side selection, explicit zero additions, and vertical
self-subtraction also retain their retail floating-point behavior.

## Call and storage contracts

The camera-first `u8 *, f32, f32` interface agrees with both calls in
`001cb0f0`; they still pass angles of 40 or 20 and a duration of two.
Those caller bytes and relocations are unchanged. The source uses the
typed sphere-center and normalization providers and complete quaternion
objects at the quaternion-angle calls. Both XZ queries receive two floats.
The matrix replacement operation initializes its complete matrix before
the point transform. The first frame is copied in full; the second
quaternion is initialized by the look-at operation, and its position is
filled before the complete pose is submitted. The optional alternate
rotation path initializes each quaternion before comparing it.

All paths initialize the selected extent and base position. The cached
base Z coordinate is loaded before the length query and remains valid
through both look-at candidates. Like retail, the function requires a
valid battle-camera action, actor, and first target record. The check is
source/storage review plus exact compiled instructions, not an emulator
execution claim.

## Receipts and reproduction

`build/first-party-next/camera-resume-a590/` contains immutable source,
header, provider, compiler-support, retail assembly, and decompiler-export
inputs. Its `before/owner.o` reuses the verified current-source object from
the preceding `001c5500` seal after source/compiler/dependency hash checks.
The exact final candidate is `reviewed-contract/`. All attempted candidates
retain their own compiler commands, objects, relocation records, and diffs.

The current-source seal is
`build/first-party-next/camera-resume-a590/release-final/integrated-proof.json`.
The adjacent `official.json`, `official.log`, and `live/` directory record
the verifier output and retained current-owner object. The configured
owner compiler is MWCC 3.0.1b210 with `-O2 -Iinclude`. No full-image or
full-tree compile was run.

```text
python tools/verify.py src/promoted/code1_001c.c --show-mismatches --json build/camera-a590-verify.json

resolved executable-prefix SHA-256
12b83a2d86cf0ffbc792b3f0402329e35e475207f6ddbd0d898b2df2f001a080

code1_001c.c source SHA-256 at this seal
b4b06056e905b4698f5e9743390258576c6a47f389e64d7e27b2faf103dc18d1

current-owner object SHA-256
8a23602d14c36e02b1046cc1f3f07f26cbae00d3661b97da5bdea5a2f0765b22
```
