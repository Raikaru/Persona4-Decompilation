/* object 240B / window 240B / normalized_diff not measured / differing offsets not measured / classification confirmed compiler floor; retail uses COP1 accumulator chain mula.s $f3,$f1; madda.s $f5,$f4; madd.s $f1,$f7,$f6 in the loop, which plain MWCCPS2 C cannot emit. Triage stopped before probing. Also ruled out movz/movn, standalone MMI, framed tail jump, custom COP1 sqrt, and rd-form mult floor by direct retail window inspection. */
