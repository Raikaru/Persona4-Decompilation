# Campaign group 2, round 3 (2026-09-26)

Scorer: fnalign edits, MWCC b210 `-O2`.

## Matched

- func_001ee610 (route smoothing, 1600 B), see RouteSmooth_001ee610_20260926.md.

## Banked

| Target | Before | Now | Note |
| --- | ---: | ---: | --- |
| func_001e9950 | 130 | 97 | SkillPick_001e9950_20260926.md (goto-free rewrite; blocked by three separate kind sign-extensions) |
| func_004b8350 | 350 | 135 | AfterMesh_004b8350_20260926.md |

## Measured, not changed

- func_0036f880 (22): variants for retail's unfolded `addiu 0x72` / `addiu 0x70`
  candidate reads all stay at 22 or get worse. Tried: struct pair, `u16 [8][2]`, `u16 [16]` with
  `r * 2 + 1`, pointer temps, u8 / u32 integer address arithmetic, and reading
  through pointers at every use. Declaration and scope changes for the
  `$s0`/`$s1` count/(s16)i swap had no effect. A 25-minute permute_ast run
  gave only mutated junk.
- func_0036ee60 (88 with `rec = iGpffffb3d4; rec += i * 14;`, was 90):
  block-scoped shuffle counters make it worse (137). A 14-byte record struct
  makes no difference. Declaration-order climbing found nothing.
- Pooled-literal lever on the fGp-heavy drafts:
  - func_00475cd0: 621 -> 620. The block is RenderWare's
    `RtQuatSlerpMacro` with `RwSinMinusPiToPiMacro`: `_RW_S1..S6` are
    fGpffff81cc, 8060, 805c, 8058, 8054, 81fc. fGpffff81f8 is 0.707107f and
    fGpffff80f0 is 0.01f. The draft's stack layout is wrong: quaternion at
    0x70, result at 0x80, RtQuatSlerpCache at 0x90 with omega at 0xB0 and the
    flag at 0xB4. It needs a rewrite, not literals.
  - func_0046b380: 2748 -> 2753. The gp floats are the RW cos (`_RW_C1..C6`
    = fGpffff81c4..81b0) and sin series and pi. The draft is too far off for
    literals to matter.
  - func_001ee610: its gp floats were literals (0.173648f, `1.0f - 0.65f` =
    0x3EB33334, 0.65f, 0.226563f), which were part of the match.
- func_00225ec0 was not attempted: the P022 audit shows that retail reads an
  uninitialised stack slot (sp+0x134).
- Re-measured drafts, not attempted further (words / edits):
  func_0047b0c0 has no draft;
  func_004bb1d0 618/314; func_004b8f40 1807/1756; func_00148280 737/226;
  func_001400f0 1686/1823; func_00131a00 2524/1628; func_00222210 652/310;
  func_003694d0 904/350; func_004afe20 722/671.

## Levers confirmed this round

- Local declaration order sets stack order in reverse: the first declared
  local gets the highest address.
- A two-element array (`seg[2]`, `seg[0] = seg[1]`) copies with lwc1/swc1
  pairs. Two plain 8-byte struct locals copy with ld/sd.
- A copy that retail makes through a pointer variable set before a call,
  e.g. `goal = base + 0x90C; ... else { node = goal; while ... }`, keeps
  retail's empty `b cond` trampoline.
- Loads of the same pointer through different types (`*(s32 *)arg0` against
  `*(u8 **)arg0`) are not CSE'd. This reproduced one of retail's reloads in
  004b8350.
